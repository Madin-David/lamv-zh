#import "@local/evan-zh:1.0.0":*

= 区域

在 18.02 中，你会被要求在某个*约束区域*（constraint region）$cal(R)$ 上求全局最小值或最大值，
而它只是 $RR^n$ 的一个子区域。
例如，如果给你一个三变量函数 $f(x,y,z)$，
你可能会被问到下面这样的问题

- $f$ 在整个 $RR^3$ 上的全局最大值是多少（如果存在的话）？
- $f$ 在卦限#footnote[
    就像 $x y$ 图像中的“象限”一样。如果你以前从未见过这个词，可以忽略它。
  ] $x,y,z > 0$ 上的全局最大值是多少（如果存在的话）？
- $f$ 在由 $-1 <= x,y,z <= 1$ 给出的立方体上的全局最大值是多少（如果存在的话）？
- $f$ 在球面 $x^2+y^2+z^2 = 1$ 上的全局最大值是多少（如果存在的话）？
- ……等等。

事实证明，思考约束区域其实就占了问题的一半。
在 18.01 中，你通常不必在这方面多想，
因为你拿到的区域总是区间，这让事情变得简单。
但在 18.02 中，你需要多花多得多的注意力。

#warning(title: [警告：如果你有读证明的能力，请读进阶版本])[
  整整这一章都会用到大量含糊其辞的术语，
  而我并没有真的给它们下定义。
  如果你是一名准备数学竞赛的高中生，
  或者你是一个能读懂证明的人，
  请改读 #url("https://web.evanchen.cc/handouts/LM/LM.pdf") 处的版本。
  在那里我们用开集/闭集和紧性来把事情做对。
]

== [TEXT] 约束区域

#digression(title: [题外话：一堂关于 circle 与 disk、sphere 与 ball 的英语课])[
  为了对一些在英语中容易混淆的词保持谨慎，
  我将采用以下约定：

  - *circle*（圆）一词指一维的对象，没有内部，例如 $x^2+y^2=1$。
    它没有面积。
  - *open disk*（开圆盘）一词指严格位于圆内部的点，例如 $x^2+y^2 < 1$
  - *closed disk*（闭圆盘）一词指一个圆以及它内部的全部点，例如 $x^2+y^2=1$ 或 $x^2+y^2<1$。
  - *disk*（圆盘）一词指开圆盘或闭圆盘。

  类似地，*sphere*（球面）只指那个曲面，而不是球体，例如 $x^2+y^2+z^2=1$。
  于是我们有 *open ball*（开球）、*closed ball*（闭球）和 *ball*（球），用类似的方式定义。
]

在 18.02 中，我们遇到的所有约束区域都由若干个（可能为零个）
等式和不等式构成。
我们提供几个例子。

#example(title: [例：$RR$ 中的区域])[
  在 $RR$ 中：
  - 整个 $RR$，不加任何进一步条件。
  - 开区间，例如 $RR$ 中的 $-1 < x < 1$。
  - 闭区间，例如 $RR$ 中的 $-1 <= x <= 1$。
]
#example(title: [例：$RR^2$ 中的二维区域])[
  在 $RR^2$ 中，一些二维区域：
  - 整个 $RR^2$，不加任何进一步条件。
  - 第一象限 $x, y > 0$，不包含坐标轴
  - 第一象限 $x, y >= 0$，包含正的 $x$ 轴和 $y$ 轴。
  - 正方形 $-1 < x < 1$ 且 $-1 < y < 1$，不包含正方形的四条边。
  - 正方形 $-1 <= x <= 1$ 且 $-1 <= y <= 1$，包含四条边。
  - 开圆盘 $x^2 + y^2 < 1$，即去掉圆周的实心单位圆盘。
  - 闭圆盘 $x^2 + y^2 <= 1$，即包含圆周的实心单位圆盘。
]
#example(title: [例：$RR^2$ 中的一维区域])[
  在 $RR^2$ 中，一些一维区域：
  - 单位圆 $x^2 + y^2 = 1$，它是一个半径为 $1$ 的圆，没有填充。
  - 同时满足 $x^2+y^2=1$ 与 $x,y > 0$：一段四分之一圆弧，不包含 $(1,0)$ 和 $(0,1)$。
  - 同时满足 $x^2+y^2=1$ 与 $x,y >= 0$：一段四分之一圆弧，包含 $(1,0)$ 和 $(0,1)$。
  - 方程 $x + y = 1$ 是一条直线。
  - 同时满足 $x + y = 1$ 与 $x,y > 0$：一条线段，不含端点 $(1,0)$ 和 $(0,1)$。
  - 同时满足 $x + y = 1$ 与 $x,y >= 0$：一条线段，包含端点 $(1,0)$ 和 $(0,1)$。
]

我本可以为 $RR^2$ 举出多得多的例子，
而我还没讲到 $RR^3$ 呢。
这就是为什么约束区域的情形在 18.02 中比在 18.01 中需要更多思考
（而在 18.01 中，基本上只会出现少数几种例子）。

为了进一步讨论区域，我必须引入一些新词。
这门课里你应当关心的三个词是：
“边界”（boundary）、“极限情形”（limit cases）和“维数”（dimension）。

#warning(title: [警告：这里全都要拼命地挥手示意])[
  据我所知，在 18.02 中无法给这些词下精确的定义。
  所以你只能随机应变。
  下面所有条目都是经验法则，在 18.02 中行得通，
  但在 18.100/18.900 中站不住脚。
]

- *边界*通常是这样的点：你挑出 $<=$ 和 $>=$ 约束中的任意一个，
  把它变成 $=$ 约束后所得到的点。
  例如，由 $-1 <= x <= 1$ 和 $-1 <= y <= 1$ 切出的区域
  （它是一个边长为 $2$ 的正方形）
  的边界是正方形的四条边，即要么 $x = pm 1$，要么 $y = pm 1$ 之处。

- *极限情形*有两种形式：
  - 如果任何变量可以趋于 $pm oo$，那么所有这些情形通常都是极限情形。
  - 如果你有任何 $<$ 和 $>$ 不等式，
    那么变量趋近那些严格界的情形通常都是极限情形。

- $cal(R)$ 的*维数*最难用言语定义，却最容易猜。
  我给你两种猜它的方法：

  - 几何猜法：
    在 $cal(R)$ 中取一个不在边界上的点 $P$。
    观察 $cal(R)$ 中所有靠近 $P$ 的点，也就是一个小的邻域。

    - 如果这个小邻域可以被赋予_长度_，就说 $cal(R)$ 是一维的。
    - 如果这个小邻域可以被赋予_面积_，就说 $cal(R)$ 是二维的。
    - 如果这个小邻域可以被赋予_体积_，就说 $cal(R)$ 是三维的。

  - 代数猜法：
    $RR^n$ 中一个区域的维数通常等于 $n$ 减去约束中 $=$ 的个数。

  总的来说，在维数问题上相信你的直觉；你通常都会猜对。

下表总结了每种约束如何影响上面三个词中的每一个。
#figure(
  table(
    columns: 4,
    align: left,
    table.header([约束], [边界], [极限情形], [维数]),
    [$<=$ 或 $>=$], [改成 $=$ 即得边界], [无影响], [无影响],
    [$<$ 或 $>$], [无影响], [取极限即得极限情形], [无影响],
    [$=$], [无影响], [无影响], [使维数减一],
  ),
  kind: table,
  caption: [经验法则的效果。]
) <table-rule-thumb>

下面用一些例子来说明。
#example(title: [例：圆、开圆盘与闭圆盘])[
  见 @fig-regions-circles。

  - 圆 $x^2 + y^2 = 1$ 是一个*一维*图形。
    再强调一次，我们把这一区域视为_一维_的，
    尽管其中的点生活在 $RR^2$ 中。
    经验法则是：有 $2$ 个变量和 $1$ 个等式时，维数应当是 $2-1=1$。

    因为完全没有任何不等式约束，
    又因为 $x$ 和 $y$ 的绝对值都不可能大于 $1$，
    所以它没有*边界*，也没有*极限情形*。

  - 开圆盘 $x^2 + y^2 < 1$ 现在是*二维*的，
    因为给它指定面积是说得通的。
    （或者按经验法则：$2$ 个变量、$0$ 个等式时，维数应当是 $2-0=2$。）

    有一族*极限情形：当 $x^2+y^2$ 趋于 $1^-$ 时*。
    但它没有边界。

  - 闭圆盘 $x^2 + y^2 <= 1$ 也是*二维*的。
    因为 $x$ 和 $y$ 的绝对值都不可能大于 $1$，
    而且没有 $<$ 或 $>$ 约束，所以没有需要考虑的极限情形。
    但它有*边界 $x^2 + y^2 = 1$*。
]

#figure(
  image("figures/regions-circles.svg", width: auto),
  caption: [$x^2+y^2 = 1$、$x^2+y^2 < 1$ 和 $x^2+y^2 <= 1$ 的图示。],
) <fig-regions-circles>


作为我没有给出真正定义的补偿，
我将改为给你一大堆例子，以及它们的维数、边界和极限情形。
见 @table-1d-regions、@table-2d-regions、@table-3d-regions。

#figure(
  table(
    columns: 4,
    align: center + horizon,
    table.header([区域], [维数], [边界], [极限情形]),
    [整个 $RR$], [1 维], [无边界], [$x -> pm oo$],
    [$-1 < x < 1$], [1 维], [无边界], [$x -> pm 1$],
    [$-1 <= x <= 1$], [1 维], [$x = pm 1$], [无极限情形],
  ),
  kind: table,
  caption: [$RR$ 内区域的例子及其性质。]
) <table-1d-regions>

#figure(
  table(
    columns: 4,
    align: center + horizon,
    table.header([区域], [维数], [边界], [极限情形]),
    [整个 $RR^2$], [2 维], [无边界], [$x -> pm oo$ 或 $y -> pm oo$],
    [$x,y > 0$], [2 维], [无边界], [$x -> 0^+$ 或 $y -> 0^+$ \ 或 $x -> +oo$ 或 $y -> +oo$],
    [$x,y >= 0$], [2 维], [$x=0$ 或 $y=0$], [$x -> +oo$ 或 $y -> +oo$],
    [$-1 < x < 1 \ -1 < y < 1$], [2 维], [无边界], [$x,y -> pm 1$],
    [$-1 <= x <= 1 \ -1 <= y <= 1$], [2 维], [$x=pm 1$ 或 $y=pm 1$], [无极限情形],
    [$x^2 + y^2 < 1$], [2 维], [无边界], [$x^2+y^2 -> 1^-$],
    [$x^2 + y^2 <= 1$], [2 维], [$x^2 + y^2 = 1$], [无极限情形],
    [$x^2 + y^2 = 1$], [1 维], [无边界], [无极限情形],
    [$x^2 + y^2 = 1 \ x,y > 0$], [1 维], [无边界], [$x -> 0^+$ 或 $y -> 0^+$],
    [$x^2 + y^2 = 1 \ x,y >= 0$], [1 维], [$(1,0)$ 与 $(0,1)$], [无极限情形],
    [$x + y = 1$], [1 维], [无边界], [$x -> pm oo$ 或 $y -> pm oo$],
    [$x + y = 1 \ x, y > 0$], [1 维], [无边界], [$x -> 0^+$ 或 $y -> 0^+$],
    [$x + y = 1 \ x, y >= 0$], [1 维], [$(1,0)$ 与 $(0,1)$], [无极限情形]
  ),
  kind: table,
  caption: [$RR^2$ 内区域的例子及其性质],
) <table-2d-regions>

#figure(
  table(
    columns: 4,
    align: center + horizon,
    table.header([区域], [维数], [边界], [极限情形]),
    [整个 $RR^3$], [3 维], [无边界], [任一变量趋于 $pm oo$],
    [$x,y,z > 0$], [3 维], [无边界], [任一变量趋于 $0$ 或 $oo$],
    [$x,y,z >= 0$], [3 维], [$x=0$ 或 $y=0$ 或 $z=0$], [任一变量趋于 $oo$],
    [$x^2 + y^2 + z^2 < 1$], [3 维], [无边界], [$x^2 + y^2 + z^2 -> 1^-$],
    [$x^2 + y^2 + z^2 <= 1$], [3 维], [$x^2 + y^2 + z^2 = 1$], [无极限情形],
    [$x^2 + y^2 + z^2 = 1$], [2 维], [无边界], [无极限情形],
    [$x^2 + y^2 + z^2 = 1 \ x,y,z > 0$], [2 维], [无边界], [$x -> 0^+$ 或 $y -> 0^+$ 或 $z -> 0^+$],
    [$x^2 + y^2 + z^2 = 1 \ x,y,z >= 0$], [2 维], [三段四分之一圆弧#footnote[
      明确地说，第一段四分之一圆弧是 $x^2+y^2=1$、$x,y >=0$ 且 $z = 0$。
      另外两段四分之一圆弧类似。
    ]], [无极限情形],
    [$x + y + z = 1$], [2 维], [无边界], [任一变量趋于 $pm oo$],
    [$x + y + z = 1 \ x, y, z > 0$], [2 维], [无边界], [任一变量趋于 $0^+$],
    [$x + y + z = 1 \ x, y, z >= 0$], [2 维], [$x=0$ 或 $y=0$ 或 $z=0$], [无极限情形],
  ),
  kind: table,
  caption: [$RR^3$ 内区域的例子及其性质],
) <table-3d-regions>


#digression(title: [题外话：故意误导人、会打破经验法则的约束])[
  我对于展示这些有些犹豫，但下面是一些经验法则失效的例子：

  - 一个格外残忍的出题人可能会把单位圆改写成
    $ x^2 + y^2 <= 1 " 与 " x^2 + y^2 >= 1 $
    而不是更自然的 $x^2 + y^2 = 1$。
    这样一来，如果你盲目地套用经验法则，就会得到错误的答案。
  - 在 $RR^3$ 中，由单个方程
    $ x^2 + y^2 + z^2 = 0 $
    切出的区域实际上是 $0$ 维的，因为其中只有一个点：$(0,0,0)$。

  话虽如此，像这样故意误导人的约束很可能超出 18.02 的教学大纲。
]

== [RECIPE] 处理区域

这个方法是不能让人满意的，因为它不过是些经验法则。
但再说一次，对 18.02 而言，经验法则应当能应付所有考试题。

#recipe(title: [方法：由等式与不等式定义的区域的经验法则])[
  给定一个包含在 $RR^n$ 中的区域 $cal(R)$，要猜出它的维数、极限情形和边界：

  - 维数大概是 $n$ 减去 $=$ 约束的个数。
  - 极限情形是通过把 $<$ 和 $>$ 变成极限，
    并考虑何时任何变量可以趋于 $pm oo$ 得到的。
  - 边界是在任何 $<=$ 和 $>=$ 变成 $=$ 时得到的。

]

这些经验法则的总结见 @table-rule-thumb，
关于大量例子也请再次查阅 @table-1d-regions、@table-2d-regions、@table-3d-regions。
