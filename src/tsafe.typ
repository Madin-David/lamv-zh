#import "@local/evan-zh:1.0.0":*

= 类型安全

在开始讲线性代数与微积分之前，
我想先快速谈谈_对象的类型_。
这对于今后检查你的作答、
审查你对某个主题的理解，是一项重要的保障；
一位好的教师会在你的作业中随时指出你犯的类型错误。

== [TEXT] 类型错误

在数学中，命题通常或真或假。假命题#footnote[分别对应印第安纳圆周率法案（Indiana Pi bill）和《1984》。]
的例子
包括 $ pi = 16 / 5 #h(2em) upright("or") #h(2em) 2 + 2 = 5 . $
然而，有可能写出不仅为假、
甚至"语法上都不正确"的命题，比如下面这些无意义的等式
$
  pi = mat(1, 0; 0, 1) , #h(2em)
  mat(0, 1, 2; 3, 4, 5) = cos vec(6, 7), #h(2em)
  det vec(5, 11) != sqrt(2).
$
把这些等式称为假的会让人误解。
如果你的朋友问你 $2 + 2 = 5$ 是否成立，你会说"不"。
但如果你的朋友问 $pi$ 是否等于 $2 times 2$ 单位矩阵，
那答案是另一种"不"；
实际上是"你的问题没有意义"。

这三个例子都是*类型错误*。
这个术语来自编程：大多数编程语言都有
不同的数据类型，如整数、布尔值、字符串、数组等，
并且通常会#footnote[JavaScript 是一个显著的例外。
  在 JavaScript 中，你也许知道 `[]` 和 `{}` 分别是
  空数组与空对象。
  那么 `[]+[]` 是空字符串，`[]+{}` 是字符串 `‘[object Object]’`，`{}+[]` 是 `0`，
  而 `{}+{}` 是 `NaN`（not a number，即"非数"）。]
阻止你做出任何蠢事，比如把字符串加到数组上。

数学中的对象运作方式与之非常相似。
在 18.02 的头几周，你会遇到实数、向量和矩阵；
它们都是不同类型的对象，而某些运算根据其底层类型
是定义好的（也即"允许"的）还是未定义的（也即"不允许"的）。
表 @table-operations 列出了你在预备微积分中见过的关于向量的一些常见例子。

#figure(
  table(
    columns: 5,
    align: (left,center,left,left,left,),
    table.header([运算], [记号], [输入 1], [输入 2], [输出],),
    table.hline(),
    [加/减], [$a pm b$], [标量], [标量], [标量],
    [加/减], [$bf(v) pm bf(w)$], [$d$ 维向量], [$d$ 维向量], [$d$ 维向量],
    [加/减], [$M pm N$], [$m times n$ 矩阵], [$m times n$ 矩阵], [$m times n$ 矩阵],
    [相乘], [$c bf(v)$ 或 $c dot bf(v)$], [标量], [$d$ 维向量], [$d$ 维向量],
    [相乘], [$a b$ 或 $a dot b$], [标量], [标量], [标量],
    [相乘], [$M N$ 或 $M dot N$], [$m times n$ 矩阵], [$n times p$ 矩阵], [$m times p$ 矩阵],
    [点积], [$bf(v) dot.op bf(w)$], [$d$ 维向量], [$d$ 维向量], [标量],
    [叉积], [$bf(v) times bf(w)$], [$3$ 维向量], [$3$ 维向量], [$3$ 维向量],
    [模长/大小], [$lr(|bf(v)|)$], [任意向量], [_不适用_], [标量],
    [行列式], [$det A$], [任意方阵], [_不适用_], [标量],
  ),
  caption: [常见的线性代数运算。在 18.02 中，"标量"与"实数"是同义词。],
  kind: table
) <table-operations>

#digression[
  到这里一个常见的问题是：你该如何判断
  某个运算是否被允许。
  例如，许多学生想试着把两个向量按分量相乘；
  为什么
  $ vec(2, 3) vec(4, 5) =^? vec(8, 15) $
  不是一句合法的句子？
  它看起来好像说得通。

  答案是：你_不必_去判断——你是_被告知_的；
  @table-operations 并不是你要推导出来的东西。
  也就是说，@table-operations 由已经给你的那些_定义_构成。

  （或者更刻薄地说，这一切都只是社会建构。
  嗯，实际情况比这微妙一些。
  定义不以"正确性"来评判；
  那说不通；你想发明什么定义都可以。
  定义的好坏在于它们是否_有用_。
  这显然是主观的，但也没有你想的那么主观。）
]

== [TEXT] 为什么你应当在意

本章有两条行动要点。

=== 学习一个新对象时，先考察它的类型

它的意思是，每当你遇到一种新的
数学对象或运算（例如偏导数）时，
*你首先要问的是：这里涉及哪些类型*。
这有助于对你自己是否理解了这个新概念做一次合理性检查。

在整本教材中，我们会用像这样的方框来做这件事：
#typesig[
  这是一个类型签名（type signature）框的例子。
  当我们想对新对象的类型发表评论时，
  就会把它们放进这样的方框里。
]

=== 写等式时，务必确保类型能通过检查

实际上真正有用的是：如果你对类型掌握得好，
它就*给了你一种对作答做类型检查的方法*。
这相当于物理学中的量纲分析，
即如果某个等式左边是
$"kg" dot "meters" dot "seconds"^(- 2)$
而右边是 $"kg" dot "meters" dot "seconds"^(- 1)$，你就知道自己搞错了。

例如，如果你在检查自己的作答时看到类似
#eqn[
  $ lr(|bf(v) times bf(p)|) = 9 bf(p) $
  <eqn-wrong-type>
]
的东西，你马上就能判断这里有错误，
因为两边不相容——
左边是实数（标量），而右边是向量。

== [RECAP] 类型安全的要点

- 在本书中，每当你遇到一个新运算，
  务必弄清楚它以什么类型的对象作为输入，又输出什么类型。
- 每当你写下一个等式，务必确保它通过类型检查。
  仅凭类型安全，你就能发现像 @eqn-wrong-type 这样的许多错误。

== [EXER] 习题

#exer[
  设 $bf(u)$、$bf(v)$、$bf(w)$ 是 $RR^3$ 中的向量。
  利用 @table-operations（或粗略浏览一下 @sec-dot），
  判断下列每个表达式是
  实数、向量，还是无意义的东西（类型错误）；三者各有一个。

  - $(bf(u) dot bf(v)) dot bf(w)$
  - $bf(u) dot bf(v) + bf(w)$（这里运算顺序是 $dot$ 先于 $+$）
  - $bf(u) dot (bf(v) + bf(w))$

  （符号 $dot$ 令人困惑地可以指三种不同的东西：
  小学的乘法、数乘，或点积。）
] <exer-tsafe>

（本题的解答写在 @ch-sol-alfa 中，
一般来说 Lima 部分包含所有习题的解答。）
