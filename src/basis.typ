#import "@local/evan-zh:1.0.0":*

= 向量的线性组合

== [TEXT] 基的定义 <sec-basis>

回忆 @sec-lintransf-def 中我提出的以下两个问题：

/ Q1.: 若 $T : RR^2 -> RR^2$ 是线性变换，且已知
  $ T( vec(3,4) ) = vec(pi, 9) " 与 " T( vec(100, 100) ) = vec(0, 12) $
  那么 $T( vec(103,104) )$ 和 $T( vec(203, 204) )$ 这两个向量是多少？

/ Q2.:
  若 $T : RR^2 -> RR^2$ 是线性变换，且已知
  $ T( vec(1,0) ) = vec(1, 3) " 与 " T( vec(0,1) ) = vec(2, 4) $
  那么 $T( vec(50, 70) )$ 是多少？

现在，如果我想给你的生活增加点难度，我本可以问：

/ Q3.: 已知 $T( vec(3,4) ) = vec(pi, 9)$ 和 $T( vec(100, 100) ) = vec(0, 12)$，
  那么 $T( vec(13,37) )$ 是多少？

这比 Q1 难得多；不过这个问题仍然可解。
如果我把你锁在房间里一个小时让你做这道题，
我想你们中的许多人最终都能得出答案：
$ vec(13, 37) = 24 vec(3, 4) - 0.59 vec(100, 100)
  ==> T( vec(13,37) ) = 24 vec(pi, 9) - 0.59 vec(0, 12) = vec(24 pi, 208.92). $

所以这向你展示了一件有趣的事：
其实 $vec(1,0)$ 和 $vec(0,1)$ 之所以特殊，仅仅在于它们让算术变得简单。
但如果你知道
$T( vec(3,4) )$ 和 $T( vec(100, 100) )$ 的输出，你_仍然_能找到你想要的 $T$ 的所有输出。
所以 $vec(1,0)$ 和 $vec(0,1)$ 其实并没有_那么_特殊。

因此我们说 $vec(3,4)$ 和 $vec(100,100)$ 是 $RR^2$ 的一个_基_
（我们马上就会给出定义）。
每个像 $vec(13, 37)$ 这样的 $bf(v)$ 都可以写成 $c_1 vec(3,4) + c_2 vec(100,100)$
（对某对 $c_1$ 和 $c_2$），因此你就能解出 Q3。

不过，让我们考虑 Q3 的更多变体。

/ Q4.: 已知 $T( vec(3,4) ) = vec(pi, 9)$，那么 $T( vec(13,37) )$ 是多少？

/ Q5.: 已知 $T( vec(3,4) ) = vec(pi, 9)$、$T( vec(100, 100) ) = vec(0, 12)$，
  以及 $T( vec(103, 104) ) = vec(pi, 21)$，
  那么 $T( vec(13,37) )$ 是多少？

/ Q6.: 已知 $T( vec(3,4) ) = vec(pi, 9)$ 和 $T( vec(300, 400) ) = vec(100 pi, 900)$，
  那么 $T( vec(13,37) )$ 是多少？

变体 Q4、Q5、Q6 都以某种方式显得奇怪，应该会让你眯起眼睛。

- Q4 显然不可能；_信息不足_，无法求出答案。
- Q5 可解，但有_冗余信息_。你可以删掉任意一条假设。
- Q6 同时具有两种缺陷。
  尽管有两个已知条件，它们却是冗余的。

借这里说明的例子，让我给出相关的术语：
#definition(title: [基的定义])[
  $RR^n$ 中一组向量 $bf(v)_1$, ..., $bf(v)_k$ 的*线性组合*
  是形如 $c_1 bf(v)_1 + ... + c_k bf(v)_k$ 的和。

  那么 $RR^n$ 中的一组向量 $bf(v)_1$, ..., $bf(v)_k$：
  - 若它的每个线性组合都给出不同的向量，则称它是*线性无关*的；
    等价地说，除了 $c_1 = ... = c_k = 0$ 之外，
    不存在给出 $bf(0)$ 的非平凡线性组合；
  - 若 $RR^n$ 中每个其他向量 $bf(w)$ 都能写成
    某个线性组合，则称它在 $RR^n$ 中*张成*；
  - 若上面两条都成立，则称它是 $RR^n$ 的一个*基*；
    换言之，$RR^n$ 中的每个向量都能以_恰好_一种方式构造出来。
]

要害在于，这些概念对应着上面各问题的表现；
Q3 具备上面所有“好”的性质，而 Q4、Q5、Q6 各自都缺失了某些东西。

#example[
  - 在 Q3 中，$vec(3,4)$ 和 $vec(100, 100)$ 是 $RR^2$ 的一组基。
    因此，Q3 是一个提法良好的问题。

  - 在 Q4 中，单靠向量 $vec(3,4)$ 不能张成
    （虽然它是线性无关的）。
    这就是 Q4 无法回答的原因：你无法只用 $vec(3,4)$
    构造出 $vec(13, 37)$。

  - 在 Q5 中，$vec(3,4)$, $vec(100, 100)$, $vec(103,104)$ 不是线性无关的
    （虽然它们能张成），因为它们之间有一个线性关系
    $ vec(3,4) + vec(100, 100) = vec(103,104) $。

  - 在 Q6 中，两个向量 $vec(3,4)$ 和 $vec(300, 400)$ 把_两条_好性质都缺失了。
    这两个向量给出冗余信息；因为 $vec(300, 400) = 100 vec(3, 4)$，
    两个向量之间存在一个线性关系。
    而且这两个向量不能张成：你无法用 $vec(3,4)$ 和 $vec(300, 400)$
    构造出 $vec(13,37)$。
]

这就是那个说明定义想要传达什么的例子。

你也许已经有感觉：对 $RR^2$ 而言，无法用两个以外的任意多个向量
写出像 Q3 那样的“好”问题，而你的感觉是对的。
事实证明，$RR^n$ 的基总是恰好有 $n$ 个向量；
这正是“$RR^n$ 是 $n$ 维的”的含义。
下面还有几个正确的直觉，我们马上就会解释：

- 在 $RR^n$ 中，至少 $n+1$ 个向量永远不可能线性无关。
- 在 $RR^n$ 中，至多 $n-1$ 个向量永远不能张成。（所以基总是恰好 $n$ 个向量。）
- 另外，如果你在 $RR^n$ 中恰好有 $n$ 个向量，且它们线性无关，那么它们就是一组基。
- 另外，如果你在 $RR^n$ 中恰好有 $n$ 个向量，且它们能张成，那么它们就是一组基。

== [RECIPE] 如何判断一组基 <sec-detect-basis>

这引出一个问题：是否存在一种简单的方法来判断某些向量是否构成基？
对 $RR^2$，你大概看一眼就能判断。
但对 $n >= 3$ 的 $RR^n$，这可能就棘手一些了。

事实上，下面这个定理是真的，虽然我们不会证明它。

#memo(title: [记住：$RR^n$ 的基，“买二送一”])[
  假设你在 $RR^n$ 中有一堆向量。
  下面三条中的任意两条都能推出第三条：

  1. 恰好有 $n$ 个向量。
  2. 这些向量线性无关。
  3. 这些向量张成整个 $RR^n$。

  此外，如果第 1 条成立，下面这第四条也成立：

  4. 以这些向量为列向量的 $n times n$ 矩阵的行列式非零。
]

我们在这门课上无法证明这个结论，
但你也许有直觉：对一组基来说 1-3 都应该是真的。
行列式那一条可能更令人意外，所以下面解释一下原因。

#digression(title: [题外话：为什么行列式确实管用])[
  令 $n = 3$，考虑 $RR^3$ 中的三个向量 $bf(u)$, $bf(v)$, $bf(w)$。
  我们给出一个非正式的解释，说明为什么行列式能让你判断
  这三个向量是否构成基。

  所说的矩阵是把 $bf(u)$, $bf(v)$, $bf(w)$ 取作矩阵的列而构成的：
  $ A = mat(bf(u), bf(v), bf(w)) $
  回忆行列式 $det(A)$ 在几何上表示
  由这些向量张成的平行六面体的带符号体积。

  假设 $bf(u)$, $bf(v)$, $bf(w)$ 不能张成（即不是基），
  意思是它们填不满整个三维空间。
  那么这些向量共面：你只能从它们得到“一部分”向量。
  所以在几何上，它们构成的平行六面体在垂直于该平面的方向上
  缺少高度；于是体积为零，即 $det(A) = 0$。

  反过来，如果 $bf(u)$, $bf(v)$, $bf(w)$ 是一组基，
  那么从图像上说这意味着它们张成整个空间，
  所以平行六面体最好是非退化的。
  非退化对应于体积非零，即 $det(A) != 0$。
  （行列式的符号告诉你一些关于定向的信息，
  但我们不关心这个符号，只关心它是否非零。）
]

无论如何，在实践中，如果你有一组具体的向量，现在方法就很简单了：
#recipe(title: [检验向量是否构成基的方法])[
  假设给你 $RR^n$ 中的一串向量，你想知道它们是否构成基。

  1. 如果向量的_个数_不是 $n$，输出“否”并停止。
  2. 否则，以这 $n$ 个向量为列构成矩阵，并计算它的行列式。
    当且仅当行列式非零时输出“是”。
]

行列式那件事很重要：行列式替你做了_大量_工作。
当 $n = 2$ 时行列式是多余的，因为你直接用“斜率”就行：
显然 $vec(1,2)$ 和 $vec(100,200)$ 之间存在线性关系。
但对 $n >= 3$，*你没法用眼睛看出来*#footnote[不过
  如果你有一组_恰好两个_向量，
  它们线性相关当且仅当它们互为倍数，即使在 $RR^n$ 中也是如此。
  而这是你_能_用眼睛看出来的；
  所以如果你想判断 $RR^3$ 中两个向量的张成是一条直线还是一个平面，
  那很容易。
  （更蠢的是，单个向量只当它为零向量时才线性相关。）]。
例如，三个向量
$ bf(v)_1 = vec(1,3,4), quad bf(v)_2 = vec(10,1,11), quad bf(v)_3 = vec(-9,10,1) $
看起来可能像是一些互不相干的小数字，但出乎意料的是
#eqn[
  $ 109 vec(1,3,4) - 37 vec(10,1,11) - 29 vec(-9,10,1) = bf(0). $
  <eqn-big-coeff>
]
没有“斜率”，对 $n >= 3$ 你无法靠肉眼注意到这些线性关系，所以用行列式吧。

#sample[
  $vec(1, 2)$, $vec(100, 200)$ 是 $RR^2$ 的基吗？
]
#soln[
  不是，因为 $detmat(1, 100; 2, 200) = 0$。
  在这个例子中你也可以直接看出 $100 vec(1, 2) = vec(100, 200)$，
  所以这些向量不线性无关。
]

#sample[
  $vec(13, 37)$, $vec(42, 88)$ 是 $RR^2$ 的基吗？
]
#soln[
  是，因为 $detmat(13, 42; 37, 88) = 13 dot 88 - 42 dot 37 != 0$。
]

#sample[
  $bf(v)_1 = vec(1,3,4)$, $bf(v)_2 = vec(10,1,11)$, $bf(v)_3 = vec(-9,10,1)$ 是 $RR^3$ 的基吗？
]
#soln[
  不是，因为
  $ detmat(1, 10, -9; 3, 1, 10; 4, 11, 1)
    &= 1 detmat(1, 10; 11, 1) - 10 detmat(3, 10; 4, 1) + (-9) detmat(3, 1; 4, 11) \
    &= 1(1-110) - 10(3-40) + (-9)(33-4) = 0. #qedhere $
]
#sample[
  $vec(3,42,18)$, $vec(1,53,17)$, $vec(71,91,13)$ 是 $RR^3$ 的基吗？
]
#soln[
  是，因为
  $ detmat(3,1,71; 42,53,91; 18,17,13) = "ugly arithmetic" = -18522 != 0. #qedhere $
]

#remark(title: [注：随机生成数字，你会得到一组基])[
  上面的例题是我在 MIT 讲这节课时随机生成的。
  我当时的讲法是走到黑板前写下：

  _“$vec(?,?,?)$, $vec(?,?,?)$, $vec(?,?,?)$ 是 $RR^3$ 的基吗？答案：是。”_

  然后我让学生们随便编九个数字填进问号里。
  当然，他们挑了大数字，
  于是我就有机会炫一下我惊人的五位数乘法能力。

  我在还不知道问题是什么的时候就把答案写下来，这个把戏
  是为了说明一个要点：如果你挑的是真正随机的大数字，
  行列式也会是某个大的随机数字，
  所以你不可能恰好得到 $0$。
  所以你应当总是期望 $RR^n$ 中 $n$ 个“随机”向量构成基，
  只有当你刻意挑选时才会得到非基。
]

#sample[
  $vec(1,4,5,8)$, $vec(3,8,11,6)$, $vec(6,19,10,2)$ 是 $RR^4$ 的基吗？
]
#soln[
  不是，这里有三个向量，而 $RR^4$ 的每组基都必须恰好有四个向量。
]

== [TEXT] 张成

基是“最好的情形”，
因为如果你有 $RR^n$ 的一组基 $bf(v)_1$, ..., $bf(v)_n$，
那就意味着 $RR^n$ 的每个向量都能用 $bf(v)_i$ 以恰好一种方式构造出来。
我们不会总是这么幸运，所以我们有一个词来表示“你能用 $bf(v)_i$ 构造出的东西”。

#definition(title: [张成的定义])[
  $RR^n$ 中一组向量 $bf(v)_1$, ..., $bf(v)_k$ 的*张成*
  指的是你能用 $bf(v)_i$ 构造出的那些向量
  （即能写成 $bf(v)_i$ 的线性组合的向量）。
]

#example(title: [$RR^2$ 中张成的例子])[
  考虑 $RR^2$ 中的向量。

  - 任意一组基（例如 $vec(3,5)$ 和 $vec(7, 11)$）的张成是整个 $RR^2$，这是由定义可知的。
  - 此外，任何_包含_一组基的集合的张成也是整个 $RR^2$。
    例如，$vec(3,5)$, $vec(8,10)$, $vec(7,11)$, $vec(700,1100)$ 的张成
    仍然是整个 $RR^2$。
    （诚然，它不是基，因为有很多线性关系，
    但这并不改变你能用它们构造出 $RR^2$ 中任意向量这一点；
    你只是有一些不必用到的多余向量而已。）
  - 向量 $vec(1,2)$, $vec(10, 20)$, $vec(100, 200)$ 的张成
    是直线 $y = 2x$。
    这些就是你能用这三个向量的组合构造出的全部向量。
  - 单个向量 $vec(1,2)$ 的张成也是直线 $y = 2x$。
    也就是说，在上一个例子中，$vec(10, 20)$ 和 $vec(100, 200)$ 完全没用。
  - 单个向量 $vec(0,0)$ 的张成只有一个点：$vec(0,0)$ 本身。
]

#example(title: [$RR^3$ 中张成的例子])[
  考虑 $RR^3$ 中的向量。

  - 任意一组基（例如 $vec(3,421,8)$, $vec(1,53,17)$, $vec(71,91,13)$）
    的张成是整个 $RR^3$，这是由定义可知的。

  - 向量 $vec(1,2,3)$, $vec(10,20,30)$, $vec(100,200,300)$ 的张成
    是满足 $y = 2x$ 与 $z = 3x$ 的倍数构成的直线，
    不过我觉得把它表述为“$vec(1,2,3)$ 的倍数”更容易。
    这些就是你能用这三个向量的组合构造出的全部向量。

  - 单个向量 $vec(1,2,3)$ 的张成是同一条直线。
    也就是说，在上一个例子中，$vec(10, 20, 30)$ 和 $vec(100, 200, 300)$
    完全没用。

  - $vec(1,3,4)$, $vec(10,1,11)$, $vec(-9,10,1)$ 的张成更有意思。
    我们之前看到这三个向量之间其实有一个线性关系，
    所以它们不是基，张成也不是整个 $RR^3$。

    但张成也不只是一条直线：它是一个二维平面！
    事实上，这三个向量都包含在平面
    $ x + y = z. $
    之内。而张成实际上就是整个该平面；
    平面中任意一点都可以写成组合，
    事实上只用前两个向量就够了。
    例如，如果我在平面上随便取一点 $vec(1337, 2025, 3362)$，
    那么相应的组合是
    $ vec(1337, 2025, 3362) = 18913/29 vec(1,3,4) + 1986/29 vec(10, 1, 11) $
    （我不会解释这些系数是怎么来的，
    但如果你想弄明白，你大概自己能想出来）。

  - 单个向量 $vec(0,0,0)$ 的张成只有一个点：$vec(0,0,0)$ 本身。
]

你从这些例子中大概能体会到，张成_也_
有维数的概念：例如在 $RR^3$ 中，
我们看到了

- 一个 0 维张成，只有单个点 $chevron.l 0,0,0 chevron.r$。
- 一个 1 维张成，即 $vec(1,2,3)$ 的倍数构成的直线
- 一个 2 维张成，即平面 $x + y = z$。
- 一个 3 维张成，如果你有完整的基。

这一点值得知道：
#tip[
  记住，在 $RR^n$ 中，一堆向量的张成

  - 总是 $d$ 维的，其中 $d = 0, ..., n$（所以共有 $n+1$ 种答案）；
  - 总是包含原点 $bf(0)$。
]


== [RECIPE] 描述若干向量的张成

在 18.02 中，你可能会被要求描述 $RR^2$ 和 $RR^3$ 中某些向量的张成。
从上面的例子出发，你应该能推断出方法。

#recipe(title: [描述 $RR^2$ 中向量张成的方法])[
- *0 维情形*：所有向量都是零向量 $vec(0,0)$ 吗？如果是，张成只是单个*点*。
- *1 维情形*：所有向量都指向同一方向（即互为倍数）吗？
  如果是，且至少有一个非零向量，
  张成就是过原点、沿这些向量共同方向的*直线*。
- *2 维情形*：有两个（非零）向量不指向同一方向
  （等价地说，线性无关）吗？如果是，张成就是*整个 $RR^2$*。
]

#sample[
  $vec(3,6)$, $vec(10,20)$, $vec(100,200)$, $vec(5000, 10000)$ 在 $RR^2$ 中的张成是什么？
]
#soln[
  所有向量都是 $vec(1,2)$ 的倍数，所以答案是一条直线：
  $vec(1,2)$ 的倍数。
]

#sample[
  $vec(420, 321)$ 和 $vec(666, 5)$ 在 $RR^2$ 中的张成是什么？
]
#soln[
  因为这两个向量不互为倍数，它们线性无关。
  （行列式也能让你看出这一点：
  $detmat(420, 666; 321, 5) = 420 dot 5 - 321 dot 666 = -211686 != 0$。）
  因此它们是 $RR^2$ 的一组基，张成就是整个 $RR^2$。
]

#recipe(title: [描述 $RR^3$ 中向量张成的方法])[
  - *0 维情形*：所有向量都是零向量 $vec(0,0,0)$ 吗？如果是，张成只是单个*点*。
  - *1 维情形*：所有向量都指向同一方向（即互为倍数）吗？
    如果是，且至少有一个非零向量，
    张成就是过原点、沿这些向量共同方向的*直线*。
  - *2 维情形*：出现了不止一个方向，
    但你找不到三个线性无关的向量吗？
    如果是，张成就是一个过原点的*平面*。
    - 如果你想要平面的方程，用叉积。
  - *3 维情形*：其中有三个向量彼此线性无关吗？
    如果是，张成就是*整个 $RR^3$*。
]
（在 18.02 中，2 维情形的理由其实就是排除法：
你可以把 2 维那一条想成排除掉 0 维、1 维、3 维之后剩下的情形。）

#sample[
  $vec(3,6,9)$, $vec(10,20,30)$ 和 $vec(100,200,300)$ 在 $RR^3$ 中的张成是什么？
]
#soln[
  所有向量都是 $vec(1,2,3)$ 的倍数，所以它是一条直线：$vec(1,2,3)$ 的倍数。
]

#sample[
  $vec(1,3,4)$, $vec(10,1,11)$ 和 $vec(-9,10,1)$ 在 $RR^3$ 中的张成是什么？
]
#soln[
  它应该是一个二维平面。
  这可以由排除法得到：我们知道 $d = 0$ 和 $d = 1$ 在这里不适用
  （这些向量中没有零向量，也没有互为倍数的）
  而我们可以排除 $d = 3$，因为我们能算出行列式
  $ detmat(1,10,-9;3,1,10;4,11,1) = 0 $
  从而看出我们的三个向量_不_线性无关。

  那我们究竟怎么求出这个平面的方程呢？
  其实，我们真正要问的是求一个过原点且
  经过 $(1,3,4)$, $(10,1,11)$, $(-9,10,1)$ 的平面
  （行列式为零保证了它存在：
  再说一次，这就是不能张成的含义）。
  我们只需要一个平面的法向量，
  它可以由任意两个向量取叉积得到。
  随意地，我们用前两个：
  $ vec(1,3,4) times vec(10,1,11)
    = detmat(ee_1, ee_2, ee_3; 1, 3, 4; 10, 1, 11)
    = 29 ee_1 + 29 ee_2 - 29 ee_3. $
  这就是我们要的法向量，
  所以我们现在知道所求平面是 $29 x + 29 y - 29 z = 0$。
  它简化为 $#boxed[$ x+y-z=0 $].$
]
#digression[
  仅作比较，如果你改用了第二个和第三个向量，你会得到
  $ vec(1,3,4) times vec(-9,10,1)
    = detmat(ee_1, ee_2, ee_3; 1, 3, 4; -9, 10, 1)
    = - 37 ee_1 - 37 ee_2 + 37 ee_3 $
  而第二和第三个向量则给出
  $ vec(10, 1, 11) times vec(-9,10,1)
    = detmat(ee_1, ee_2, ee_3; 10, 1, 11; -9, 10, 1)
    = - 109 ee_1 - 109 ee_2 + 109 ee_3 $
  它们仍然都是 $ee_1 + ee_2 - ee_3$ 的倍数，
  所以平面仍然是 $x+y-z=0$。

  @eqn-big-coeff 中的神奇数字 $29$, $37$, $109$ 重新出现在法向量里并非巧合，
  但这次我不会解释为什么会这样，
  如果你想弄明白，就自己琢磨吧。
]

#sample[
  $vec(3,42,18)$, $vec(1, 53, 17)$, $vec(71,91,13)$ 在 $RR^3$ 中的张成是什么？
]
#soln[
  正如我们上面提到的（@eqn-big-coeff），三个及以上维度你不应该用肉眼判断；
  如果你在 $RR^3$ 中得到三个向量，想知道它们是否线性无关，
  你应该总是取行列式：
  $ detmat(3,1,71; 42,53,91; 18,17,13) = "ugly arithmetic" = -18522 != 0. $
  所以这三个向量是一组基，张成是整个 $RR^3$。
]

== [TEXT] 方程组

正如我们之前评论过的，随机选取的 $n$ 个向量“通常”是 $RR^n$ 的一组基。
我现在想把它与你高中见过的另一件事联系起来：
一个由 $n$ 个方程、$n$ 个变量组成的“随机”线性方程组“通常”只有一个解。

例如，在高中代数中，你大概被要求解过像
$ x_1 + 2 x_2 = 14 \
  3 x_1 + 4 x_2 = 38. $
这样的方程组。
你可以用你习惯的任何方法来做；
你应该求出 $(x_1,x_2) = (10, 2)$ 是唯一的解。
而且你大概已经能看出，$14$ 和 $38$ 可以是任何数字，
你仍然总会得到恰好一个解。

为什么这和关于基的那些内容有关？
嗯，要点在于你可以把上面的变量看作线性组合中的系数，
从而把前面的方程组看作是在说
$ x_1 vec(1,3) + x_2 vec(2,4) = vec(14, 38). $
这意味着我们的观察可以用线性代数的语言重新表述：
#idea[
  $vec(1,3)$ 和 $vec(2,4)$ 是 $RR^2$ 的一组基，
  所以像 $vec(14, 38)$ 这样的任意向量都能以恰好一种方式构造出来。
]
换言之，只要由方程组左端系数构成的列向量构成一组基，
就总是恰好有一个解。

那么当它_不_构成基时，出了什么问题？
让我们把 @eqn-big-coeff 中的例子拿回来。
- 考虑像
  $
    x_1 + 10x_2 - 9x_3 &= 0 \
    3 x_1 + x_2 + 10 x_3 &= 0 \
    4 x_1 + 11 x_2 + x_3 &= 0.
  $
  这样的方程组。
  这个方程显然至少有一个解：$x_1 = x_2 = x_3 = 0$。
  我们看到 $vec(1,3,4)$, $vec(10,1,11)$, $vec(-9, 10, 1)$ 不是 $RR^3$ 的基，
  而这翻译过来就是说还有别的解：
  从 @eqn-big-coeff 读出，注意 $x_1 = 109k$, $x_2 = -37k$, $x_3 = -29k$
  对任意 $k$ 都是解。

- 如果我们改而考虑像
  $
    x_1 + 10 x_2 - 9 x_3 &= 17 \
    3 x_1 + x_2 + 10 x_3 &= 42 \
    4 x_1 + 11 x_2 + x_3 &= 1337
  $
  这样的东西呢？
  我们仍知道左端的系数之间存在线性关系，
  所以我们预期会有东西“出错”。

  在这种情况下，失效的方式不同：$vec(17, 42, 1337)$
  竟然_不_在我们三个向量的张成之中。
  事实上，我们之前看到这些向量的张成是平面 $x+y=z$，
  它不包含 $vec(17, 42, 1337)$。

  所以对这个方程组而言，同时满足这三个方程应当是不可能的
  （也就是说，是_无解_而不是_解太多_）。
  而事实上你也许能直接看出这一点：
  如果你把前两个方程相加
  再减去最后一个方程，就能看出矛盾：
  $
    + [x_1 + 10 x_2 - 9 x_3] &= + 17 \
    + [3 x_1 + x_2 + 10 x_3] &= + 42 \
    - [4 x_1 + 11 x_2 + x_3] &= - 1337 \
    ==> 0 &= 17 + 42 - 1337 "，这是荒谬的。"
  $

- 另一方面，如果我们有
  $
    x_1 + 10 x_2 - 9 x_3 &= 100 \
    3 x_1 + x_2 + 10 x_3 &= 200 \
    4 x_1 + 11 x_2 + x_3 &= 300
  $
  那么张成确实包含 $vec(100, 200, 300)$，
  所以我们预期又回到“解太多”的情形。
  我不会展示如何想出这些数字（虽然其实并不难），
  但你可以发现一个解的例子是 $(x_1, x_2, x_3) = (58, 6, 2)$。
  如果你把它和 @eqn-big-coeff 结合起来，现在就能得到很多解：
  $ x_1 &= 58 + 109 k \
    x_2 &= 6 - 37 k \
    x_3 &= 2 - 29 k. $

总之，我们在本节看到的是：
对一个 $n$ 个方程、$n$ 个变量的方程组，
我们应该考察由系数构成的 $n$ 个列向量。
那么

- 如果这些向量构成 $RR^n$ 的一组基（通常情形），
  方程组总有一个解。
- 否则，方程组是有缺陷的：
  要么完全无解（自相矛盾），
  要么实际上有无穷多个解。

== [RECIPE] 方阵线性方程组解的个数

现在，记住_在实践中_我们判断 $n$ 个向量是否构成基
的方法是看行列式。
所以我们把上一节的发现改写成下面的方法，
用“行列式”这个词替换“基”。

#recipe(title: [计算方程组解的个数的方法])[
  假设你被要求求一个方阵线性方程组
  $
    a_11 x_1 + a_12 x_2 + ... + a_(1n) x_n &= b_1 \
    a_21 x_1 + a_22 x_2 + ... + a_(2n) x_n &= b_2 \
    &dots.v \
    a_(n 1) x_1 + a_(n 2) x_2 + ... + a_(n n) x_n &= b_n.
  $
  的解的个数。
  令 $A$ 为由 $a_(i j)$ 构成的 $n times n$ 矩阵。

  1. 若 $det A != 0$，你甚至不用看 $b_i$；直接*输出“恰好 1 个解”*。
  2. 若 $det A = 0$，你应当*输出“零个解”或“无穷多个解”*，
    取决于是否至少有一个解。
    要看属于哪种情形：

    - 有一种常见情形 $b_1 = b_2 = ... = b_n = 0$，
      此时方程组有一个显然的解 $x_1 = ... = x_n = 0$。
      因此输出“无穷多个解”。

    - 当 $n = 2$ 时，你通常能一眼看出这两个方程是否冗余。
      如果两个方程互为倍数，输出“无穷多个解”；
      如果两个方程互相矛盾，输出“零个解”。

    - 否则，对 $n >= 3$，如果你猜不出解，
      你应该逐个消元。
      不过这种情形不会在 18.02 中出现。
]

下面是当我说 $n=2$ 可以“一眼看出”时的例子：

- 方程组 $x + 3y = 8$ 与 $10x + 30y = 80$ 显然是“无穷多个”，
  因为两个方程相同。在这种情形下，*输出“无穷多个”*。

- 方程组 $x + 3y = 8$ 与 $10x + 30y = 42$ 显然是“无解”，
  因为它将意味着 $80 = 10 dot 8 = 10 dot (x+3y) = 10x + 30y = 42$，
  这是自相矛盾的。
  在这种情形下，*输出 0*。

== [EXER] 习题

#exer[
  取你的生日，把它写成八位数字的 $Y_1 Y_2 Y_3 Y_4$-$M_1 M_2$-$D_1 D_2$ 格式。
  考虑两个向量
  $ bf(v)_1 = vec(M_1 M_2, D_1 D_2) " 与 " bf(v)_2 = vec(Y_1 Y_2, Y_3 Y_4). $
  例如，如果你的生日是 1994 年 5 月 17 日，
  你就应取 $bf(v)_1 = vec(5, 17)$ 和 $bf(v)_2 = vec(19, 94)$。

  - 计算这两个向量在 $RR^2$ 中的张成。
  - 找一位现任或前任 K-pop 偶像，让他们用自己的生日算出与你不同的答案。
] <exer-basis-birthday>

#exer[
  在 $RR^5$ 中，考虑向量 $bf(v) = chevron.l 1,2,3,4,5 chevron.r$。
  计算能找到的、都与 $bf(v)$ 垂直的线性无关向量的最大可能个数。
] <exer-basis-perpbasis>
