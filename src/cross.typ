#import "@local/evan-zh:1.0.0":*

= 叉积 <ch-cross>

叉积是我们要引入的最后一个主要的线性代数工具
（连同行列式和点积一起）。
像点积一样，叉积也有两个定义，
一个代数的、一个几何的。

然而，与点积不同，叉积更生硬、更不自然，
用得也没那么多 --- 事实上直到 @ch-scalint 它才会再次出现。
（关于这一点，@sec-cross-sucks 有更多讨论。）
我会尽量让本章简短。

== [TEXT] 叉积的两个定义

这个定义很糟糕，请耐心听我说。

#definition[
  设 $bf(v) = vec(a_1, a_2, a_3)$ 与 $bf(w) = vec(b_1, b_2, b_3)$ 是 $RR^3$ 中的两个向量。

  叉积的*代数定义*是：
  #eqn[
    $ bf(v) times bf(w) := detmat(a_2, a_3; b_2, b_3) ee_1
      - detmat(a_1, a_3; b_1, b_3) ee_2 + detmat(a_1, a_2; b_1, b_2) ee_3
      = vec(a_2 b_3 - a_3 b_2, a_3 b_1 - a_1 b_3, a_1 b_2 - a_2 b_1). $
    <eqn-cross-raw-alg>
  ]

  （关于更容易记住这个公式的方法，见下面的提示。）

  叉积的*几何定义*基于同时指定方向和模长。
  - $bf(v) times bf(w)$ 的*模长*等于由 $bf(v)$ 与 $bf(w)$
    构成的平行四边形的面积。
    用三角学的说法，若 $theta$ 是夹角，
    它就等于 $|bf(v)| |bf(w)| sin theta$。
  - *方向*由如下要求给出：$bf(v) times bf(w)$ 同时垂直于
    $bf(v)$ 与 $bf(w)$，并且满足*右手定则*
    （我们稍后会多谈一些）。
]

几何定义在下面的 @fig-cross-def 中图示。
#figure(
  image("figures/cross-def.svg", width: auto),
  caption: [叉积几何定义的图示。],
) <fig-cross-def>

和点积一样，这两个定义为何相容这一点完全不显然！
@eqn-cross-raw-alg 大概也非常神秘，看起来不知从何而来。
在这个情形中，我认为思路应当是：从几何定义出发，
然后硬做一些计算，得到一个方程组。
如果你解出这个方程组，结果就得到 @eqn-cross-raw-alg。
我们在下一节把这件事作为一个选读的旁注来做。

#typesig[
  叉积*只*接受两个*都在 $RR^3$ 中*的向量。
  而且它输出单个*$RR^3$ 中的向量*。
]

#remark(title: [注：右手定则])[
  右手定则这个补丁是必要的，
  因为如果我只告诉你 $RR^3$ 中一个向量的长度，
  以及它垂直于 $RR^3$ 中另外两个向量，
  那么实际上有两个向量满足条件。
  （例如，垂直于 $ee_1$ 与 $ee_2$ 且长度为 $5$ 的向量有两个：
  即 $pm 5 ee_3$。）

  所以我们需要选定其中一个，而右手定则说的是：
  如果你的右手食指指向 $bf(v)$，
  右手中指沿 $bf(w)$ 指向更靠近掌心的方向，
  并伸出右手拇指，那么 $bf(v) times bf(w)$ 就沿你的拇指指向。

  描述右手定则的另一种方式是要求下面的表格成立：
  $ ee_1 times ee_2 &= ee_3 = -ee_2 times ee_1 \
    ee_2 times ee_3 &= ee_1 = -ee_3 times ee_2 \
    ee_3 times ee_1 &= ee_2 = -ee_1 times ee_3. $
]

要记住 @eqn-cross-raw-alg 可能不那么容易。
实际上，我认为几乎所有人都用下面这个助记法。

#tip(title: [提示：如何记住叉积的代数定义])[
  代数定义通常用下面这个助记法来记住：
  #eqn[
    $ bf(v) times bf(w) =
      detmat(
        ee_1, ee_2, ee_3;
        a_1, a_2, a_3;
        b_1, b_2, b_3
      ). $
    <eqn-cross-det>
  ]
  从数学上讲，右端没有意义，是一个类型错误，
  因为不可能有一个矩阵，其中有些东西是数、
  另一些东西是向量。
  然而，如果你不管这一点、照样去乘，就会得到上面的代数定义。

  在本讲义中，*我将始终使用 @eqn-cross-det 而不是 @eqn-cross-raw-alg*，
  因为人们在实践中就是这么做的。
  （我这么做相当不情愿，因为 @eqn-cross-det 按官方说法是一个类型错误，
  理论上纯属胡说。
  但用 Linus Torvalds 的话说：
  “理论与实践有时会冲突。而当冲突发生时，理论总是输。每一次都是。”）
]

#warning(title: [警告：叉积是反交换的])[
  从两个定义中的任何一个，你都应该能看出
  $ bf(v) times bf(w) = - bf(w) times bf(v) $
  这与点积形成对比。
  注意那个负号。
  （右手定则意味着你不能交换食指和中指。）

  另外，注意 $bf(v) times bf(v) = bf(0)$
  （事实上，只要 $bf(v)$ 与 $bf(w)$ 平行，就有 $bf(v) times bf(w) = bf(0)$）。
]

我真的很想赶紧把这一节结束掉，所以我只给你一个带数字的例子，
连对应的几何都不谈了。

#sample[
  计算 $bf(v) = vec(1,2,3)$ 与 $bf(w) = vec(4,5,6)$ 的叉积。
]
#soln[
  写出
  $ bf(v) times bf(w) &:=
  detmat(ee_1, ee_2, ee_3; 1, 2, 3; 4, 5, 6) \
  &= detmat(2, 3; 5, 6) ee_1
    - detmat(1, 3; 4, 6) ee_2 + detmat(1, 2; 4, 5) ee_3 \
  &= -3 ee_1 + 6ee_2 - 3ee_3 = vec(-3,6,-3). #qedhere $
]
作为对几何定义的合理性检查，你可以用点积验证
这个向量确实同时垂直于 $bf(v)$ 与 $bf(w)$：
$ vec(-3,6,-3) dot vec(1,2,3) &= (-3)(1) + (6)(2) + (-3)(3) = 0 \
  vec(-3,6,-3) dot vec(4,5,6) &= (-3)(4) + (6)(5) + (-3)(6) = 0. $

== [SIDENOTE] 叉积公式推导概要

在这个旁注中，我们概述如何从几何定义
推导出叉积的代数定义。
这个证明太难，不会出现在 18.02 的考试中，
但我决定把它放在这里而不是附录里，
因为它实际上是使用点积和行列式的好练习。
为了简单起见，我完全不操心除以零的问题。

那么假设我们有两个给定的向量
$ bf(v) &= a_1 ee_1 + a_2 ee_2 + a_3 ee_3 \
  bf(w) &= b_1 ee_1 + b_2 ee_2 + b_3 ee_3. $
我们把叉积记作
$ bf(v) times bf(w) &= x ee_1 + y ee_2 + z ee_3. $
我们需要用 $a_1$、$a_2$、$a_3$、$b_1$、$b_2$、$b_3$ 解出 $x$、$y$、$z$。

那么已知条件有哪些呢？
首先，我们要求 $bf(v) times bf(w)$ 垂直于 $bf(v)$ 与 $bf(w)$。
于是从点积得到两个方程：
$ 0 = bf(v) dot (bf(v) times bf(w)) &= a_1 x + a_2 y + a_3 z \
  0 = bf(w) dot (bf(v) times bf(w)) &= b_1 x + b_2 y + b_3 z. $
这是含三个变量的两个方程组成的方程组，但它已足以求出比值 $x:y:z$。
例如，如果你把第一个方程乘以 $b_3$、第二个乘以 $a_3$，
然后相减，就会得到
$ 0 = b_3(a_1 x + a_2 y) - a_3(b_1 x + b_2 y)
  ==> x / y = (a_2 b_3 - a_3 b_2) / (a_3 b_1 - a_1 b_3). $
用类似的方法，你可以得到比值 $y / z$ 为
$ y / z = (a_3 b_1 - a_1 b_3) / (a_1 b_2 - a_2 b_1). $
从几何上说，这意味着我们已经求出了 $bf(v) times bf(w)$ 的*方向*。
从代数上说，这意味着我们知道了比值 $x:y:z$；应当存在某个标量常数 $k$ 使得
$ x &= k (a_2 b_3 - a_3 b_2) \
  y &= k (a_3 b_1 - a_1 b_3) \
  z &= k (a_1 b_2 - a_2 b_1). $
所以现在我们需要做的只是求出 $k$。
为了简洁，我们令
$ x_0 := a_2 b_3 - a_3 b_2 \
  y_0 := a_3 b_1 - a_1 b_3 \
  z_0 := a_1 b_2 - a_2 b_1 $
于是 $x = k x_0$、$y = k y_0$、$z = k z_0$，而我们需要解出 $k$。

我们还有一个条件要用，那就是我们希望模长
$sqrt(x^2+y^2+z^2)$ 等于由 $bf(v)$ 与 $bf(w)$ 张成的平行四边形的面积 $A$，
并且指向右手定则所指定的方向。
我们怎样把它写成一个方程呢？
从表面上看，我们目前工具箱里的武器，
都不能让你得到三维空间中平行四边形的面积。
我们只有适用于二维空间中平行四边形的行列式。

但这里有一个巧妙的技巧可以绕过它。
求面积的技巧是考虑由三个向量构成的平行六面体：
两个给定的向量 $bf(v)$ 与 $bf(w)$，以及沿 $bf(v) times bf(w)$ 方向的*单位*向量。
也就是说，考虑向量
$ bf(n) = (chevron.l x_0, y_0, z_0 chevron.r) / sqrt(x_0^2 + y_0^2 + z_0^2). $
我们刚刚看到 $bf(n)$ 同时垂直于 $bf(v)$ 与 $bf(w)$，
并且我们已经把 $bf(n)$ 按比例缩放使得 $|bf(n)| = 1$。
见 @fig-cross-proof。

#figure(
  image("figures/cross-proof.svg", width: auto),
  caption: [所引入的额外向量 $bf(n)$。
    它是我们想要的方向上的单位向量，且不依赖于 $k$。],
) <fig-cross-proof>

为简单起见，我们假设 $bf(n)$ 指向符合右手定则的正确方向。
（如果 $bf(n)$ 指向另一边，下面的计算就需要一堆额外的负号；
这里我们不再纠缠，同样是为了保持简单。）
那么，如果我们考虑由 $bf(n)$、$bf(v)$、$bf(w)$ 构成的平行六面体，
它的体积就等于高乘以底面的平行四边形，即 $1 dot A = A$。
而平行六面体的体积*确实*是我们能得到的东西：它是一个 $3 times 3$ 行列式。
于是我们的技巧成功地让我们抓住了 $A$：
$ A = detmat(
    x_0 / sqrt(x_0^2 + y_0^2 + z_0^2), y_0 / sqrt(x_0^2 + y_0^2 + z_0^2), z_0 / sqrt(x_0^2 + y_0^2 + z_0^2);
    a_1, a_2, a_3;
    b_1, b_2, b_3;
  ). $
这里，行列式确实等于 $+A$（而不是 $-A$），因为我们假设 $bf(n)$
指向正确的方向，所以行列式给出正号。
正是引入 $bf(n)$ 这个技巧让我们得到了 $A$ 的公式，
否则只凭表面上能拿到的行列式是无法得到它的。

现在我们解出 $k$。
因为我们处在 $bf(n)$ 指向正确方向的情形，我们知道应当有 $k > 0$。
现在我们必须把
$ |bf(v) times bf(w)| = k sqrt(x_0^2 + y_0^2 + z_0^2) $
的模长设为等于上面的 $A$；也就是说，我们得到方程
$ k sqrt(x_0^2 + y_0^2 + z_0^2) = A = detmat(
    x_0 / sqrt(x_0^2 + y_0^2 + z_0^2), y_0 / sqrt(x_0^2 + y_0^2 + z_0^2), z_0 / sqrt(x_0^2 + y_0^2 + z_0^2);
    a_1, a_2, a_3;
    b_1, b_2, b_3;
  ). $
两边乘以 $sqrt(x_0^2 + y_0^2 + z_0^2)$ 并展开行列式，得到
$ k (x_0^2 + y_0^2 + z_0^2) =
  x_0 detmat(a_2, a_3; b_2, b_3) - y_0 detmat(a_1, a_3; b_1, b_3) + z_0 detmat(a_1, a_2; b_1, b_2). $
如果你现在看看 $x_0$、$y_0$、$z_0$ 的定义，
就会发现右端的那些行列式恰好分别等于
$x_0$、$-y_0$ 和 $z_0$。
于是突然间整件事都消掉了，我们直接得到 $k = 1$。
所以 $x = x_0$、$y = y_0$、$z = z_0$，这就给出了我们想要的代数公式。

== [RECIPE] 叉积可以用来做什么 <recipe-cross>

点积只是一个数，而叉积是一个向量。
所以它含有更多信息 --- 既有方向又有模长。

- $bf(v) times bf(w)$ 的方向同时垂直于 $bf(v)$ 与 $bf(w)$。
- 模长是平行四边形的面积。

然而在实践中，当我们使用叉积时，
我们常常*只用其中一条信息*。
（直到 @ch-flux，我们才真正开始同时使用两个部分。）

因此有如下两个方法。

#recipe(title: [求法向量的方法])[
  求一个同时垂直于 $bf(v)$ 与 $bf(w)$ 的向量：

  1. 输出 $bf(v) times bf(w)$ 的任意非零倍数。
]

#recipe(title: [求面积的方法])[
  求 $RR^3$ 中由 $bf(v)$ 与 $bf(w)$ 构成的平行四边形的面积：

  1. 输出 $bf(v) times bf(w)$ 的模长。
]

注意，在第一个方法中我们忽略了模长；
在第二个方法中我们忽略了方向。

#sample[
  考虑三个点 $A = (1,0,0)$、$B = (0,2,0)$、$C = (0,0,3)$。

  - 求过 $A$、$B$、$C$ 的平面的一个法向量。
  - 计算该平面的方程。
  - 计算三角形 $A B C$ 的面积。
]
#soln[
  首先，我们来求过 $A$、$B$、$C$ 的平面的一个法向量。
  思路是计算两个向量 $arrow(A B)$ 与 $arrow(A C)$：
  $ arrow(A B) &= vec(0 - 1 , 2 - 0 , 0 - 0) = vec(- 1 , 2 , 0) \
    arrow(A C) &= vec(0 - 1 , 0 - 0 , 3 - 0) = vec(- 1 , 0 , 3) . $
  这两个向量可以画成包含在过它们的平面内的箭头。
  所以如果我们计算叉积，就会得到我们想要的法向量！
  也就是说，
  $ arrow(A B) times arrow(A C)
    &= detmat(ee_1, ee_2, ee_3; -1,2,0; -1,0,3) \
    &= (2 dot 3 - 0 dot 0) ee_1  - (- 1 dot 3 - 0 dot - 1) ee_2
      + (- 1 dot 0 - 2 dot - 1) ee_3 \
    &= (6 - 0) ee_1 - (- 3 - 0) ee_2 + (0 - (- 2)) ee_3 = vec(6,3,2). $

  这就是法向量。为求平面的方程，我们知道应当有
  $ 6 x + 3 y + 2 z = d $
  其中 $d$ 是某个常数。
  代入三个点 $A$、$B$、$C$ 中的任意一个都给出 $d = 6$
  （这里的冗余也给了我们一个检查算术的方法）。
  所以平面是 $ 6 x + 3 y + 2 z = 6. $

  最后，$triangle.stroked.t A B C$ 的面积是由
  $arrow(A B)$ 与 $arrow(A C)$ 构成的平行四边形面积的一半，因此
  $ op("面积")(triangle.stroked.t A B C)
    = 1 / 2 lr(|arrow(A B) times arrow(A C)|)
    = 1 / 2 sqrt(6^2 + 3^2 + 2^2) = 7 / 2. #qedhere $
]

#remark[
  这种题型值得记住：
  叉积常常能给你一种求某个平面的法向量的方法，
  因为它太擅长造出直角了。
  一旦你有了法向量，就可以用 @recipe-plane-known-dir 中的方法
  求出平面的方程。
]


== [SIDENOTE] 叉积很糟糕 <sec-cross-sucks>

与点积和行列式相比，
叉积也许让人感觉最不自然，这是有充分理由的 ---
严肃的数学家使用它的频率远低于你见到的其他工具。
见 @fig-cross-meme。

叉积在数学家中不受欢迎的原因是，
叉积的定义*实在相当脆弱*。
例如，叉积无法对任意维数定义，#footnote[开个玩笑，
  显然存在
  #link("https://w.wiki/BVNL")[七维叉积]？
  今天我又学到了一件事。
  只不过在七维中显然有 $480$ 种不同的定义方式，
  所以，呃，大概不是什么好事。]
而且你还得记住这个古怪的右手定则，又多了一条随意的约定。
所以这个定义相当不令人满意。

为了替代叉积，数学家使用一种不同的
对象，称为*二重向量*（bivector），它是某个称为 $and.big^2 (RR^n)$ 的空间中的元素。
（他们甚至可能声称二重向量能做叉积能做的一切，而且做得更好。）
同样，这种新对象远远超出 18.02 的范围，
但在我的 #link("https://web.evanchen.cc/napkin.html")[Napkin] 第 12 章中有记载，
如果你确实想看的话。

不过我还是要给你一点剧透。
一般地，对任意 $n$，$RR^n$ 中的二重向量由 $(n(n-1))/2$ 个坐标确定。
所以当 $n=3$ 时，你*可以*仅仅通过读取坐标
就把 $RR^3$ 中的每个二重向量翻译成 $RR^3$ 中的一个向量
（尽管作为这种翻译的副产物，你最终会得到右手定则），
而你得到的东西正好就是叉积。
但当 $n=4$ 时，$RR^4$ 中的一个二重向量有六个数，
这个信息量太大，装不进 $RR^4$ 中的一个向量里。
类似地，当 $n > 4$ 时，这种翻译做不到。
这就是为什么叉积如此脆弱，无法超出 $RR^3$。

#figure(
  image("media/cross-products.jpg", width: 80%),
  caption: [如何看待叉积。],
) <fig-cross-meme>

== [RECAP] 至此向量内容的回顾

对前几章的简要总结。

- 点积和叉积既有代数公式又有几何性质，
  这使它们在许多三维几何应用中很有用。

- 点积让你能判断垂直性和投影。
  - 两个向量垂直当且仅当它们的点积为零。

- 叉积产生垂直关系，并让你能计算面积。

- 两者都用于平面的理论：
  - 我们用点积证明平面 $a x + b y + c z = d$ 的
    法向量是向量 $vec(a,b,c)$。
  - 我们用点积中的投影来求点到平面的距离。
  - 给定平面上的三个点，叉积让我们能求出法向量。

另见 @table-vector-objects，它总结了我们在应用中出现过的一些向量。

#figure(
  table(
    columns: 3,
    align: left,
    table.header([向量], [方向], [模长]),
    [平面的法向量 $bf(n)$], [垂直于平面], [*无关紧要！*],
    [$op("proj")_(bf(w))(bf(v))$], [与 $bf(w)$ 相同], [标量分量],
    [叉积 $bf(v) times bf(w)$], [同时垂直于 $bf(v)$ 与 $bf(w)$], [平行四边形的面积]
  ),
  caption: [我们目前见过的一些常用向量类型。],
  kind: table
) <table-vector-objects>

== [EXER] 习题

#exer[
  设实数 $a$ 与 $b$ 满足
  $ vec(1, 2, 3) times vec(100, a, b) = bf(0). $
  求 $a$ 与 $b$。
] <exer-cross-100ab>

#exer[
  设 $bf(v)$ 与 $bf(w)$ 是 $RR^3$ 中的向量，
  且满足 $bf(v) times bf(w) = vec(1,2,3)$。
  计算 $5bf(w) times 4bf(v)$。
] <exer-cross-scale>

#exer[
  设 $bf(v)$ 与 $bf(w)$ 是 $RR^3$ 中的单位向量。
  求下式所有可能的值
  $ |bf(v) times bf(w)|^2 + (bf(v) dot bf(w))^2. $
] <exer-cross-pythag>

#exer[
  设 $bf(v)$ 是 $RR^3$ 中的一个向量，$k$ 是一个实数，且满足
  $ vec(1,2,3) times bf(v) = vec(4,5,k). $
  求 $k$。
] <exer-cross-perp>
