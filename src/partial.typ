#import "@local/evan-zh:1.0.0":*

= 偏导数 <ch-partial>

== [TEXT] 微分的意义在于线性近似

在 18.01 中，当 $f : RR -> RR$ 时，你对每个输入 $p in RR$ 定义了*导数* $f'(p)$，
你把它看作 $p$ 处*切线*的*斜率*。
想想 $f(5.01) approx f(5) + f'(5) dot 0.01$。
这个斜率大致告诉你：如果你从输入 $p$ 稍微移动一点距离，
你预期 $f$ 会变化多快。
再把这一点讲透一次：在 18.01 中，我们有
$ f(p + epsilon) = f(p) + f'(p) dot epsilon. $
见下图。

#figure(
  image("figures/partial-slope.svg", width: auto),
  caption: [在 18.01 中，斜率 $f'(p)$ 告诉你 $f$ 在 $p$ 附近变化得多快。],
)

18.01 的导数的类型是“标量”。
但对于_二元_函数来说，这还不够。
具体起见，我们取
$ f(x,y) = x^2+y^2 $
作为示例函数（我们之前已经画过它的等值线），
并考虑某个点 $P = (3,4)$，于是 $f(3,4) = 25$。

那么，一个“接近”$(3,4)$ 的点是什么意思呢？
点 $(3.01,4)$ 很近，但 $(3,4.01)$ 也很近。
同样，$(3.006, 4.008)$ 也是——它同样是距离为 $0.01$ 的点！
所以，仅用一个数已经不足以描述变化率了。

对于二元函数，我们其实想要_两个_数，也就是说
我们想填出下面方程中的空格
$ f(3 + epsilon_x, 4 + epsilon_y) approx 25
  + ("沿 " x " 方向的斜率") dot epsilon_x
  + ("沿 " y " 方向的斜率") dot epsilon_y. $

#idea[
  对于 $n$ 元函数，我们在 $n$ 个方向_各自_都有一个变化率。
  因此，*我们需要 $n$ 个数，而不只是一个*。
]

第一个空格对应的是：如果你设想 $y$ 被固定在 $4$ 处，
而我们只把 $x$ 的值改成 $3.01$，会发生什么。
第二个空格类似。
所以我们需要一种计算它们的办法；满足我们愿望的那个答案
就是所谓的_偏导数_（partial derivative）。

== [TEXT] 计算偏导数其实只是 18.01 <text-compute-partial>

关于偏导数的好消息是*它们其实非常容易计算*。
你基本上只需要做 18.01 中教过的事情，
让一个变量变化，同时把其他变量都当作常数。

下面是定义：

#definition[
  设 $f(x,y)$ 是一个二元函数。
  那么_关于 $x$ 的偏导数_（我们记作 $f_x$
  或 $(partial f) / (partial x)$）
  就是把 $x$ 视为变量、把 $y$ 视为常数来对 $f$ 求导所得的结果。
  偏导数 $f_y = (partial f) / (partial y)$ 的定义方式相同。

  类似地，如果 $f(x,y,z)$ 是一个三变量函数，
  我们记 $f_x = (partial f) / (partial x)$ 为固定 $y$ 和 $z$ 时的导数。
]

#typesig[
  每个偏导数都具有与 $f$ 相同的类型签名。也就是说：

  - 给定 $f : RR^n -> RR$，它接受 $RR^n$ 中的*点*并输出*标量*。
  - 那么偏导数 $(partial f) / (partial x) = f_x$
    也接受 $RR^n$ 中的*点*并输出*标量*。
]

但这话太啰嗦了。
我觉得用例子来解释其实更好。
事实上你大概可以直接读这些例子，跳过上面的定义。

#example(title: [例：$f(x,y) = x^3 y^2 + cos(y)$ 的偏导数])[
  设 $f(x,y) = x^3 y^2 + cos(y)$。

  我们来计算 $f_x$。
  再次把 $y$ 当作常数，于是看函数
  $ x |-> y^2 dot x^3 + cos(y). $
  如果我们对 $x$ 求导，那么 $x^3$ 变成 $3x^2$，
  而 $cos(y)$ 变成 $0$（它里面没有任何含 $x$ 的东西）。
  所以
  $ f_x = y^2 dot 3x^2. $

  类似地，我们来计算 $f_y$。
  这次我们把 $x$ 当作常数，看
  $ y |-> x^3 dot y^2 + cos(y). $
  这次 $y^2$ 变成 $2y$，而 $cos(y)$ 的导数是 $-sin(y)$。所以
  $ f_y = x^3 dot 2y - sin(y). $
]

#example(title: [例：$f(x,y,z) = e^(x y z)$ 的偏导数])[
  取 $f(x,y,z) = e^(x y z)$ 作为三变量的例子。
  要计算 $f_x$，考虑函数
  $ x |-> e^(y z dot x) $
  其中我们把 $y$ 和 $z$ 当作常数。
  那么关于 $x$ 的导数就是 $y z e^(y z dot x)$
  （就像 $e^(3 x)$ 的导数是 $3 e^(3 x)$ 一样）。换言之，
  $ f_x (x,y,z) = y z dot e^(x y z). $
  由类似的理由：
  $
    f_y (x,y,z) &= x z dot e^(x y z) \
    f_z (x,y,z) &= x y dot e^(x y z).
  $
]

#example(title: [例：$f(x,y) = x^2 + y^2$ 的偏导数与线性近似])[
  让我们回到
  $ f(x,y) = x^2+y^2 $
  这是我们在前面的例子中用作动机的函数，取点 $P = (3,4)$。

  让我们为我们选定的例子 $f(x,y) = x^2+y^2$ 填上具体的数字。
  到现在你应该能算出
  $
  f_x (x,y) &= 2x \
  f_y (x,y) &= 2y \
  $

  现在，让我们把注意力集中到点 $P = (3,4)$ 上。
  我们知道
  $
  f(P) = 3^2 + 4^2 = 25 \
  f_x (P) = 2 dot 3 = 6 \
  f_y (P) = 2 dot 4 = 8.
  $
  所以我们的近似方程可以写成
  #eqn[
    $ (3 + epsilon_x)^2 + (4 + epsilon_y)^2 approx 25 + 6 epsilon_x + 8 epsilon_y. $
    <example-circle-3-4>
  ]
  如果你手动展开两边，就能看出这看起来是对的。
  两边只相差 $epsilon_x^2$ 和 $epsilon_y^2$，
  直观上的意思是：如果 $epsilon_x$ 和 $epsilon_y$ 是很小的数，
  那么它们的平方就小到可以忽略。

  等我们引入梯度时，还会回到 @example-circle-3-4。
]

== [RECIPE] 计算偏导数 <recipe-compute-partial>

你大概已经从上面的章节中看出这个方法了，
不过为了完整起见，我们把它写在这里。

#recipe(title: [计算偏导数的方法])[
  要计算函数 $f(x,y)$、$f(x,y,z)$
  或 $f(x_1, ..., x_n)$ 关于某个输入变量的偏导数，

  1. 把其他所有变量都当作常数，
    只关注你要求偏导数的那一个变量。
  2. 像 18.01 那样只对这个变量求 $f$ 的导数。
  3. 输出你得到的导数。

  这很容易，只需要 18.01 的内容。
]

我们刚刚看了三个例子，计算了 $f(x,y) = x^3 y^2 + cos (y)$、
$f(x,y,z) = e^(x y z)$ 和 $f(x,y) = x^2+y^2$ 的偏导数。
下面还有更多例子，你可以跟着做：

#sample[
  计算 $f(x,y,z) = x+y+z$ 的各个偏导数。
]
#soln[
  关于 $x$ 的偏导数可以通过对
  $ x |-> x + y + z. $
  求导得到。
  由于我们把 $y$ 和 $z$ 当作常数，只需对 $x$ 求导得到 $1$。
  对 $y$ 和 $z$ 也是如此。
  因此
  $
  f_x (x,y,z) &= 1 \
  f_y (x,y,z) &= 1 \
  f_z (x,y,z) &= 1. #qedhere
  $
]

#sample[
  计算 $f(x,y,z) = x y + y z + z x$ 的各个偏导数。
]
#soln[
  我们先对 $x$ 求导，此时把它看作函数
  $ x |-> (y + z) x + y z $
  并假定 $y$ 和 $z$ 是常数。
  这给出导数 $f_x (x,y,z) = y + z$。
  类似地，$f_y (x,y,z) = x + z$，$f_z (x,y,z) = x + y$。
  所以
  $
  f_x (x, y, z) &= y + z  \
  f_y (x, y, z) &= z + x  \
  f_z (x, y, z) &= x + y. #qedhere
  $
]

#sample[
  计算 $f(x,y) = x^y$ 的各个偏导数，其中假定 $x,y > 0$。
]
#soln[
  如果我们把 $y$ 看作常数、把 $x$ 看作变量，那么
  $ x |-> x^y $
  用“幂法则”求导得到 $y x^(y-1)$。
  然而，如果我们把 $x$ 看作常数、把 $y$ 看作变量，那么
  $ y |-> x^y = e^(log x dot y) $
  的导数最终是 $log x dot e^(log x dot y) = log x dot x^y$。
  （记住，在本书中 $log$ 表示_自然对数_。）
  因此
  $
    f_x (x,y) &= y x^(y-1) \
    f_y (x,y) &= log x dot x^y. #qedhere
  $
]

== [EXER] 习题

#exer[
  计算下列函数（定义在 $x,y,z > 0$ 上）的所有偏导数：

  - $f(x,y,z) = x / y + y / z + z / x$
  - $f(x,y,z) = sin(x y z)$
  - $f(x,y,z) = x^y + y^z + z^x$.
] <exer-partials>
