#import "@local/evan-zh:1.0.0":*

= Foxtrot 部的解答 <ch-sol-foxtrot>

== @exer-crit-2var 的解答（二元函数的临界点）

#recall-thm(<exer-crit-2var>)

#h3[求临界点]

一阶偏导数为：
$ f_x = (partial f) / (partial x) = 3 x^2 - 6 y , quad f_y = (partial f) / (partial y) = 6 y^2 - 6 x . $

为求出临界点，我们解方程组：
$ 3 x^2 - 6 y = 0 , quad 6 y^2 - 6 x = 0 . $
把方程改写为：$ x^2 = 2 y , quad y^2 = x . $
由 $y^2 = x$ 代入 $x^2 = 2 y$：
$ (y^2)^2 = 2 y ==> 0 = y (y^3 - 2). $
因此或者 $y = 0$，或者 $y = root(3, 2)$。
它们分别对应 $x = 0$ 与 $x = root(3, 4)$。

#h3[对临界点分类]

因此两个临界点是
$ #boxed[$ (x,y) = (0,0) " 与 " (x,y) = (root(3, 4), root(3, 2)). $] $

为对它们分类，计算二阶导数。
于是按 @sec-second-deriv 的记号，
$ A = f_(x x) = 6 x , quad C = f_(y y) = 12 y , quad B = f_(x y) = - 6 . $

- 在 $(0 , 0)$ 处，我们有 $ A C - B^2 = 72 (0) (0) - 36 = - 36 < 0 . $
  所以 $#boxed[$(0,0)$ is a saddle point]$.

- 在 $(root(3, 4) , root(3, 2))$ 处，我们有
  $ A C - B^2 = 72 (root(3, 4)) (root(3, 2)) - 36 = 108 > 0. $
  由于 $A, C > 0$ 且 $A C - B^2 > 0$，
  由此可得 $#boxed[$(root(3, 4) , root(3, 2))$ is a local minimum]$.

== @exer-crit-3var 的解答（三元函数的临界点）

#recall-thm(<exer-crit-3var>)

#h3[求临界点]

一阶偏导数为：
$ f_x = (partial f) / (partial x) = 2 x , quad f_y = (partial f) / (partial y) = 3 y^2 , quad f_z = (partial f) / (partial z) = 4 z^3 . $
为求出临界点，我们解方程组：
$ 2 x = 0 , quad 3 y^2 = 0 , quad 4 z^3 = 0 . $
分别解出各变量：
$ x = 0 , quad y^2 = 0 arrow.r.double y = 0 , quad z^3 = 0 arrow.r.double z = 0 . $
因此，唯一的临界点是：$ (0 , 0 , 0) . $

#h3[对临界点分类]

由于这是一个 $3$ 元函数，
我们无法用二阶导数判别法对它分类。
然而，单凭观察函数就能看出它既不是
局部最小值也不是局部最大值。
一个简单的办法是注意：对任意小的 $epsilon > 0$，我们都有

$ f(0, epsilon, 0) &= epsilon^3 > 0 \
  f(0, -epsilon, 0) &= -epsilon^3 < 0 $

换言之，原点 $(0,0,0)$ 附近总同时存在
大于 $f(0,0,0) = 0$ 和小于 $f(0,0,0) = 0$ 的点。
因此 $#boxed[$(0,0,0)$ is a saddle point]$.

== @exer-every-saddle 的解答（每个点都是鞍点）

#recall-thm(<exer-every-saddle>)

不，这是不可能的。

我们将证明下面这个结论：
#thm[
  设 $f : RR^2 -> RR$ 是一个每个点都是临界点的函数。
  那么 $f$ 必为常函数。
]
特别地，$f$ 的每个点都既是局部最小值又是局部最大值。
这意味着 $f$ 根本没有鞍点。

#proof[
  任取两点 $P_1 = (x_1, y_1)$ 与 $P_2 = (x_2, y_2)$。
  我们证明 $f(x_1, y_1) = f(x_2, y_2)$。

  沿从 $(x_1 , y_1)$ 到 $(x_2 , y_2)$ 的线段
  定义函数 $g : [0 , 1] arrow.r RR$ 为
  $ g (t) = f (x_1 + t (x_2 - x_1) , y_1 + t (y_2 - y_1)) . $
  函数 $g$ 表示 $f$ 限制在两点之间直线路径上的取值。

  由于 $f$ 可微，$g$ 在 $(0,1)$ 上也可微，且 $g$ 的
  导数可以用链式法则计算：
  $ g' (t) = (partial f) / (partial x) (d x) / (dif t) + (partial f) / (partial y) (d y) / (dif t) . $
  由于 $x = x_1 + t (x_2 - x_1)$ 且 $y = y_1 + t (y_2 - y_1)$，我们有
  $ (d x) / (dif t) = x_2 - x_1 , quad (d y) / (dif t) = y_2 - y_1 . $
  因此，
  $ g' (t) = (partial f) / (partial x) (x_2 - x_1) + (partial f) / (partial y) (y_2 - y_1) . $
  由假设，每个点都是临界点，也就是说
  在每一点处都有 $(partial f) / (partial x) = 0$ 与 $(partial f) / (partial y) = 0$。
  因此，
  $ g' (t) = 0 quad upright("for all ") t in (0 , 1) . $

  因此，$g$ 必为常函数。
  于是 $g(0) = g(1)$ 给出所需的 $f (x_1 , y_1) = f (x_2 , y_2)$。
]

== @exer-lattice-saddle 的解答（每个格点都是鞍点）

#recall-thm(<exer-lattice-saddle>)

#h3[构造这个函数]

函数
$ #boxed[$ f(x,y) = (cos((x-y) pi) - cos((x+y) pi)) / 2 = sin(pi x) sin(pi y) $] $
可行。
其想法是：鞍点出现当且仅当下列两个条件之一成立：

- 两个余弦都取到最大值，即 $x-y$ 与 $x+y$ 都是偶数；
- 两个余弦都取到最小值，即 $x-y$ 与 $x+y$ 都是奇数。

#h3[验证该函数可行]

构造出上面这个例子（它只是众多例子之一）是本题的主要难点。
为完整起见，我们来验证上面的猜测确实可行。
从现在起，取 $ f (x , y) = sin (pi x) sin (pi y) . $

一阶偏导数为：
$ f_x &= (partial f) / (partial x) = pi cos (pi x) sin (pi y) , \
  f_y &= (partial f) / (partial y) = pi sin (pi x) cos (pi y) . $
为求出临界点，我们令 $f_x = 0$ 与 $f_y = 0$：
$ pi cos (pi x) sin (pi y) = 0 , quad pi sin (pi x) cos (pi y) = 0 . $
由于 $pi != 0$，方程化为：
$ cos (pi x) sin (pi y) = 0 , quad sin (pi x) cos (pi y) = 0 . $
解这些方程：

- 当 $x = m + 1 / 2$（$m in bb(Z)$）时，$cos (pi x) = 0$。
- 当 $y = n$（$n in bb(Z)$）时，$sin (pi y) = 0$。
- 当 $x = m$（$m in bb(Z)$）时，$sin (pi x) = 0$。
- 当 $y = n + 1 / 2$（$n in bb(Z)$）时，$cos (pi y) = 0$。

临界点必须同时满足两个条件。
公共解只出现在两种情形中。

- 每个格点 $(m , n)$（其中 $m , n$ 为整数）都是临界点。
- 每个形如 $(m + 1/2, n + 1/2)$ 的点（其中 $m , n$ 为整数）都是临界点。

我们用二阶导数判别法分类。
计算
$ A &= f_(x x) = - pi^2 sin (pi x) sin (pi y) \
  C &= f_(y y) = - pi^2 sin (pi x) sin (pi y) \
  B &= f_(x y) = pi^2 cos (pi x) cos (pi y) . $

现在逐种情形检查：

- 在任意格点 $(m , n)$ 处，$A = C = 0$ 而 $B = pm pi^2$，
  所以 $A C - B^2 = -pi^4 < 0$。
  因此每个格点确实都是鞍点。

- 在任意形如 $(m + 1/2, n + 1/2)$ 的点处，$A = C = pm pi^2$ 而 $B = 0$，
  所以 $A C - B^2 = pi^4 > 0$。
  因此这里没有鞍点。

== @exer-opt-geo 的解答（几何最优化）

#recall-thm(<exer-opt-geo>)

对 $P = (x , y)$，我们令
$ f (x , y) = P A^2 + P B^2 + P C^2 . $
见 @fig-sol-echo-triangle。

#figure(
  image("figures/sol-echo-triangle.svg", width: auto),
  caption: [三角形 $A B C$ 以及与其三个顶点相连的点 $P$],
) <fig-sol-echo-triangle>

我们先把 $f$ 化成我们熟悉的形式。
为此使用勾股定理，如下：
$ P A^2 &= (x-0)^2 + (y - 12)^2 = x^2 + y^2 - 24 y + 144 , \
  P B^2 &= (x + 5)^2 + (y-0)^2 = x^2 + 10 x + 25 + y^2 , \
  P C^2 &= (x - 9)^2 + (y-0)^2 = x^2 - 18 x + 81 + y^2 . $
把这些表达式相加：
$ f (x , y) &= (x^2 + y^2 - 24 y + 144) + (x^2 + 10 x + 25 + y^2) + (x^2 - 18 x + 81 + y^2) \
  &= 3 x^2 + 3 y^2 - 8 x - 24 y + 250 . $

我们在整个空间 $cal(R) = RR^2$ 上最优化 $f$。
我们按 @sec-recipe-opt-easy 中的方法来做：

0. 没有边界，但当 $x -> pm oo$ 或 $y -> pm oo$ 时存在极限情形。
1. 为求出临界点，我们计算偏导数：
  $ f_x = (partial f) / (partial x) = 6 x - 8 = 0 &==> x = 4 / 3 .
    f_y = (partial f) / (partial y) = 6 y - 24 = 0 &==> y = 4 . $
  因此，唯一的临界点是：$ P = (4 / 3 , 4) . $
2. 没有需要考虑的边界点。
3. 若 $x -> pm oo$ 或 $y -> pm oo$，
  则二次项 $3 x^2 + 3 y^2$ 起主导作用，使 $f (x , y) -> +oo$。
  因此 $f$ 可以取任意大的值。

综合起来，点 $P = (4 / 3 , 4)$ 是使
$P A^2 + P B^2 + P C^2$ 最小的唯一一点。

#remark[
  如果把 $A B C$ 换成另一个三角形，
  那么最佳的点 $P$ 是三个点 $A$、$B$、$C$ 的_质心_。
  换言之，若 $A = (x_1, y_1)$、$B = (x_2, y_2)$、$C = (x_3, y_3)$，
  答案将是
  $ P = ((x_1 + x_2 + x_3)/3, (y_1 + y_2 + y_3)/3). $
  （这个点称为 $A B C$ 的_形心_或_重心_，
  常记作字母 $G$。）
]

== @exer-opt-lm-sin 的解答（正弦最优化）

#recall-thm(<exer-opt-lm-sin>)

0. 区域 $cal(R)$ 在 $x = 0$ 或 $y = 0$ 处有边界。
  当 $x -> +oo$ 或 $y -> +oo$ 时也存在极限情形。

1. 我们求 LM 临界点。
  梯度为 $ nabla f = vec(1,1) $ 与 $ nabla g = vec(cos(x), cos(y)). $

  由前两个方程，我们有：
  $ lambda = 1 / (cos (x)) quad upright("and") quad lambda = 1 / (cos (y)) . $
  令这两个 $lambda$ 的表达式相等，得到：
  $ frac(1, cos (x)) = frac(1, cos (y)) ==> cos (x) = cos (y) . $
  因此，只要下式成立我们就得到一个 LM 临界点
  $ cos(x) &= cos(y) \
    sin(x) + sin(y) &= 1 $
  注意 $sin(x)^2 = 1 - cos(x)^2 = 1 - cos(y)^2 = sin(y)^2$。
  由于 $sin(x)+sin(y) = 1 != 0$，我们得出 $sin(x) = sin(y) = 1/2$。
  结合 $cos(x) = cos(y)$，
  这意味着 $x$ 与 $y$ 必须相差 $2 pi$ 的整数倍。

  由于我们希望 $x+y$ 尽可能小，不妨取 $x=y$。
  使 $sin(x) = 1/2$ 的最小 $x$ 是 $x = pi/6$。
  所以在所有 LM 临界点中，最小值出现在
  $ f (pi / 6 , pi / 6) = pi / 6 + pi / 6 = pi / 3 . $

2. 在极限情形中，若 $x -> +oo$ 或 $y -> +oo$，则 $f -> +oo$。
3. 设 $x = 0$。那么 $sin(y) = 1$。
  所以边界的这一部分由点 $(0,pi/2)$、$(0,5pi/2)$、$(0,9pi/2)$、…… 组成。
  这些点都满足 $x+y >= pi/2$，所以它们都比之前的点 $(pi/6, pi/6)$ 差。

  同理，若 $y=0$，我们得到边界点 $(pi/2,0)$、$(5pi/2,0)$、$(9pi/2,0)$、……。
  同样它们都满足 $x+y >= pi/2$，所以都比之前的点 $(pi/6, pi/6)$ 差。

总之，最小可能值出现在
$ f (pi / 6 , pi / 6) = pi / 3 . $

== @exer-lm-abs 的解答（含绝对值的最优化）

#recall-thm(<exer-lm-abs>)

本题展示了 $nabla f$ 在某些点处不存在的情形：
$lr(|x|)$ 的导数只在 $x != 0$ 时存在。

为避免这个问题，我们按绝对值内那一项的符号来分情形讨论。
我们将分成#footnote[
  如果你愿意，也可以换一种方式分情形。
  例如，你可以分成 $x^2 + y^2 <= 25$ 与 $x^2 + y^2 > 25$，这样两种情形不会重叠。
  或者也可以分成三种情形：$x^2 + y^2 < 25$、$x^2 + y^2 = 25$、$x^2 + y^2 > 25$。
  不过我认为用 $x^2 + y^2 <= 25$ 与 $x^2 + y^2 >= 25$ 最干净，
  这样当 $x^2+y^2$ 趋近 $25$ 时你就不必考虑极限情形。
  边界 $x^2 + y^2 = 25$ 是共用的，所以你只需要处理一次。
] 两种情形 $x^2 + y^2 <= 25$ 与 $x^2 + y^2 >= 25$。
在每种情形中我们都执行该步骤。

#h3[$x^2 + y^2 <= 25$ 的情形]
我们按 @sec-recipe-opt-easy 来做。

0. 区域 $x^2 + y^2 <= 25$ 是二维区域，没有极限情形，
  但其边界为 $x^2 + y^2 = 25$。
1. 我们寻找所有满足 $nabla f = bf(0)$ 的点。
  在区域 $x^2 + y^2 <= 25$ 中，我们有
  $ f (x , y) = 25 - x^2 - y^2 - 3 x - 4 y . $
  解 $nabla f = bf(0)$ 得到
  $ (partial f)/(partial x) &= - 2 x - 3 = 0 , \
    (partial f)/(partial y) &= - 2 y - 4 = 0 . $
  因此得到临界点 $(x,y) = (- 3 / 2 , - 2)$。
  该点确实位于区域 $x^2 + y^2 < 25$ 中，
  所以它是临界点。
2. 我们在边界 $x^2 + y^2 = 25$ 上应用拉格朗日乘子（@sec-recipe-lm）。
  令 $g(x,y) = x^2 + y^2$。
  0. 这是一维区域，没有边界也没有极限情形。
  1. 我们寻找所有 LM 临界点。
    首先注意 $nabla g$ 在此边界上从不为零。
    方程组为
    $ - 3 &= lambda dot 2 x , \
    - 4 &= lambda dot 2 y \
    x^2 + y^2 &= 25 . $
    显然 $lambda$、$x$、$y$ 都必须非零。
    因此，前两个方程合起来给出 $x / y = 3 / 4$。
    于是，这种情形下我们得到两个临界点 $(3,4)$ 与 $(-3,-4)$。
  2. 没有需要考虑的边界点。
  3. 没有需要考虑的极限情形点。
3. 没有极限情形。

#h3[$x^2 + y^2 >= 25$ 的情形]
我们再次按 @sec-recipe-opt-easy 来做。

0. 区域 $x^2 + y^2 >= 25$ 是二维区域，当 $x -> pm oo$ 与 $y -> pm oo$
  时存在极限情形，其边界为 $x^2 + y^2 = 25$。
1. 在区域 $x^2 + y^2 >= 25$ 中，我们有
  $ f (x , y) = x^2 + y^2 - 25 - 3 x - 4 y . $
  解 $nabla f = bf(0)$ 得到
  $ (partial f)/(partial x) &= 2 x - 3 = 0 , \
    (partial f)/(partial y) &= 2 y - 4 = 0 . $
  这给出点 $(3/2, 2)$，
  但该点不在 $x^2 + y^2 >= 25$ 内部，
  所以我们不需要考虑它。
2. 我们需要在边界 $x^2 + y^2 = 25$ 上应用拉格朗日乘子（@sec-recipe-lm）。
  然而，我们在前面 $x^2 + y^2 <= 25$ 的情形中已经做过了。
  所以这里照搬同样的计算即可。
3. 当 $|x| -> +oo$ 或 $|y| -> +oo$ 时，平方项起主导作用，$f -> +oo$。
  因此可知 $f$ 是有上界的。

#h3[综合起来]

我们在寻找 $f$ 的全局最小值。
汇总这些临界点，我们检查
$ f (- 3 / 2 , - 2) &= 125 / 4 , \
  f (3 , 4) &= - 25 , \
  f (- 3 , - 4) = 25 . $
因此，全局最小值是 $#boxed[$ f(3,4) = -25 $]$。


== @exer-mt2p1 的解答（蝴蝶）

#recall-thm(<exer-mt2p1>)

#h3[轨迹的草图]

其实我们先画轨迹的草图（尽管这是最后一部分），
因为这样更容易看清后面各部分在讲什么。
见 @fig-mt2-butterfly。
由 $bf(r) (t) = chevron.l cos (t) , cos (t) chevron.r$ 描述的轨迹
在 $x y$ 平面中描出一条直线，因为对所有 $t$ 而言，
$x$ 坐标与 $y$ 坐标都相等。具体地说，蝴蝶的运动沿直线 $y = x$，
其中 $t in [0 , 2 pi]$ 使它在 $x = 1$ 与 $x = - 1$ 之间往复。

#figure(
  image("figures/mt2-butterfly.svg", width: auto),
  caption: [蝴蝶沿平面振翅飞行。
  为便于说明，轨迹上另外几个点用绿色标出，
  但真正重要的是蓝色的端点。
  绿色点稍稍偏移了一点，以便同时显示轨迹的两部分，
  例如 $bf(r)(pi/2) = (0,0)$ 画得比它应在的位置略偏左。],
) <fig-mt2-butterfly>

轨迹是沿直线 $y = x$ 从 $(1,1)$ 到 $(- 1 , - 1)$ 再返回的直线段。

#h3[蝴蝶在 $t = pi / 3$ 处的速率]

蝴蝶的速率由其速度向量的模长给出，
而速度向量是 $bf(r) (t)$ 对时间 $t$ 的导数。

首先计算速度 $bf(r)'(t)$：
$ bf(r)' (t) = (d) / (dif t) chevron.l cos (t) , cos (t) chevron.r = chevron.l - sin (t) , - sin (t) chevron.r . $
（它的方向沿直线 $y=x$，这正是我们所预期的。）

任意时刻 $t$ 的速率是速度向量的模长：
$ "速率" = lr(|bf(r)' (t)|) = sqrt((- sin (t))^2 + (- sin (t))^2) = sqrt(2 sin^2 (t))
  = sqrt(2) lr(|sin (t)|) . $

在 $t = pi / 3$ 处，我们有：$ sin (pi / 3) = sqrt(3) / 2 . $
因此，$t = pi / 3$ 处的速率是：
$ "速率" = sqrt(2) dot sqrt(3) / 2 = sqrt(6) / 2 . $

#h3[蝴蝶轨迹从 $t = 0$ 到 $t = 2 pi$ 的弧长]

注意，由轨迹的草图，我们其实完全不需要微积分
就能求出弧长。的确，"弧长" 这个叫法有点用词不当，因为这里的
"弧" 不过是两条线段！

由勾股定理，从 $(1,1)$ 到 $(-1,-1)$ 的距离是
$ sqrt((1-(-1))^2 + (1-(-1))^2) = sqrt(4+4) = 2sqrt(2). $
所以总路程为 $ 2sqrt(2) + 2sqrt(2) = 4sqrt(2). $

当然，也可以使用弧长公式，下面我们演示如何做。
轨迹的弧长由速率的积分给出：
$ L = integral_(t="起始时刻")^("终止时刻") lr(|bf(r)' (t)|) dif t . $

我们刚才看到 $lr(|bf(r)' (t)|) = sqrt(2) lr(|sin (t)|)$。
因此，从 $t = 0$ 到 $t = 2 pi$ 的弧长为：
$ L = integral_0^(2 pi) sqrt(2) lr(|sin (t)|) dif t . $
#warning[
  别忘了绝对值！
  一般地，对实数 $X$，我们有 $sqrt(X^2) = |X|$。
  如果你在这里漏掉绝对值，最终会得到答案 $0$，
  这说不通，因为蝴蝶走过的路程肯定大于 $0$。
  记住，速率（速度向量的绝对值）应当始终非负。
]

由于有绝对值，
我们可以把积分拆成两部分。
在区间 $[0 , pi]$ 上 $sin (t) >= 0$，在区间 $[pi , 2 pi]$ 上 $sin (t) <= 0$，所以
$ L = sqrt(2) (integral_0^(pi) sin (t) dif t + integral_(pi)^(2 pi) - sin (t) dif t) . $
这个积分的图示见 @fig-abs-sin。

#figure(
  image("figures/mt2-abs-sin.svg", width: auto),
  caption: [积分 $integral_0^(2pi) |sin(t)| dif t$ 是第一个波峰
  $integral_0^(pi) sin(t) dif t$（它没有带绝对值）的两倍。],
) <fig-abs-sin>

两个积分相同，所以我们算一个再乘以 2：
$ integral_0^pi sin (t) dif t = [- cos (t)]_0^pi = - cos (pi) + cos (0) = 1 + 1 = 2 . $
因此，总弧长为：
$ L = sqrt(2) dot 2 dot 2 = 4 sqrt(2) . $

== @exer-mt2p2 的解答（等值线的切线）

#recall-thm(<exer-mt2p2>)

第一步是求出题目中没有给出的 $k$ 的值。
首先，把点 $(1 , 2)$ 代入函数 $f (x , y)$：
$ f (1 , 2) = 1^3 + k (2^2) = 1 + 4 k . $
已知 $f (1 , 2) = 21$，所以我们令该式等于 21：
$ 1 + 4 k = 21 ==> 4 k = 20 ==> k = 5 . $
因此，该函数为：
$ f (x , y) = x^3 + 5 y^2 . $

既然知道了 $f$，我们就可以通过求偏导数来计算梯度：
$ nabla f = vec((partial f) / (partial x) , (partial f) / (partial y)) = vec(3x^2 , 10y). $
现在在 $P = (1 , 2)$ 处对梯度求值：
$ nabla f (1 , 2) = vec( 3 (1)^2 , 10 (2) ) = vec(3 , 20) . $
梯度总是垂直于切线，所以切线必具有
$ 3 x + 20 y = t $
的形式，其中 $t$ 是某个数。
这条直线经过 $(1,2)$，所以我们可以得到
$ t = 3 dot 1 + 20 dot 2 = 43. $
因此所求的直线是
$ 3 x + 20 y = 43. $

== @exer-mt2p3 的解答（$f = x^(5 y)$ 的近似）

#recall-thm(<exer-mt2p3>)

已知函数：$ f (x , y) = x^(5 y) $，要求从点 $(1 , 3)$ 出发
用线性近似估计 $f (1.001 , 3.001)$，
在该点处 $ f(1,3) = 1. $

我们先计算 $nabla f$。
- 为求出关于 $x$ 的偏导数，使用幂法则与链式法则：
  $ (partial f) / (partial x) = 5 y x^(5 y - 1) . $
- 对于关于 $y$ 的偏导数，我们把 $x$ 视为常数：
  $ (partial f) / (partial y) = x^(5 y) log(x) dot 5 . $
因此，$f (x , y)$ 的梯度为：
$ nabla f (x , y) = ⟨5 y x^(5 y - 1) , 5 x^(5 y) log (x)⟩ . $
于是 $(1,3)$ 处的梯度为
$ nabla f(1,3) = vec(15,0). $

$f (1.001 , y)$ 在点 $(1 , 3)$ 附近的线性近似可以
用梯度与位移的点积表示为：
$ f (1.001 , 3.001) &approx f (1 , 3) + nabla f (1 , 3) dot vec(0.001, 0.001)
  &= 1 + vec(15,0) dot vec(0.001, 0.001) = 1.015. $

== @exer-mt2p4 的解答（余弦—四次式的临界点）

#recall-thm(<exer-mt2p4>)

虽然这道题是以 18.02 的题目给出的，
但其实基本上只用 18.01 的方法就能解决。
我们仍将从 18.02 的视角给出解答，
但会多次指出哪些地方只用 18.01 的方法就足够了。

#h3[求临界点]

为求出临界点，我们先计算梯度。
偏导数为
$ f_x (x , y) &= (partial) / (partial x) (cos (pi x) + y^4 / 4 - y^3 / 3 - y^2) = - pi sin (pi x) . \
  f_y (x , y) &= (partial) / (partial y) (cos (pi x) + y^4 / 4 - y^3 / 3 - y^2) = y^3 - y^2 - 2 y . $
因此
$ nabla f(x,y) = vec(-pi sin(pi x), y^3 - y^2 - 2 y). $
令它等于 $bf(0)$，我们就可以分别解每个方程：

- 当 $x$ 为整数时，$-pi sin(pi x) = 0$ 成立。
- 为解 $y^3 - y^2 - 2 y = 0$，
  把方程因式分解：$ 0 = y (y^2 - y - 2) = y (y - 2) (y + 1) = 0 . $

所以有无限多个临界点！
临界点出现在 $x$ 为任意整数
且 $y = -1$、$y = 0$、$y = 2$ 时。
见 @fig-mt2-crits。

#figure(
  image("figures/mt2-crits.svg", width: auto),
  caption: [函数临界点在 $x y$ 平面中的图像，用红点表示。],
) <fig-mt2-crits>


#h3[用二阶导数判别法分类]

现在我们用二阶导数判别法对每个点分类。
计算所需的二阶导数：
$ A = f_(x x) (x , y) &= (partial^2 f) / (partial x^2) = - pi^2 cos (pi x) , \
  B = f_(x y) (x , y) &= (partial^2 f) / (partial x partial y) = 0, \
  C = f_(y y) (x , y) &= (partial^2 f) / (partial y^2) = 3 y^2 - 2 y - 2. $

- 若 $x$ 为偶数则 $A = -pi^2$，若 $x$ 为奇数则 $A = pi^2$。
- 恒有 $B = 0$。
- 我们有
  $ C = cases(
    3(-1)^2 - 2(-1) - 2 &= 3 &" if " y = -1,
    3(0)^2 - 2(0) - 2 &= -2 &" if " y = 0,
    3(2)^2 - 2(2) - 2 &= 6 &" if " y = 2.) $

我们把六种情形汇总在下表中。
对表中每一项，我们还计算 $A C - B^2$，然后根据二阶导数判别法
给出结论。

#figure(
  table(
    columns: 3,
    align: left,
    table.header([],
      [$x = ..., -4, -2, 0, 2, 4 ...$ 为偶数],
      [$x = ..., -3, -1, 1, 3 ...$ 为奇数]),
    [$y = -1$],
      [$(A,B,C) = (-pi^2,0,3)$ \ $A C - B^2 = -3pi^2 < 0$ 给出*鞍点*],
      [$(A,B,C) = (pi^2,0,3)$ \ $A C - B^2 = 3pi^2 > 0$ 给出*局部最小值*],
    [$y = 0$],
      [$(A,B,C) = (-pi^2,0,-2)$ \ $A C - B^2 = 2pi^2 > 0$ 给出*局部最大值*],
      [$(A,B,C) = (pi^2,0,-2)$ \ $A C - B^2 = -2pi^2 < 0$ 给出*鞍点*],
    [$y = 2$],
      [$(A,B,C) = (-pi^2,0,6)$ \ $A C - B^2 = -6pi^2 < 0$ 给出*鞍点*],
      [$(A,B,C) = (pi^2,0,6)$ \ $A C - B^2 = 6pi^2 > 0$ 给出*局部最小值*],
  ),
  kind: table
) <table-sol-mt2-critical>

#h3[不用二阶导数判别法的另一种方法]

只看给定的函数本身也能得到同样的分类。
关键在于这个函数能漂亮地拆成两半：
如果定义单变量函数
$ a(x) &:= cos(pi x) \
  b(y) &:= y^4/4 - y^3/3 - y^2 $
那么
$ f(x,y) = a(x) + b(y). $
在这种情况下，下面的结论成立：

- 如果 $x$ 是 $a(x)$ 的临界点且 $y$ 是 $b(y)$ 的临界点，
  那么点 $P = (x,y)$ 是 $f(x,y)$ 的临界点。
- 若是如此，那么点 $P$ 就是……
  - 若 $x$ 是 $a(x)$ 的局部最小值且 $y$ 是 $b(y)$ 的局部最小值，则是 $f$ 的局部最小值。
  - 若 $x$ 是 $a(x)$ 的局部最大值且 $y$ 是 $b(y)$ 的局部最大值，则是 $f$ 的局部最大值。
  - 否则是鞍点。

如果你对鞍点有好的概念性理解，这一点应当是显然的。
它本质上就是 @sec-saddle-sim 中的 @fig-whysaddle。

#figure(
  image("figures/mt2-graph.svg", width: auto),
  caption: [函数 $f$ 只是两个独立函数之和，
  可以分别独立地最优化。],
) <fig-sol-mt2-graph>

这给出与上面相同的表格，因为：

- $a(x) = cos(pi x)$ 的临界点是 $x = -2, -1, 0, 1, 2, ...$。
  当 $cos$ 取到 $-1$ 时，取局部最小值的是奇数点；
  当 $cos$ 取到 $+1$ 时，取局部最大值的是偶数点。

- $b(y) = y^4 / 4 - y^3 / 3 - y^2$ 的临界点
  是 $b'(y) = y^3 - y^2 - 2y = y(y+1)(y-2)$ 的根，
  也就是我们之前看到的同样的 $y = -1, 0, 2$。
  见 @fig-sol-mt2-graph。
  在 $y =-1$ 与 $y = 2$ 处有局部最小值，
  在 $y = 0$ 处有局部最大值。

#h3[全局最小值与最大值]

首先，我们在每个临界点上求 $f$ 的值。
如果沿用之前的 $a$ 与 $b$ 记号并计算下面这些，这一步最容易做
$
  a("偶") &= cos(pi dot "偶") = 1 \
  a("奇") &= cos(pi dot "奇") = -1 \
  b(-1) &= (-1)^4/4 - (-1)^3/3 - (-1)^2 = -5/12 \
  b(0) &= (0)^4/4 - (0)^3/3 - (0)^2 = 0 \
  b(2) &= (2)^4/4 - (2)^3/3 - (2)^2 = -8/3
$
于是我们得到 @table-sol-mt2-extremes 中所示的六个值。
#figure(
  table(
    columns: 3,
    align: left,
    table.header([],
      [$x = ..., -4, -2, 0, 2, 4 ...$ 为偶数],
      [$x = ..., -3, -1, 1, 3 ...$ 为奇数]),
    [$y = -1$],
      [$f("偶", -1) = 1 - 5/12 = 7/12$],
      [$f("奇", -1) = -1 - 5/12 = -17/12$],
    [$y = 0$],
      [$f("偶", 0) = 1 + 0 = 1$],
      [$f("奇", 0) = -1 + 0 = -1$],
    [$y = 2$],
      [$f("偶", 2) = 1 - 8/3 = -5/3$],
      [$f("奇", 2) = -1 - 8/3 = -11/3$],
  ),
  kind: table,
  caption: [$f$ 在各临界点处的值]
) <table-sol-mt2-extremes>

完全没有不等式约束，所以我们只需考虑极限情形
$x -> pm oo$ 或 $y -> pm oo$。

当 $y -> pm oo$ 时，四次式 $b(y) = y^4 / 4 - y^3 / 3 - y^2$ 趋于无穷大。
这已经说明不可能存在全局最大值。

在 $x -> pm oo$ 的情形中，
$f(x,y)$ 的余弦项会在 $-1$ 与 $1$ 之间振荡，周期为 $2 pi$。
所以在这里得不到新的更小的 $f$ 值。

#h3[看全局最小值与最大值的另一种方式]

由于
$ f(x,y) = a(x) + b(y) $
$f$ 的全局最小值应当是 $a$ 与 $b$ 的全局最小值之和，
同样地 $f$ 的全局最大值应当是 $a$ 与 $b$ 的全局最大值之和。
所以我们也可以像 @fig-sol-mt2-graph 那样，
对 $a$ 与 $b$ 分别使用 18.01 的方法。
即：

- 由于 $min a(x) = -1$ 且 $min b(y) = - 8/3$，全局最小值是 $-11/3$。
- 由于 $max a(x) = 1$ 而 $min b(y) = +oo$，不存在全局最大值。

记住，这只在能把 $f(x,y)$ 轻松拆成
关于 $x$ 的函数加上关于 $y$ 的函数时才有效。
对大多数函数 $f(x,y)$（例如 $x y$ 或 $e^x sin(y)$）来说，
这个做法行不通。

== @exer-mt2p5 的解答（拉格朗日乘子练习）

#recall-thm(<exer-mt2p5>)

令 $f(x,y,z) = x+2y+2z$。
令 $cal(R)$ 表示区域 $x^2 + y^2 + z^2 <= 100$（半径为 $10$ 的球体），
并令 $cal(S)$ 表示边界 $x^2 + y^2 + z^2 = 100$（半径为 $10$ 的球面）。
我们按照 @sec-recipe-opt-easy 与 @sec-recipe-lm 中的方法所描述的步骤来做。

0. $cal(R)$ 是三维的，没有极限情形，但有一个二维边界 $cal(S)$。
  （由于条件 $x^2 + y^2 + z^2 <= 100$ 且所有平方项都非负，
  任何变量都不可能趋于 $pm oo$。）
1. 我们计算目标函数 $f(x,y,z) = x+2y+2z$ 的所有临界点。
  其梯度为 $ nabla f = vec(1, 2, 2) $
  所以没有临界点，因为这个梯度从不为 $0$。
2. $cal(S)$ 中的边界是球面，不容易直接处理。
  我们搬出拉格朗日乘子，把整套方法再完整走一遍。
  0. $cal(S)$ 是二维的，没有极限情形也没有边界。
  1. 我们通过令 $g(x,y,z) = x^2+y^2+z^2$ 来寻找 LM 临界点，
    于是 $cal(S)$ 是等值面 $g = 100$。
    计算 $g$ 的梯度：
    $ nabla g = vec(2x, 2y, 2z). $
    回想一下，LM 临界点是指满足 $g(P) = 100$ 且 _或者_
    $ nabla f (P) = lambda nabla g (P) " OR " nabla (P) = 0. $
    的点。这个梯度 $nabla g$ 可能在 $(x,y,z) = (0,0,0)$ 处为 $bf(0)$，
    但该点不在 $cal(S)$ 上，所以我们不予考虑。

    在主要情形 $nabla f = lambda g$ 中，我们寻找满足下式的点
    $ 1 &= lambda dot 2 x \
      2 &= lambda dot 2 y \
      2 &= lambda dot 2 z. $
    我们的策略是消去除 $lambda$ 外的所有变量，即写出
    $ x &= 1 / (2 lambda) \
      y &= 1 / (lambda) \
      z &= 1 / (lambda). $
    把它代回约束方程 $x^2 + y^2 + z^2 = 100$
    并化简，得到
    $ ((1) / (2 lambda))^2 + (1 / lambda)^2 + (1 / lambda)^2 &= 100 \
    <==> (9) / (4 lambda^2) = 100 \
    <==> lambda^2 = 9 / 400 \
    <==> lambda = pm 3 / 20 . $
    把这两个 $lambda$ 值代入，得到
    $ (x,y,z) = (10/3, 20/3, 20/3) " 与 " (x,y,z) = (-10/3, -20/3, -20/3)$
    这就是那两个 LM 临界点。
    求值得到
    $ f (10 / 3 , 20 / 3 , 20 / 3) = 10 / 3 + 2 dot 20 / 3 + 2 dot 20 / 3 &= 30 \
      f (-10 / 3 , -20 / 3 , -20 / 3) = -10 / 3 + 2 dot -20 / 3 + 2 dot -20 / 3 &= -30. $

  2. 对 $cal(S)$ 没有需要考虑的边界情形。
  3. 对 $cal(S)$ 没有需要考虑的极限情形。

  总之，最大值是 $30$，最小值是 $-30$，
  分别在前面求得的点 $(10/3, 20/3, 20/3)$ 与 $(-10/3, -20/3, -20/3)$ 处取得。

3. 对 $cal(R)$ 没有需要考虑的极限情形。

#digression[
  注意，其实可以_先验地_看出，任何最大值或最小值都应出现在球面上。
  看清这一点的一种方式是：如果取一个严格位于 $cal(R)$ 内部的点
  例如 $P = (6,8,0)$，总可以通过缩放 $P$ 来增大 $f$ 的绝对值，
  直到它落在球面上（例如 $(60, 80, 0)$）。
  因此在假定最大值与最小值位于 $cal(S)$ 上时，并没有失去一般性。
  所以如果足够敏锐，可以直接跳到 $cal(S)$ 上的 LM 步骤，
  完全忽略区域 $cal(R)$。
]

== @exer-mt2p6 的解答（切平面）

#recall-thm(<exer-mt2p6>)

函数 $f$ 的梯度为
$ nabla f = vec(2(x-1), 3(y-1)^2, 4(z-1)^3) $
因此在原点处的梯度是
$ nabla f (0,0,0) = vec(-2, 3, -4). $
切平面 $cal(H)$ 由那些与 $vec(-2, 3, -4)$ 垂直的向量组成。
这个平面是二维的。
所以，为求出张成 $cal(H)$ 的两个向量，
根据 @sec-detect-basis 中 "买二送一" 的结论，
我们只需给出任意两个线性无关（即互不为倍数）的向量，
它们都与 $vec(-2, 3, -4)$ 垂直。

有很多可行的选择。
一个例子可以是 $vec(3, 2, 0)$ 与 $vec(0, 4, 3)$。
这两个向量显然互不为倍数，而且
$ vec(-2, 3, -4) dot vec(3,2,0) &= (-2) dot 3 + 3 dot 2 + (-4) dot 0 = 0 \
  vec(-2, 3, -4) dot vec(0,4,3) &= (-2) dot 0 + 3 dot 4 + (-4) dot 3 = 0 $
所以它们确实是包含在 $cal(H)$ 中的切向量。
