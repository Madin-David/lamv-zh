#import "@local/evan-zh:1.0.0":*

= 临界点

== [TEXT] 18.01 中的临界点

首先，与 18.01 做一个对比。
早在你有一个可微的单变量函数 $f : RR -> RR$、
并试图把它最小化的时候，你会用到下面这些术语：

#figure(
  table(
    columns: 2,
    align: left,
    table.header([18.01 术语], [含义]),
    [全局最小值], [在你所考虑的整个区域上函数 $f$ 的最小值],
    [局部最小值], [一个点，在该点处 $f$ 比小邻域内任何附近的点都小],
    [临界点], [一个满足 $f'(x) = 0$ 的点],
  ),
  kind: table,
  caption: [18.01 中关于临界点的术语]
)
每一行都包含它上面的所有行，但反过来不成立。
下面这幅图给出了一个例子，展示随机函数
$f(x) = -1/5 x^6 - 2/7 x^5 + 2/3 x^4 + x^3$ 的上述各种情形。
在 @fig-1801-critical-points 中从左到右共有四个临界点：

- 一个局部最大值（它不是全局最大值），用蓝色画出。
- 一个局部最小值（它不是全局最小值），用绿色画出。
- 一个临界拐点（critical inflection point）——既不是局部最小值_也不是_局部最大值。用橙色画出。
- 一个全局最大值，用紫色画出。

注意这里根本没有全局最小值，因为当 $x -> -oo$ 或 $x -> +oo$ 时，
函数 $f$ 在两个方向上都会趋于 $-oo$。

#figure(
  image("figures/minmax-1801graph.svg", width: auto),
  caption: [18.01 中单变量函数图像里的一些临界点示例。],
) <fig-1801-critical-points>

== [TEXT] 18.02 中的临界点 <sec-critical-points>

在 18.02 中，当我们考虑 $f : RR^n -> RR$ 时，唯一做出的改动是：

#definition[
  在 18.02 中，我们把*临界点*的定义推广为：使 $nabla f (P) = bf(0)$ 为零向量的点 $P$。
  （另外两个定义不变。）
]

话一出口，我就得把 18.01 中相应的警告也搬过来：
#warning[
  - 请记住，下面每个蕴含关系
    $ "全局最小值" ==> "局部最小值" ==> "临界点，即 " nabla f = bf(0) $
    都只有单向成立，反之不然。
    所以局部最小值未必是全局最小值；
    而梯度为零的点也未必是（哪怕局部的）最小值。
    你仍然应该找出所有临界点，
    只是要意识到其中许多其实并不是最小值或最大值。
  - 可能根本_不存在_任何全局最小值或最大值，就像我们刚刚看到的那样。
]

#definition[
  在 18.02 中，一个既不是局部最小值也不是局部最大值的临界点称为*鞍点*（saddle point）。
]

#example[
  对于函数 $ f(x,y) = x^2 - y^2. $ 而言，
  最值得记在脑子里的鞍点例子就是原点。
  为什么它是鞍点？我们有 $f(0,0) = 0$，梯度也为零，因为
  $ nabla f = vec(2x, 2y) ==> nabla f (0,0) = vec(2 dot 0, 2 dot 0) = vec(0, 0). $
  问题在于 $x$ 和 $y$ 的小变化在符号上互相冲突。
  具体来说，如果我们沿 $x$ 方向向左或向右挪一点点，
  那么 $f$ 会_增大_一点点，例如
  $ f(0.1, 0) = f(-0.1, 0) = 0.01 > 0. $
  但 $-y^2$ 这一项的作为恰恰相反：如果我们沿 $y$ 方向向上或向下挪一点点，
  那么 $f$ 会_减小_一点点。
  $ f(0, 0.1) = f(0, -0.1) = -0.01 < 0. $
  所以问题就在于 $x$ 方向与 $y$ 方向小变化的符号互相冲突。
  这使得 $f$ 既不是局部最小值，也不是局部最大值。

  其实 $pm x$ 和 $pm y$ 本身并没有什么特别之处；
  我之所以用它们，只是为了算起来方便。
  你可以在 @fig-hyperbola-saddle 中看到 $f$ 在其他附近点处的取值。
]

#figure(
  image("figures/minmax-hyperbola-saddle.svg", width: auto),
  caption: [在距鞍点 $(0,0)$ 距离为 $0.1$ 处 $f(x,y) = x^2-y^2$ 的取值。
    绿色数值为正，红色数值为负。
    它之所以是鞍点，正是因为两者都有。
  ],
) <fig-hyperbola-saddle>


#remark[
  “鞍点”这个名字来自下面这幅图像：如果你观察曲面
  $ z = x^2 - y^2 $
  那么在 $(0,0)$ 附近，它看起来就像马鞍。
  它沿 $x$ 方向向上弯曲，但沿 $y$ 方向向下弯曲。
]

我们马上就会给出区分鞍点与局部最小值、局部最大值的方法；
就像 18.01 一样，有一个东西叫做二阶导数判别法（second derivative test）。
首先，先讲一段题外话和几个求临界点的例子。

== [SIDENOTE] 鞍点比临界拐点常见得多 <sec-saddle-sim>

乍看之下，你可能会觉得鞍点就是临界拐点在 18.02 中的版本。
然而，这种类比其实对你的直觉并没有什么好处，
而且*鞍点的感觉与 18.01 的临界拐点很不一样*。
下面我来解释原因。

在 18.01 中，临界点_有可能_既不是局部最小值也不是局部最大值，
我们称这类点为临界拐点。
然而在 18.01 中，这种情况其实非常罕见。
为了把这一点说清楚，假设我们考虑一个形如
$ f(x) = square x^3 + square x^2 + square x + square $
的随机 18.01 函数，其中每个方块都是 $-1000000$ 到 $1000000$（含端点）之间的随机整数。
在这种形状的大约 $10^(25)$ 个函数中，
你会发现，虽然临界点有很多，
但找到临界拐点的概率大约只有 $10^(-15)$ ——
比中彩票还差得远。
（当然，如果你_知道_该往哪儿找，还是能找到的：
例如 $f(x) = x^3$ 在原点就有一个临界拐点。）

在 18.02 中，这一点不再成立。
如果我们挑选一个形式类似的随机函数
$ f(x,y) = square x^3 + square x^2 + square x + square y^3 + square y^2 + square y + square $
其中每个方块填入一个 $-1000000$ 到 $1000000$ 之间的数，
那么你会突然发现鞍点遍地都是。
例如，当我把这个模拟运行 $10000$ 次时，
在出现的临界点中，我最终得到的大致是

- 24.6% 局部最小值
- 25.3% 局部最大值
- 50.1% 鞍点。

而真实的极限（把 $10^6$ 换成 $N$ 并令 $N -> oo$ 取极限）
正是你从上面能猜到的：25%、25%、50%。
（如果你想看代码，它在附录里，见 @appendix-saddle-sim。）

为什么 18.02 的情形如此不同？
归根结底在于：在 18.02 中，你可能有两个互相冲突的方向。
对于我在这里做的两个实验，请看 @fig-whysaddle 中的图。
这里 $P$ 是一个临界点，
我们考虑从它出发沿两个方向之一走开。
如果 $f$ 增大，我就画一个蓝色 $+$ 箭头；如果 $f$ 减小，就画一个红色 $-$ 箭头。

#figure(
  image("figures/minmax-whysaddle.svg", width: auto),
  caption: [为什么 18.01 与 18.02 的多项式实验会有完全不同的结果。],
) <fig-whysaddle>

在 18.01 的实验中，我们看到指向_相反_方向的两个箭头
几乎总是同一种颜色。
所以在 18.01 中，当我们只能沿一个方向走时，
这意味着几乎每个点要么是局部最小值，要么是局部最大值。
但 18.02 的图像完全不同，因为没有什么能迫使
南/北这一对与东/西这一对取相同的符号。
对于一个“随机”函数，如果你认为各种颜色出现的可能性相同，
那么有一半的时间箭头颜色不匹配，于是你就得到一个鞍点。

整个这一节针对的都是形如 $P(x) + Q(y)$ 的二元函数，所以它本身已经是一种简化了。
如果你对多项式 $f(x,y,z) = P(x) + Q(y) + R(z)$ 做类似的
三变量实验：

- 12.5% 局部最小值
- 12.5% 局部最大值
- 75.0% 鞍点。

如果我们回到_任意_二元函数的世界，真相还要比这更复杂。
在这个旁注里，我只讨论了形如 $P(x) + Q(y)$ 的函数 $f(x,y)$，
其中 $P$ 和 $Q$ 是多项式。
函数的 $x$ 部分与 $y$ 部分完全互不相关，
所以我们只看了南/北/东/西这四个方向。
但大多数二元函数的 $x$ 与 $y$ 之间存在更多依赖关系，
例如 $f(x,y) = x^2y^3$ 或 $f(x) = e^x sin(y)$ 之类。
这时你实际上需要考虑比南/北/东/西更多的方向。
#digression[
  例如，Poonen 的讲义（见问题 9.22）展示了一个奇怪的_猴鞍面_（monkey saddle）：
  点 $(0,0)$ 是
  $ f(x,y) = x y (x-y) $
  的临界点，其附近 $f$ 的取值分成六个区域，正负交替，
  这与 @fig-hyperbola-saddle 形成对比，那里圆周上只有四个区域。
  （另见 #link("https://w.wiki/6LLG")[维基百科：猴鞍面]。）
  Poonen 还邀请读者自己想出_章鱼鞍面_（octopus saddle）
  （听起来它需要十六个区域，章鱼的每条腿对应八个向下的区域）。
]

== [RECIPE] 求临界点

求临界点这件事，在纸上你只要照着定义做就行：

#recipe(title: [求临界点的方法])[
  要求 $f : RR^n -> RR$ 的临界点

  1. 计算梯度 $nabla f$。
  2. 令它等于零向量，并求解由此得到的 $n$ 元 $n$ 个方程的方程组。
]

可能有些麻烦的地方在于，你必须解一个方程组。
取决于你的函数有多难对付，要算对代数可能需要一点创造力。
我们会展示一些代数非常简单的例子，
也会展示一些代数复杂得多的例子。

#sample[
  计算 $f(x,y,z) = x^2 + 2 y^2 + 3 z^2$ 的临界点。
]
#soln[
  梯度为
  $ nabla f (x,y,z) = vec(2x, 4y, 6z). $
  要使它等于 $vec(0,0,0)$，我们需要求解这个三变量方程组
  $
  2x &= 0 \
  4y &= 0 \
  6z &= 0 \
  $
  它简单得几乎有点侮辱人：$x=y=z=0$。
  唯一的临界点是 $(0,0,0)$。
]

#sample[
  计算 $f(x,y) = x y (6 - x - y)$ 的临界点。
]
#soln[
  这个例子比上一个讨厌得多，尽管变量更少，
  因为你会被迫做分类讨论。
  如你将看到的，你需要解四个线性方程组，而不只是一个。

  我们把 $ f(x,y) = 6 x y - x^2 y - x y^2. $ 展开。
  所以 $ nabla f = vec(6 y - 2 x y - y^2, 6 x - x^2 - 2 x y). $
  因此，需要求解的方程组是
  $
    y (6 - 2 x - y) &= 0 \
    x (6 - 2 y - x) &= 0.
  $
  坏消息是这些都是二次方程。
  幸运的是，它们已经是因式分解的形式，所以我们可以把它们改写成“或”的陈述：
  $
    y (6 - 2 x - y) &= 0 ==> (y = 0 " OR " 2x + y = 6) \
    x (6 - 2 y - x) &= 0 ==> (x = 0 " OR " x + 2y = 6).
  $
  所以实际上有 $2^2 = 4$ 种情形需要考虑，而我们必须手动处理全部四种。
  这些情形可以排成下面的 $2 times 2$ 表格；我们把四个方程组都解出来。
  #align(center)[
    #block(breakable: false)[
      #table(
        columns: 3,
        align: left + horizon,
        stroke: 0.5pt,
        [], [上面的方程给出 $y=0$], [上面的方程给出 $2x+y=6$],
        [下面的方程给出 $x=0$], [$cases(y=0, x=0) ==> (x,y) = (0,0)$], [$cases(2x+y=6, x=0) ==> (x,y) = (0,6)$],
        [下面的方程给出 $x+2y=6$], [$cases(y=0, x+2y=6) ==> (x,y)=(6,0)$], [$cases(2x+y=6, x+2y=6) ==> (x,y) = (2,2)$]
      ),
    ]
  ]
  所以我们得到四个临界点，每种情形一个：$(0,0)$、$(0,6)$、$(6,0)$ 和 $(2,2)$。
]
== [TEXT] 解方程组的一般建议 <sec-system-advice>

在上一个 $f(x,y) = x y(6 - x - y)$ 的例子中，
我们看到解方程组并不一定是个轻松的任务。
一般来说，求解一个一般的方程组，
即使变量个数等于未知量个数，
其难度相对于变量个数而言也可能高得不成比例。

#digression(title: [题外话：看似简单的方程组也可能很棘手])[
  很容易构造出无法手算求解的方程组。
  但也可能构造出这样的方程组：它们看起来人畜无害，
  _而且_确实能用巧妙的方法手算求解，
  _但_要找到那个巧妙的方法却极其困难。

  这类方程组的一个例子，是在 $RR$ 上求解
  $
  x^3 &= 3 x - 12 y + 50 \
  y^3 &= 12 y + 3 z - 2 \
  z^3 &= 27 z + 27 x.
  $
  它有一种手算解法，但相当难以想到，
  即便是对世界上最优秀的高中生来说也是如此。
  （这道题的来源是
  #link("https://aops.com/community/p1566057")[USA Team Selection Test 2009]。）
]

这意味着你需要暂时摘下厨师帽，
戴上解题帽：
好消息是，这全是高中代数：不涉及微积分，不涉及导数，等等。
坏消息是它很棘手。你得真正动脑筋。

#tip(title: [关于方程组的提示])[
  - 解方程组时，*把它当作一个自成一体的代数谜题*。
    这意味着你不能盲目地套用方法，而必须真正思考。

  - 某些情况下可用的策略：设法分离出一个变量，用其他变量来表示它。
    例如，如果你看到 $x^2 + x + 2 y = 7$，
    一个策略是把它改写成 $y = 1/2 (7 - (x^2 + x))$，
    然后用这个代换消去方程组中所有的 $y$。
    这样做把变量个数减少了 $1$，代价是多花一些功夫。

  - 如果方程组具有对称性，看看能否利用它来省些功夫。

  - 一旦发现可以提取的公因式，就尝试因式分解。
    例如，如果你看到 $x y - x = 0$，把它写成 $x (y-1) = 0$，
    于是要么 $x=0$，要么 $y=1$。

  - 如果你要在等式两边开平方：
    也就是说，如果 $a^2 = b^2$，你得到的结论是 $a = pm b$，而不是 $a = b$。

  - 当你开始得到“或”的陈述时，要小心别漏掉情形。
    在上一个例子中有 $2^2 = 4$ 种情形。
    你很容易想象粗心的学生会不小心漏掉一种情形。

  - 看看能否先“猜出”一些显然的解（例如全为零）。
    如果可以，把它们记下来，这样你就知道它们稍后应该会再次出现。
]

我还需要给出一个警告：一定要小心*除以零*。
例如，在上一节的例子中，
粗心的学生可能会试图除以 $y$ 和 $x$，得到
$
  6 y - 2 x y - y^2 &= 0 ==> 2x + y = 6 \
  6 x - 2 x y - x^2 &= 0 ==> x + 2y = 6.
$
但这是错的，因为 $x$ 和 $y$ 也可能为零！
如果你犯了这个错误，你就只得到了四个临界点中的一个。
这一点足够重要，我要把它框起来：

#warning(title: [警告：当心除以零])[
  *任何时候只要你在等式两边做除法*，
  都要问问自己：你所除的那个表达式是否也可能等于 $0$。
  如果是，那种情形就需要单独处理。
]

接下来我要给出两个例子，每个都含三个变量，
用来展示我刚才在提示中提到的这些想法。
事先提醒：这两个例子是故意弄得稍微棘手一点，好留出空间来展示这些想法。
如果你自己做不出这两个，也不用担心。
考试题里的这步代数大概会温和一些。

#sample[
  计算函数
  $ f(x, y, z) = x^3 + y^3 + z^3 - 3 x y z. $
  的所有临界点。
]

#soln[
  我们先计算梯度：
  $ nabla f = vec(3 x^2 - 3 y z, 3 y^2 - 3 z x, 3 z ^2 - 3 x y). $

  临界点出现在 $nabla f = bf(0)$ 时，
  这给出方程组（在除以 $3$ 之后）：
  $
  x^2 &= y z  \
  y^2 &= z x  \
  z^2 &= x y.
  $
  我们很想把变量约掉，但那样就变成除以零了。
  确实，注意 $(0,0,0)$ 就是一个解！

  - 若 $x = 0$，则由最后一个方程可得 $z = 0$，再由第二个方程得 $y = 0$。
  - 由对称性，若三个变量中_任意一个_为零，则三个都为零。

  现在假设所有变量都非零。
  那么我们可以安全地把第一个方程写成 $ z = x^2 / y$，
  并用它消去第二个方程中的 $z$：
  $ y^2 = (x^2 / y) x => x^3 = y^3. $
  同理可得 $y^3 = z^3$ 和 $z^3 = x^3$。

  所以实际上 $x = y = z$，因为我们可以安全地对实数开立方。
  而且任何满足 $x = y = z$ 的三元组都可行。

  总之，形如 $(t,t,t)$ 的每个点都是临界点 ——
  一个无穷的临界点族！
]

#sample[
  计算函数
  $ f(x, y, z) = z(x-y)(y-z) - 2 x z. $
  的所有临界点。
]
#soln[
  梯度为
  $ nabla f = vec(z(y - z) - 2 z, z(-2y+x+z), y(x-y) -2z(x-y) -2x). $
  这看起来很吓人，但事实证明前两个方程可以因式分解。
  整理之后，我们得到：
  $
    z(y-z-2) &= 0 \
    z(-2y+x+z) &= 0 \
    y(x-y) -2(x-y)z -2x &= 0.
  $
  在第一个方程中，我们按 $z = 0$ 与 $y = z+2$ 分类讨论。

  - 第一种情形：若 $z = 0$，则第一个和第二个方程都成立，且不再提供更多信息。
    于是我们转向最后一个方程，当 $z = 0$ 时它说
    $ y(x-y) - 2x = 0. $
    这是关于 $x$ 的线性方程，我们可以把它分离出来：
    $ (y-2) x - y^2 = 0 ==> (y-2) x = y^2. $
    同样，在除以 $y-2$ 之前，我们先检查各种情形：

    - 若 $y = 2$，我们得到显然的矛盾 $0 = 4$。
    - 所以我们可以假设 $y != 2$，且有 $x = y^2/(y-2)$。

    因此，对_任意_实数 $y != 2$，我们都得到一个临界点
    $ (y^2/(y-2), y, 0). $

  - 现在假设 $z != 0$。
    那么我们可以在前两个方程中安全地除以 $z$，得到
    $ y &= z + 2 \ x &= 2 y - z. $
    现在的策略是把一切都用 $z$ 表示。
    第一个方程告诉我们 $y = z+2$，所以第二个方程给出
    $ x = 2(z+2) - z = z + 4. $
    我们还有一个方程，于是我们把这两个代换代入所有地方并展开：
    $ 0 &= (z+2)((z+4)-(z+2)) -2((z+4)-(z+2))z -2(z+4) \
      &= 2(z+2) - 4z - 2(z+4) = -4z - 4 \
      &==> z = -1. $
    因此，我们又得到一个临界点 $(3, 1, -1)$。

  总之，答案是
  $ (y^2/(y-2), y, 0) " 对每个 " y != 2 "，外加一个额外的点" (3, 1, -1). #qedhere $
]

== [RECIPE] 二元函数的二阶导数判别法 <sec-second-deriv>

前面我们是通过观察附近的点来对临界点分类的。
严格来说，我们并没有给出“附近”的精确定义，
只是用了像 $0.01$ 或 $0.1$ 这样的小数字来说明问题。
所以在 18.02 中，考试会需要一个更系统的定理，用来把
临界点分类为局部最小值、局部最大值或鞍点。

我曾考虑过要不要解释二阶导数判别法为什么成立，
但最终还是决定不把它写进这份讲义。
以下是一些借口：
#digression[
  问题在于，要“正确地”理解这件事，我就必须讲_二次型_（quadratic forms）。
  然而，在这份讲义的前置部分 Alfa 和 Bravo 中，
  我们只讲了线性代数，完全没有在这个语境下涉及二次型。
  我不太愿意为了论证一个之后不再使用的结论，就引入整整一章来介绍二次型
  （它们比线性函数难直观得多），_然后_再把它与 $2 times 2$ 矩阵的特征值联系起来。
  （不过如果你想看的话，Poonen 在他讲义的第 9 节里关于二次型有一些提示。）

  另一个缺点是，即使把二次型讲得完全正确，
  当函数在临界点附近的变化低于二次（例如三次）时，
  二阶导数判别法本来也并非在所有情形下都适用。
  而多元泰勒级数并不在 18.02 的教学大纲内。
]

所以为了尽快把这一节讲完，我直接给出结论。
很抱歉，它看起来会来得很突兀。

#recipe(title: [方法：二阶导数判别法])[
  设 $f(x,y)$ 在 $P$ 处有一个临界点。
  我们想判断它是局部最小值、局部最大值还是鞍点。
  假设 $f$ 在 $P$ 附近有二阶连续导数。

  1. 令 $A = f_(x x) (P)$，$B = f_(x y) (P) = f_(y x) (P)$，$C = f_(y y) (P)$。
    它们是 $f$ 的偏导数的偏导数（是的，很抱歉），
    在 $P$ 处取值。
    如果你更喜欢用梯度，也可以改写成
    $ nabla f_x (P) = vec(A,B), #h(1em) nabla f_y (P) = vec(B,C). $

  2. 若 $A C - B^2 != 0$，则根据下面的表输出答案：
    - 若 $A C - B^2 < 0$，输出“鞍点”。
    - 若 $A C - B^2 > 0$ 且 $A, C > 0$，输出“局部最小值”。
    - 若 $A C - B^2 > 0$ 且 $A, C < 0$，输出“局部最大值”。
  3. 若 $A C - B^2 = 0$，则二阶导数判别法无法给出结论。
    上面任何一种答案都有可能，包括像猴鞍面那样古怪、罕见的鞍点。
    你必须改用其他方法。
]

量 $A C - B^2$ 有时被称为_海森行列式_（Hessian determinant）；
它是矩阵 $mat(A, B; B, C)$ 的行列式。

#tip[
  确实有一个定理：若 $f$ 二阶连续可微，则 $f_(x y) = f_(y x)$。
  也就是说，如果你取一个性质良好的函数 $f$，先对 $x$ 求导
  再对 $y$ 求导，
  得到的结果与先对 $y$ 求导再对 $x$ 求导相同。
  你在文献中会看到它有时被写成
  $ (partial)/(partial x) (partial)/(partial y) f = (partial)/(partial y) (partial)/(partial x) f. $
]

#warning(title: [警告：这仅适用于两个变量])[
  二阶导数判别法只对 $f(x,y)$ 有效。
  在这门课中，$f(x,y,z)$ 没有类似的方法。
]

#sample[
  用二阶导数判别法对函数
  $ f(x,y) = x^3 + x^2 + y^3 - y^2. $
  的临界点 $(0,0)$ 进行分类。
]
#soln[
  先计算偏导数：
  $ nabla f = vec(3x^2 + 2x, 3y^2 - 2y) ==> cases(f_x = 3x^2 + 2x, f_y = 3y^2 - 2y). $
  现在我们再对它们各自求一次偏导。
  取决于你使用的记号，你可以把它写成
  $ nabla f_x = vec(6x+2,0) #h(1em) nabla f_y = vec(0,6y-2) $
  或
  $ f_(x x) = 6x+2, #h(1em) f_(x y) = f_(y x) = 0, #h(1em) f_(y y) = 6y-2. $
  再说一次，那个重复出现的 $f_(x y) = f_(y x)$ 要么是 $(partial) / (partial y) (6x+2) = 0$，
  要么是 $(partial) / (partial x) (6y-2) = 0$；
  对于性质良好的函数，$f_(x y)$ 与 $f_(y x)$ 总是得到相同的结果。

  在原点处，我们得到
  $
  A &= 6 dot 0 + 2 = 2 \
  B &= 0 \
  C &= 6 dot 0 - 2 = -2.
  $
  由于 $A C - B^2 = -4 < 0$，我们输出答案“鞍点”。
]

#sample[
  计算 $f(x,y) = x y + y^2 + 2 y$ 的临界点，
  并用二阶导数判别法对它们进行分类。
]
#soln[
  先计算梯度：
  $ nabla f = vec(y, x + 2 y + 2). $
  解方程组 $y = 0$ 与 $x + 2y + 2 = 0$，只得到 $(x,y) = (-2,0)$。
  因此这是唯一的临界点。

  现在我们计算二阶导数：
  $
  f_(x x) &= (partial)/(partial x) (y) = 0 \
  f_(x y) = f_(y x) &= (partial)/(partial y) (y) = (partial)/(partial x)(x + 2 y + 2) = 1 \
  f_(y y) &= (partial)/(partial y) (x + 2 y + 2) = 2.
  $
  在这个例子中它们都是常函数；
  无论如何，我们有 $A = 0$、$B = 1$、$C = 2$，且 $A C - B^2 = -1 < 0$，
  所以输出“鞍点”。
]

== [EXER] 习题

#exer[
  计算 $f(x,y) = x^3 + 2 y^3 - 6 x y$ 的临界点，并把它们分类
  为局部最小值、局部最大值或鞍点。
] <exer-crit-2var>

#exer[
  计算 $f(x,y,z) = x^2 + y^3 + z^4$ 的临界点，并把它们分类
  为局部最小值、局部最大值或鞍点。
] <exer-crit-3var>

#exerstar[
  是否存在一个可微函数 $f : RR^2 -> RR$，
  使得 $RR^2$ 中的_每个_点都是鞍点？
] <exer-every-saddle>

#exerstar[
  给出一个具有下述性质的可微函数 $f : RR^2 -> RR$ 的例子：
  每个格点 $(x,y)$（即 $x$ 和 $y$ 都是整数的点）
  都是鞍点，且没有其他鞍点。
  例如，$(2, -7)$、$(100, 100)$ 和 $(-42, -13)$ 应当是鞍点，
  但 $(1/2, 0)$、$(pi, -sqrt(2))$ 和 $(sqrt(7), sqrt(11))$ 不应当。
] <exer-lattice-saddle>
