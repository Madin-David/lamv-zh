#import "@local/evan-zh:1.0.0":*

= 平面及其法向量

一般地，$RR^3$ 中平面的方程具有
$ a x + b y + c z = d $
的形式，其中 $a$、$b$、$c$、$d$ 是实数（且 $a$、$b$、$c$ 不全为零）。

== [TEXT] $RR^3$ 中平面的法向量

在 18.01 中，我们研究 $RR^2$ 中的直线，经常使用直线斜率这个概念。
而在 18.02 中，平面没有“斜率”；至少不是单个数。
所以我想传达的是：
#idea[
  我们在 18.01 中用斜率做的每一件事，
  在 18.02 中都应当改用法向量来表述。
]

那是什么呢？
平面的一个*法向量*（normal vector）$bf(v)$ 是这样一个向量：
如果你在平面上任取一点 $P$，那么把 $P$ 与 $P + bf(v)$ 连接起来的箭头
--- 也就是把起点画在 $P$ 处的箭头 $bf(v)$ ---
垂直于该平面。

#figure(
  image("figures/planes-normalvector.svg", width: auto),
  caption: [平面的法向量。],
)

（注意，你选哪个点 $P$ 都无所谓。
你甚至可以完全忽略 $P$，设想把 $bf(v)$ 画成从某个随意的、
未必在平面上的点 --- 比如原点 --- 出发的箭头，
并要求 $bf(v)$ 以直角穿过该平面。）

本章的主要目标是证明下面这个结果：
#memo(title: [要记住：平面的法向量])[
  给定平面 $a x + b y + c z = d$，它的法向量
  是 $vec(a,b,c)$ 的倍数。
]

请记住，法向量只在相差一个倍数的意义下有意义：
若 $vec(1,2,3)$ 是法向量，那么 $vec(10,20,30)$、$vec(-100, -200, -300)$ 等等也都是。

== [TEXT] $RR^2$ 中直线的法向量

在解释为什么这在 $RR^3$ 中成立之前，
我想先在 $RR^2$ 中把一切都做一遍作为对照，
那里图更容易画，而且你有来自八、九年级代数的直觉。

这里有一个问题：$RR^2$ 中哪些向量垂直于 $vec(1,2)$？
它们是位于过原点、斜率为 $-1/2$ 的直线上的那些向量，即
$ 0 = vec(x,y) dot vec(1, 2) <==> 0 = x + 2 y. $
见 @fig-planes-x-2y。

#figure(
  image("figures/planes-x-2y.svg"),
  caption: [$x+2y=0$ 与 $x+2y=pi$ 的图像。],
) <fig-planes-x-2y>

好，那么在这种情况下，直线
$ x + 2 y = pi $
长什么样呢？嗯，它是一条平行直线，斜率仍然相同。

等价地，你也可以把它想成满足
$ vec(x,y) - vec(pi,0) " is perpendicular to " vec(1,2) $
的向量 $vec(x,y)$，
或者对直线上任意一点做同样的事，比如
$ vec(x,y) - vec(0, pi slash 2) " is perpendicular to " vec(1,2) $
甚至
$ vec(x,y) - vec(0.218 pi, 0.391 pi) " is perpendicular to " vec(1,2) $
但那很傻。大多数时候你并不关心基点。
你只关心这条直线的斜率是 $-1/2$，
为此左端只需是 $x + 2y$（甚至 $100 x + 200 y$）。
右端可以是你想要的任何东西。

== [TEXT] 平面的法向量

在 $RR^3$ 中，对于表达式 $a x + b y + c z = d$，情况完全相同。
唯一的区别是“斜率”这个词被禁用了。
不过，即使我们不能谈斜率，我们仍然可以谈平行平面，
于是整段讨论可以原封不动地搬过来。

例如，垂直于 $vec(1,2,3)$ 的向量集合是什么？
这等同于要求
$ 0 = vec(x,y,z) dot vec(1,2,3) = x + 2 y + 3 z. $
所以平面 $x + 2 y + 3 z = 0$ 过原点，且
法向量为 $vec(1,2,3)$。

那么像 $x + 2 y + 3 z = 6$ 这样的呢？
与上一节类似，把它写成不同形式的方式有：

- 把方程改写为 $1 (x-6) + 2 (y-0) + 3 (z-0) = 0$，
  平面就可以看作满足
  $ vec(x,y,z) - vec(6,0,0) " is perpendicular to " vec(1,2,3). $
  的点 $(x,y,z)$。
- 把方程改写为 $1 (x-0) + 2 (y-3) + 3 (z-0) = 0$，
  平面就可以看作满足
  $ vec(x,y,z) - vec(0,3,0) " is perpendicular to " vec(1,2,3). $
  的点 $(x,y,z)$。
- 把方程改写为 $1 (x-0) + 2 (y-0) + 3 (z-2) = 0$，
  平面就可以看作满足
  $ vec(x,y,z) - vec(0,0,2) " is perpendicular to " vec(1,2,3). $
  的点 $(x,y,z)$。
- 把方程改写为 $1 (x-0.753) + 2 (y-0.618) + 3 (z-1.337) = 0$，
  平面就可以看作满足
  $ vec(x,y,z) - vec(0.753, 0.618, 1.337) " is perpendicular to " vec(1,2,3). $
  的点 $(x,y,z)$。

但和上次一样，基点其实无关紧要。
最终结论是一样的：系数通过法向量控制平面的方向
（见 @fig-planes-x-2y-3z-6）。

#figure(
  image("figures/planes-x-2y-3z-6.svg", width: auto),
  caption: [平面 $x + 2 y + 3 z = 6$ 的法向量。],
) <fig-planes-x-2y-3z-6>


== [RECIPE] 求过一点且具有给定方向的平面 <recipe-plane-known-dir>

有时你知道平面朝哪个方向延伸，
并且需要让一个点落在它上面。
这只是意味着你必须选取相应的数 $d$：

#recipe(title: [已知法向量与平面上一点求平面的方法])[
  设给定的法向量是 $vec(a,b,c)$，且 $P in RR^3$ 是给定的点。

  1. 把左端写成 $a x + b y + c z$。
  2. 在 $P$ 处求左端的值，得到一个数 $d$。
  3. 输出 $a x + b y + c z = d$。
]

#sample[
  求与 $x + 2 y + 3 z = 100$ 平行
  且过点 $(1, 4, 9)$ 的平面的方程。
]
#soln[
  两个平面平行当且仅当它们的法向量方向相同，
  所以我们对两者都用同一个法向量 $vec(1,2,3)$。
  因此答案应当具有 $ x + 2 y + 3 z = d $ 的形式，其中 $d$ 待定。
  为了过 $(1, 4, 9)$，我们应取 $d = 1 + 2 dot 4 + 3 dot 9 = 36$。
  所以输出 $ x + 2 y + 3 z = 36$。
]

== [TEXT] 计算点到平面的距离

有一道经典的习题，用来检验对平面法向量
与投影的理解，那就是求点到平面的距离。

#sample[
  求点 $(7, 8, 5)$ 到平面 $x + 2 y + 3 z = 0$ 的距离。
]
#soln[
  平面 $x + 2 y + 3 z = 0$ 有一个法向量 $bf(n)$，
  它由 $x$、$y$、$z$ 的系数给出：
  $ bf(n) = vec(1 , 2 , 3). $

  现在考虑从原点（它在平面上）指向给定点 $(7,8,5)$ 的向量 $bf(v) = vec(7,8,5)$。
  关键的洞察是，$bf(v)$ 在向量 $bf(n)$ 上的标量分量
  恰好等于我们要计算的距离；
  看看图 @fig-planes-789 就知道为什么这是对的。
  要点在于，原点、$bf(v)$ 的终点，
  以及 $bf(v)$ 分别投影到 $bf(n)$ 和平面上的投影点，构成一个矩形。

  #figure(
    image("figures/planes-dist789.svg", width: auto),
    caption: [$bf(v)$ 分别到 $bf(n)$ 和平面（紫色）的两个投影
      构成一个矩形，因此 $bf(v)$ 到平面的距离
      恰好由 $op("comp")_(bf(n)) (bf(v))$ 给出。],
  ) <fig-planes-789>

  计算点积：
  $ bf(v) dot bf(n) = (7) (1) + (8) (2) + (5) (3) = 7 + 16 + 15 = 38 . $
  计算模长：
  $ |bf(n)| = sqrt((1)^2 + (2)^2 + (3)^2) = sqrt(1 + 4 + 9) = sqrt(14) . $
  因此，标量分量为：
  $ op("comp")_(bf(n)) bf(v) = frac(bf(v) dot bf(n), |bf(n)|) = 38 / sqrt(14) . $
  这就是答案。
]

下面还是同一道习题，只改一处：我们把方程改成 $x + 2 y + 3 z = 60$。
这意味着我们必须选取平面上原点以外的一个点。
#sample[
  求点 $(7, 8, 5)$ 到平面 $x + 2 y + 3 z = 60$ 的距离。
]
#soln[
  和之前一样，平面 $x + 2 y + 3 z = 60$ 有一个法向量 $bf(n)$，
  它由 $x$、$y$、$z$ 的系数给出：
  $ bf(n) = chevron.l 1 , 2 , 3 chevron.r . $

  这次我们不能再用原点 $(0,0,0)$，
  但可以选取平面上任何其他点；我们就选 $(0,0,20)$。
  （如果你愿意，也可以用 $(60,0,0)$ 或 $(0,30,0)$，甚至 $(-77,13,37)$ 来做这道题；
  它们给出的答案都一样。）

  从 $(0 , 0 , 20)$ 指向 $(7 , 8 , 5)$ 的向量 $bf(v)$ 是：
  $ bf(v) = chevron.l 7 - 0 , thin 8 - 0 , thin 5 - 20 chevron.r = chevron.l 7 , thin 8 , thin - 15 chevron.r . $

  现在我们只需重复之前的步骤，其中
  $ bf(v) dot bf(n) &= (7) (1) + (8) (2) + (- 15) (3) = - 22 \
    |bf(n)| &= sqrt((1)^2 + (2)^2 + (3)^2) = sqrt(1 + 4 + 9) = sqrt(14) . $
  因此
  $ op("comp")_(bf(n)) bf(v) = frac(bf(v) dot bf(n), |bf(n)|) = frac(- 22, sqrt(14)) . $
  距离是绝对值 $22 / sqrt(14)$。
]
#remark[
  标量分量得到负数并不要紧。
  这对应于点 $(7,8,5)$ 被夹在
  两个平面 $x+2y+3z=0$ 与 $x+2y+3z=60$ 之间这一事实。
  取决于你选择把 $bf(n)$ 指向哪一边，
  两个分量中会有一个为正、另一个为负。
  见 @fig-planes-sandwich。
]
#figure(
  image("figures/planes-sandwich.svg", width: auto),
  caption: [点 $(7,8,5)$ 被夹在平面
    $x+2y+3z=0$ 与 $x+2y+3z=60$ 之间的二维示意图。
    若我们取 $bf(n) = chevron.l 1,2,3 chevron.r$，
    则如紫色所示，标量分量分别为 $+38/sqrt(14)$ 与 $-22/sqrt(14)$。
  ],
) <fig-planes-sandwich>

这道题的特点是，你可以用符号代替数字来做，
从而得到一个一般公式；这意味着反复用具体数字去做
这件事多少是白费力气。我们干脆一次性把它们全做了。

#sample[
  求点 $(x_0 , y_0 , z_0)$ 到由方程
  $ a x + b y + c z = d. $
  定义的平面的距离。
  答案用 $a$、$b$、$c$、$d$、$x_0$、$y_0$、$z_0$ 表示。
]
#soln[
  平面的法向量 $bf(n)$ 由平面方程中
  $x$、$y$、$z$ 的系数给出：
  $ bf(n) = chevron.l a , b , c chevron.r . $

  现在我们选取平面上任意一个基点 $(x_1 , y_1 , z_1)$，它落在平面 $cal(P)$ 上。
  我们来做 $c != 0$ 的情形并使用
  $ (x_1 , y_1 , z_1) = (0 , 0 , d / c) $
  但另外两种情形 $a != 0$ 与 $b != 0$ 的做法完全相同。
  （事实上，你也并不真的需要选取基点，
  它只是让接下来的计算在思考上稍微容易一些。）

  从 $(x_1 , y_1 , z_1)$ 指向 $(x_0 , y_0 , z_0)$ 的向量 $bf(v)$ 是：
  $ bf(v) = chevron.l x_0 - x_1 , y_0 - y_1 , z_0 - z_1 chevron.r
    = lr(chevron.l x_0 , y_0 , z_0 - d / c chevron.r) . $

  现在，$bf(v)$ 沿 $bf(n)$ 的标量分量由下式给出
  $ op("comp")_(bf(n)) bf(v) = frac(bf(v) dot bf(n), |bf(n)|) . $
  我们来计算它。
  计算点积 $bf(v) dot bf(n)$：
  $ bf(v) dot bf(n) = a x_0 + b y_0 + c (z_0 - d / c) = a x_0 + b y_0 + c z_0 - d . $
  计算 $bf(n)$ 的模长：
  $ |bf(n)| = sqrt(a^2 + b^2 + c^2) . $
  因此，标量分量为：
  $ op("comp")_(bf(n)) bf(v) = frac(a x_0 + b y_0 + c z_0 - d, sqrt(a^2 + b^2 + c^2)) . $

  最后，点到平面的距离是标量分量的绝对值：
  $ lr(|op("comp")_(bf(n)) bf(v)|) = lr(|frac(a x_0 + b y_0 + c z_0 - d, sqrt(a^2 + b^2 + c^2))|) . #qedhere $
]

== [RECIPE] 到平面的距离 <sec-distance-plane>

如果你只想记住最终结果，它就在这里：

#recipe(title: [点到平面距离的方法])[
  如果要求点 $(x_0 , y_0 , z_0)$ 到由方程
  $a x + b y + c z = d$ 定义的平面的距离，输出答案
  $ frac(lr(|a x_0 + b y_0 + c z_0 - d|), sqrt(a^2 + b^2 + c^2)) . $
]
（我们把绝对值移到了分子上，因为平方根总是正的。）


== [EXER] 习题

#exer[
  $RR^3$ 中某处画了一个立方体（它的面不平行于坐标轴）。
  立方体有两个面分别包含在平面
  $x + 2 y + 3 z = 4$ 与 $5 x + 6 y + k z = 7$ 中，
  其中 $k$ 是某个实数。
  根据这些信息，求 $k$。
] <exer-planes-cube>

#exer[
  某点 $P$ 到平面 $3x+4y+12z=-1$ 的距离是 $42$。
  $P$ 到平面 $3x+4y+12z=1000$ 的距离可能是多少？
] <exer-planes-sandwich>
