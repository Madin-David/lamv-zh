#import "@local/evan-zh:1.0.0":*

= 最优化问题 <ch-opt>

既然我们已经理解了 $f$ 的临界点（critical point）与区域 $cal(R)$，
接下来我们把注意力转向求全局最小值与最大值的问题。

== [TEXT] 简单情形与困难情形

设你有一个函数 $f : RR^n -> RR$，能够计算它的 $nabla f$，
以及一个区域 $cal(R)$。
我们将区分两种情形：

- *简单情形* 是指 $cal(R)$ 的维数也为 $n$。
  经验法则说此时应当有零个 "=" 约束（constraint）。
- *困难情形* 是指 $cal(R)$ 的维数为 $n-1$。
  经验法则说此时应当有一个 "=" 约束。
  在困难情形中，我们将使用 *拉格朗日乘子*（Lagrange multipliers）。

在 18.02 中我们不会涉及 $cal(R)$ 的维数为 $n-2$ 或更小的情形
（即两个或更多约束），尽管这是可以做到的。

== [RECIPE] 简单情形 <sec-recipe-opt-easy>

#recipe(title: [不使用拉格朗日乘子的最优化方法])[
  设你想求 $f : RR^n -> RR$ 在区域 $cal(R)$ 上的最优值，
  且 $cal(R)$ 的维数为 $n$。

  0. 找出区域 $cal(R)$ 的边界（boundary）与极限情形（limit case）。
    （这一步不需要看 $f$。）
  1. 计算 $f$ 在区域 $cal(R)$ 中 $f$ 的所有 *临界点* 处的值。
  2. 计算 $f$ 在区域 $cal(R)$ 中 $f$ 的所有 *边界点* 处的值。
  3. 计算 $f$ 在区域 $cal(R)$ 中 $f$ 的所有 *极限情形* 处的值。
  4. 输出前面各步中给出最优值的点，
    或者断言最优值不存在（如果第 3 步中的点比第 1–2 步的更好）。

  如果存在使 $nabla f$ 无定义的点，你也应当检查这些点。
  不过，对于 18.02 中出现的例子，这种情况似乎相当罕见。
]

#warning(title: [警告：不要低估边界！])[
  在 18.01 中，你大概只会在区间 $I = [a,b]$ 上做最优化，
  此时边界只是两个输入 $a$ 和 $b$。
  在 18.02 中，情况完全不同：
  边界（如果非空）往往有 _无穷多个_ 点。
  所以要处理边界情形可能相当费力！
  不要低估第 2 步可能带来的复杂程度。

  特别地，第 2 步甚至可能要求你使用拉格朗日乘子，
  也就是说，简单情形中的某一步本身就是一整个困难情形。
  正因如此，"简单情形" 与 "困难情形" 这两个名字有点名不副实。
]

我们先从一个没有边界的例子开始，其中的极限情形很容易考察，
所以第 2 步和第 3 步基本上不成问题。
后面我们会做更多第 2 步和第 3 步更为复杂的例子。

#sample[
  求
  $ f(x,y) = x + y + 8/(x y) $
  在 $x,y > 0$ 上的最小可能值与最大可能值（如果存在）。
]
#soln[
  区域 $cal(R)$ 是第一象限，
  它确实是二维的（没有 $=$ 约束），
  所以这属于简单情形，前述方法在此适用。
  我们依次检查所有点：

  0. $cal(R)$ 没有边界，而当任何变量趋于 $0^+$ 或 $+oo$ 时存在极限情形。
  1. 为求临界点，计算梯度
    $ nabla f (x,y) = vec(1 - 8 / (x^2 y), 1 - 8 / (x y^2)) $
    然后令它等于 $vec(0,0)$。
    这给我们方程组
    $ 1 = 8 / (x^2 y) = 8 / (x y^2). $
    由此可得 $x^2 y = x y^2$，即 $x = y$
    （在 $cal(R)$ 中有 $x,y > 0$，所以不必担心除以零）
    因此唯一的临界点是 $(x,y) = (2,2)$。

  2. 区域 $cal(R)$ 没有边界，所以没有边界点需要检查。
  3. 区域 $cal(R)$ 有四类不同的极限情形：

    - $x -> 0^+$
    - $y -> 0^+$
    - $x -> +oo$
    - $y -> +oo$.

    事实上这四种情形都会使 $f -> +oo$。
    在前两种情形中，$f$ 中的项 $8 / (x y)$ 都会使 $f -> +oo$。
    在 $x -> oo$ 的情形中，项 $x$ 使 $f -> +oo$。
    在 $y -> oo$ 的情形中，项 $y$ 使 $f -> +oo$。

  综合以上：

  - 全局最小值在 $(2,2)$ 处取得，此时 $f(2,2) = 6$。
  - 不存在全局最大值，因为我们已经看到有使 $f -> +oo$ 的极限情形。 #qedhere
]

== [TEXT] 拉格朗日乘子

设 $f : RR^n -> RR$ 是我们要在某个区域 $cal(R)$ 上做最优化的函数。
现在我们转向 $cal(R)$ 的维数为 $n-1$ 的情形，
这是由于形如 $g(x,y) = c$ 或 $g(x,y,z) = c$ 的单个约束。

我们需要一个关于临界点的新定义。
为了引出它，我们先看 @fig-lm 中的一个具体例子。
这里 $n = 2$，且

- $f(x,y) = x^2 + y^2$，且
- $g(x,y) = c$ 是下图所示的蓝色等值线（level curve）；
- $cal(R)$ 就是这条等值线 $g(x,y) = c$（没有更多 $<$ 或 $<=$ 约束）。

在这张图中，在约束 $g(x,y) = c$ 下尝试最优化 $f$，等同于
在这条等值线上找出离原点最远与最近的点。
我在图中把这两个点标为 $P$ 和 $Q$。
理解如何求出它们的关键在于：_同时_ 画出 $f$ 经过 $P$ 和 $Q$ 的等值线；
于是我们观察到，取得这些最小值与最大值的 $f$ 的等值线，
应当在 $P$ 和 $Q$ 处与 $g(x,y) = c$ 相切。

#figure(
  image("figures/minmax-lm.svg", width: auto),
  caption: [一个 LM 型最优化问题的例子，其中要在 $g(x,y) = c$ 上
  找出使 $f$ 最优的点],
) <fig-lm>

那么，如何检验是否相切？
答案：看梯度！
我们期望在点 $P$ 和 $Q$ 处，$nabla f$ 与 $nabla g$ 指向同一方向。
这就给出了策略：寻找 $nabla f$ 与 $nabla g$ 指向相同的点。

我认为下面的术语并不是一个正式名称，但我很喜欢它，就用它了：

#definition[
  *LM 临界点* 是指曲线/曲面 $g(P) = c$ 上满足以下 _二者之一_ 的点 $P$：

  - 对某个标量 $lambda$ 有 $nabla f(P) = lambda nabla g(P)$；或者
  - $nabla g(P) = bf(0)$。
]

注意有 _两种_ 可能的情形。
如果你愿意，也可以把要求看成 $nabla f (P)$ 与 $nabla g (P)$
_线性相关_，这样就只是一条了。
然而在实践中，人们最终通常还是会像这样分成各种情形。

#digression[
  参数 $lambda$ 正是 "Lagrange multipliers" 这个名字的由来；
  它是作用在 $nabla g$ 上的一个标量乘子。
  就我个人而言，我觉得这个名字没太大道理。

  另外，有些资料会以下面这种
  （在我看来更令人困惑的）方式定义 LM 临界点。
  设 $f(x,y,z)$ 是一个三变量函数。
  定义一个 _四变量_ 的 "Lagrangian function"
  $L(x,y,z,lambda) = f(x,y,z) - lambda g(x,y,z)$。
  那么 LM 临界点就是满足 $nabla g = bf(0)$
  或 $nabla L = bf(0)$ 的点，即 $L$ 的普通临界点。
  可以验证这与原来的定义等价，
  但我个人觉得这很不自然。
  不过，如果你喜欢这个定义，尽管用它。
]


既然我们已经有了这些，就可以描述 "困难" 情形的方法了。
唯一的改动是把旧的临界点定义（即 $nabla f (P) = bf(0)$）
换成 LM 临界点的定义。

== [RECIPE] 拉格朗日乘子 <sec-recipe-lm>

#recipe(title: [拉格朗日乘子的方法])[
  设你想求 $f : RR^n -> RR$ 在区域 $cal(R)$ 上的最优值，
  且由于单个约束 $g = c$（其中 $g : RR^n -> RR$），$cal(R)$ 的维数为 $n-1$。

  0. 找出区域 $cal(R)$ 的边界与极限情形。
    （这一步不需要看 $f$。）
  1. 计算 $f$ 在区域 $cal(R)$ 中 $f$ 的所有 *LM 临界点* 处的值。
  2. 计算 $f$ 在区域 $cal(R)$ 中 $f$ 的所有 *边界点* 处的值。
  3. 计算 $f$ 在区域 $cal(R)$ 中 $f$ 的所有 *极限情形* 处的值。
  4. 输出前面各步中给出最优值的点，
    或者断言最优值不存在（如果第 3 步中的点比第 1–2 步的更好）。

  如果存在使 $nabla f$ 或 $nabla g$ 无定义的点，
  你也应当检查这些点。
  不过，对于 18.02 中出现的例子，这种情况似乎相当罕见。
]

同样，这与 @sec-recipe-opt-easy 中的方法相同，只是我们把 "critical point"
改成了 "LM-critical point"。

#tip[
  还记得求临界点可能导出需要相当代数技巧才能解出的方程组吗？
  拉格朗日乘子也是如此，而且更甚，
  因为多了一个你必须在意的参数 $lambda$。
  所以它被称为 "困难情形"，并不是因为所需的 18.02 思想有什么不同，
  而是因为在寻找 LM 临界点时，代数运算可能变得相当复杂。

  事实上，在高中数学竞赛中，代数运算有时会变得极其丑陋，
  以至于拉格朗日乘子法有时被戏称为 "Lagrange _murderpliers_"，
  以反映某些问题所需的惊人计算量。
]

#sample[
  求
  $ f(x,y,z) = x + y + z $
  在满足条件 $x y z = 8$ 的 $x,y,z > 0$ 上的最小可能值与最大可能值（如果存在）。
]
#soln[
  我们按方法来一遍。

  0. 区域 $cal(R)$ 是二维的，由严格不等式
    $x, y, z > 0$ 以及条件 $g(x,y,z) = x y z = 8$ 组成。
    所以它没有边界，但当任何变量趋于 $0^+$ 或 $+oo$ 时存在极限情形。
  1. 为求 LM 临界点，我们需要同时计算 $nabla f$ 与 $nabla g$。
    我们这样做：
    $ nabla f (x,y,z) &= vec(1,1,1) \ nabla g &= (y z, z x, x y). $
    现在，区域 $cal(R)$ 中没有使 $nabla g = vec(0,0,0)$ 的点，
    因为在 $cal(R)$ 中所有变量都被约束为正数。
    于是我们现在解方程组
    $
      1 &= lambda y z \
      1 &= lambda z x \
      1 &= lambda x y
    $
    看看它取什么值。

    解这个方程组的技巧是把前两式相除，
    以消去我们其实并不真正关心的参数 $lambda$，得到
    $ 1 / 1 = (lambda y z)/(lambda z x) = y / x. $
    所以必须有 $x = y$。
    同理，我们得到 $y = z$ 和 $z = x$。

    因此 LM 临界点必须有 $x = y = z$。
    由于 $x y z = 8$，由此可得唯一的 LM 临界点是 $(2,2,2)$。
    在此处求值 $f$ 得到 $f(2,2,2) = 6$。

  2. 区域 $cal(R)$ 没有边界，因为不存在 $<=$ 或 $>=$ 约束。
  3. 区域 $cal(R)$ 在变量 $x,y,z$ 中任何一个
    趋于 $0$ 或 $+oo$ 时存在极限情形。
    然而，请记住 $x y z = 8$。
    所以如果有变量趋于 $0^+$，必有另一个变量变得很大。
    因此，在每一种极限情形中，我们都会发现 $f -> +oo$。

  把所有结果汇总起来：

  - 唯一的全局最小值在 $(2,2,2)$ 处取得，此时 $f(2,2,2) = 6$。
  - 不存在全局最大值。 #qedhere
]

#remark[
  如果你足够细心，可能会意识到我们刚做的这道例题
  只是 @sec-recipe-opt-easy 中例子的一个简单改写。
  这说明了一件事：有时你可以把一个 $3$
  变量的困难情形最优化问题改写成 $2$ 变量的简单情形问题。
]

== [TEXT] 解方程组的建议（再述）

在 @sec-system-advice 中，当我们求所有临界点时，
我提醒过你要小心除以零、要仔细处理各种情形，
并给了你一些解方程组的建议。
求 _LM 临界点_ 所满足的方程组是类似的情况：方程组很难，
你必须把它当成一道自成一体的高中代数谜题来对待。

对于由拉格朗日乘子产生的方程组，
有一个新特点：变量 $lambda$ 的取值从未被用到，
但它出现在除约束 $g = c$ 之外的每一个方程中。
所以这里有两条专门针对拉格朗日乘子方程组的提示：

#tip(title: [提示：关于拉格朗日乘子方程组的建议])[
  除 @sec-system-advice 中的提示外，
  下面还有一些有时管用的策略：

  - 如果容易做到，尽快消去 $lambda$ 可能是合理的。
    毕竟我们其实并不关心 $lambda$ 是多少。
  - 或者，你可以试着消去 _除_ $lambda$ 以外的每一个变量！
    当每个方程只涉及一个非 $lambda$ 的变量时，这很常用。
    也就是说，用 $lambda$ 表示 $x$ 并解出；对 $y$ 和 $z$ 也同样处理。
    然后把这些代入原来的约束方程解出 $lambda$，
    进而得到 $(x,y,z)$。

  注意第二条建议与第一条建议正好相反！
  再次强调，方程组不能靠盲目套用方法来解决。
  你应当使用你认为对给定问题合理的任何方法。
  采用某种做法并不需要任何人的许可。
]

#warning(title: [警告：务必只除以非零的东西！])[
  记住：小心除以零！
  例如，如果你得到方程 $2 lambda y = 10 y$，
  那么它意味着要么 $lambda = 5$，要么 $y = 0$。

  在 2024 年的期中考试中，一道标准拉格朗日乘子题目的某一步
  就出现了这样的方程。
  拿到这个方程的学生中，大约 50%–80% 会漏掉两种情形中的一种
  （具体忘掉哪一种因人而异）。
  别让自己也这样！每当你试图约分时，都要检查除以零！
]

下面这个例子中，一个好主意是尽快消去 $lambda$：

#sample[
  使用拉格朗日乘子求
  $x^2 + x y + y^2 + y$ 在约束 $x + 2 y = 3$ 下的最小可能值。
]
#soln[
  我们要最小化函数 $f (x , y) = x^2 + x y + y^2 + y$，
  约束为 $g(x , y) = x + 2 y = 3$。

  0. 约束区域没有边界，但沿直线 $x + 2y = 3$
    当 $x -> +oo$ 且 $y -> -oo$，以及 $x -> -oo$ 且 $y -> +oo$ 时存在极限情形。

  1. 计算梯度得到
    $ nabla f &= vec(2 x + y , x + 2 y + 1) \
      nabla g &= vec(1 , 2). $
    注意 $nabla g$ 恒不为 $bf(0)$。
    因此，拉格朗日乘子方程变为：
    $ vec(2 x + y , x + 2 y + 1) = lambda vec(1 , 2) . $
    这给出如下方程组：
    $ 2 x + y &= lambda \
      x + 2 y + 1 &= 2 lambda . $
    把这两个方程结合起来，就得到消去 $lambda$ 的简便方法：
    $ x + 2y + 1 = 2 dot underbrace((2 x + y), = lambda) = 4x + 2y. $
    两边消去 $2 y$ 就得到 $x$：$ x + 1 = 4 x ==> x = 1/3 $

    现在把 $x = 1 / 3$ 代入约束 $x + 2 y = 3$ 得到：
    $ 2 y = 3 - 1 / 3 = 8 / 3 ==> y = 4 / 3. $

    换言之，唯一的 LM 临界点是 $(1/3, 4/3)$，在该点我们有
    $ f(1/3, 4/3) = (1 / 3)^2 + (1 / 3) (4 / 3) + (4 / 3)^2 + 4 / 3 =
       1 / 9 + 4 / 9 + 16 / 9 + 4 / 3 = 11/3. $

  2. 没有边界需要检查。

  3. 如果任何变量趋于 $+oo$（从而另一个趋于 $-oo$），
    $f$ 的值也会变得很大。#footnote[这其实有点难以看出，
      因为在这个极限情形中
      你有两个正项 $x^2$ 和 $y^2$ 以及一个负项 $x y$。
      一个办法是当 $x$ 和 $y$ 很大时写成
      $ f(x,y) approx 3/4  x^2 + (x / 2 + y)^2 = 3/4 y^2 + (x + y / 2)^2 $。
      第一个表达式表明如果 $x$ 很大，那么 $f$ 也很大；
      第二个表达式表明如果 $y$ 很大，那么 $f$ 也很大。
    ]

  总之，全局最小值是 $f(1/3, 4/3) = 11/3$。
]

下面这个例子中，我们消去 _除_ $lambda$ 以外的每一个变量：

#sample[
  使用拉格朗日乘子求
  $x^2 + y^2 + z^2 + y - z$ 在约束 $x + 2 y + 3 z = 4$ 下的最小可能值。
]

#soln[
  我们要最小化函数
  $f (x , y , z) = x^2 + y^2 + z^2 + y - z$，约束为
  $ g (x , y , z) = x + 2 y + 3 z = 4$。

  0. $g = 4$ 的区域是一个平面，它没有边界，但
    当任何变量变为 $pm oo$ 时存在极限情形。

  1. 我们来求所有 LM 临界点。
    首先计算所有梯度：
      $ nabla f &= vec(2 x , 2 y + 1 , 2 z - 1) \
      nabla g &= vec(1 , 2 , 3) . $
    注意梯度 $nabla g$ 恒不为 $bf(0)$。
    因此，拉格朗日乘子方程变为：
    $ vec(2 x , 2 y + 1 , 2 z - 1) = lambda vec(1 , 2 , 3)  $
    或者
    $ 2 x &= lambda, \ 2 y + 1 &= 2 lambda , \ 2 z - 1 &= 3 lambda . $

    我们用 $lambda$ 解出各个变量，从而消去除 $lambda$ 以外的每一个变量：
    $ 2x = lambda &==> x = lambda / 2 \
      2 y + 1 = 2 lambda &==> y = lambda - 1 / 2 \
      2 z - 1 = 3 lambda &==> z = (3 lambda + 1) / 2. $

    现在把 $x = lambda / 2$、$y = lambda - 1 / 2$ 和
    $z = (3 lambda + 1) / 2$ 代入约束 $x + 2 y + 3 z = 4$：
    $ lambda / 2 + 2 (lambda - 1 / 2) + 3 (frac(3 lambda + 1, 2)) &= 4 \
      ==> lambda / 2 + 2 lambda - 1 + frac(9 lambda + 3, 2) &= 4 \
      ==> frac(10 lambda + 3, 2) + 2 lambda - 1 &= 4 \
      ==> 10 lambda + 3 + 4 lambda - 2 &= 8 \
      ==> 14 lambda + 1 &= 8 \
      ==> lambda &= 1/2. $

    现在我们有了 $lambda$，代回去求出 $(x,y,z)$：
    $ x = lambda / 2 = 1 / 4 , $
    $ y = lambda - 1 / 2 = 1 / 2 - 1 / 2 = 0 , $
    $ z = frac(3 lambda + 1, 2) = frac(3 (1 / 2) + 1, 2) = frac(3 / 2 + 1, 2) = 5 / 4 . $

    这给出单个 LM 临界点 $(x,y,z) = (1/4, 0, 5/4)$，
    在此处 $f(1/4, 0, 5/4) = 3/8$。

  2. 没有边界情形需要考虑。
  3. 极限情形是其中一个变量趋于 $pm oo$。
    然而在这种情况下 $f$ 显然会变得很大，
    所以这种情况不存在最小值。

  总之，全局最小值是 $f(1/4, 0, 5/4) = 3/8$。
]

#digression(title: [题外话：给细心学生的巧妙几何解法])[
  如果你能看出如何把上一题改写成一道几何题，
  我们就在这里展示一种跳过所有微积分步骤的方法。
  这个做法非常巧妙，你甚至不需要求出 $(1/4, 0, 5/4)$；
  它会直接告诉你最小值。
  除非你确实清楚自己在做什么，否则不要在考试中尝试这一招。

  令 $P := (x,y,z)$ 是一个点。用 $cal(H)$ 表示平面 $x+2y+3z=4$。
  神奇之处在于把式子改写为
  $ f = x^2 + y^2 + z^2 + y - z = x^2 + (y+1/2)^2 + (z-1/2)^2 - 1/2 = P Q^2 - 1/2 $
  其中 $Q := (0, -1/2, 1/2)$。
  换言之，$f(P)$ 是 $P$ 到 $Q$ 距离平方的一个平移。
  所以实际上，我们求得的全局最小值点 $(1/4, 0, 5/4)$
  正是平面 $cal(H)$ 上离 $Q = (0, -1/2, 1/2)$ 最近的点。

  但如果你关心的只是从 $Q = (0, -1/2, 1/2)$ 到 $cal(H)$ 的距离，
  那么用微积分就是杀鸡用牛刀：改用 @sec-distance-plane 中早已给出的方法：
  $ min (P Q) = (|1 dot 0 + 2 dot -1/2 + 3 dot 1/2 - 4|)/(sqrt(1^2+2^2+3^2))
    = 7/(2sqrt(14)). $
  所以最小值是
  $ min(f) = min(P Q)^2 - 1/2 = (7/(2sqrt(14)))^2 - 1/2 = 3/8. $
  因此，一个极其聪明的学生本可以把整道题
  转化为一个几何问题，从而绕开它。
  别担心，在 18.02 中不会要求你想出这样的办法。
]

在此基础上再加码，下面是一个需要考虑大量情形的拉格朗日乘子例子。
由于所需的算术量，这个计算对 18.02 来说大概过于冗长；
它放在这里只是为了举例说明。

#sample[
  求 $x^3 + 3y^3 + 4z^3$ 在 $x^4 + y^4 + z^4 = 2$ 下的最小可能值与最大可能值。
]

#soln[
  该区域既没有边界也没有极限情形。
  所以，我们只需专注于计算 LM 临界点。

  $f(x,y,z) = x^3+3y^3+4z^3$ 与 $g(x,y,z) = x^4+y^4+z^4$ 的梯度为
  $ nabla f &= vec(3x^2, 9y^2, 12z^2) \
    nabla g &= vec(4x^3, 4y^3, 4z^3). $
  我们先把每个方程化简：

  - $3 x^2 - 4 lambda x^3 = 0$ 化为：$x^2 (3 - 4 lambda x) = 0$。
    这给出两种可能：
    - $x = 0$，或者
    - $lambda = (3) / (4 x)$（假设 $x != 0$）。
  - $9 y^2 - 4 lambda y^3 = 0$ 化为：$y^2 (9 - 4 lambda y) = 0$。
    这给出两种可能：
    - $y = 0$，或者
    - $lambda = (9) / (4 y)$（假设 $y != 0$）。
  - $12 z^2 - 4 lambda z^3 = 0$ 化为：$z^2 (12 - 4 lambda z) = 0$。
    这给出两种可能：
    - $z = 0$，或者
    - $lambda = 3 / z$（假设 $z != 0$）。

  这样总共有八种情形！
  我们逐一考察它们。

  / 情形 1：$x = 0$，$y = 0$，$z != 0$:
    由约束 $z^4 = 2$，我们得到：$ z = pm root(4, 2) . $
    于是 $lambda = 3 / z = pm 3 / root(4, 2)$。
  / 情形 2：$x = 0$，$y != 0$，$z = 0$:
    由约束 $y^4 = 2$，我们得到：$ y = pm root(4, 2) . $
    于是 $lambda = (9) / (4 y) = pm frac(9, 4 root(4, 2))$。
  / 情形 3：$x != 0$，$y = 0$，$z = 0$:
    由约束 $x^4 = 2$，我们得到：$ x = pm root(4, 2) . $
    于是 $lambda = (3) / (4 x) = pm frac(3, 4 root(4, 2))$。
  / 情形 4：$x = 0$，$y != 0$，$z != 0$:
    由约束 $y^4 + z^4 = 2$，我们利用
    $lambda = (9) / (4 y)$ 与 $lambda = 3 / z$ 求解。令二者相等，我们得到：
    $ (9) / (4 y) = 3 / z ==> z = 4 / 3 y . $
    代入约束：
    $ y^4 + (4 / 3 y)^4 = 2 ==> y^4 + 256 / 81 y^4 = 2 . $
    这化简为：
    $ 337 / 81 y^4 = 2 ==> y^4 = 162 / 337 ==> y = pm root(4, 162 / 337) . $
    于是 $z = pm 4 / 3 root(4, 162 / 337)$。
  / 情形 5：$x != 0$，$y = 0$，$z != 0$:
    由约束 $x^4 + z^4 = 2$，我们利用
    $lambda = (3) / (4 x)$ 与 $lambda = 3 / z$ 求解。令二者相等，我们得到：
    $ (3) / (4 x) = 3 / z ==> z = 4 x . $ 代入
    约束：
    $ x^4 + (4 x)^4 = 2 ==> x^4 + 256 x^4 = 2 ==> 257 x^4 = 2 . $
    于是 $x^4 = 2 / 257$，并且：
    $ x = pm root(4, 2 / 257) , quad z = pm 4 root(4, 2 / 257) . $
  / 情形 6：$x != 0$，$y != 0$，$z = 0$:
    由约束 $x^4 + y^4 = 2$，我们利用
    $lambda = (3) / (4 x)$ 与 $lambda = (9) / (4 y)$ 求解。令二者相等，我们
    得到：$ (3) / (4 x) = (9) / (4 y) ==> y = 3 x . $
    代入约束：
    $ x^4 + (3 x)^4 = 2 ==> x^4 + 81 x^4 = 2 ==> 82 x^4 = 2 . $
    于是 $x^4 = 1 / 41$，并且：
    $ x = pm root(4, 1 / 41) , quad y = pm 3 root(4, 1 / 41) . $
  / 情形 7：$x != 0$，$y != 0$，$z != 0$:
    令 $lambda$ 的三个表达式相等：
    $ (3) / (4 x) = (9) / (4 y) = 3 / z . $ 由
    $(3) / (4 x) = (9) / (4 y)$，我们得到 $y = 3 x$；由
    $(3) / (4 x) = 3 / z$，我们得到 $z = 4 x$。代入
    约束：
    $ x^4 + (3 x)^4 + (4 x)^4 = 2 ==> x^4 + 81 x^4 + 256 x^4 = 2 ==> 338 x^4 = 2 . $
    于是 $x^4 = 2 / 338$，并且：
    $ x = pm root(4, 1 / 169) , quad y = pm 3 root(4, 1 / 169) , quad z = pm 4 root(4, 1 / 169) . $
    由于 $169 = 13^2$ 是一个平方数，这可以更简单地写成
    $ x = pm 1 / sqrt(13) , quad y = pm 3 / sqrt(13) , quad z = pm 4 / sqrt(13) . $
  / 情形 8：$x = 0$，$y = 0$，$z = 0$:
    这不能给出有效的解，因为它不在约束 $x^4+y^4+z^4=2$ 上。

  因此，总共有高达 $14$ 个 LM 临界点。
  它们分别是：
  - $x = 0$，$y = 0$，$z = pm root(4, 2)$，
  - $x = 0$，$y = pm root(4, 2)$，$z = 0$，
  - $x = pm root(4, 2)$，$y = 0$，$z = 0$，
  - $x = 0$，$y = pm root(4, 162 / 337)$，$z = pm 4 / 3 root(4, 162 / 337)$。
  - $x = pm root(4, 2 / 257)$，$z = pm 4 root(4, 2 / 257)$，$y = 0$，
  - $x = pm root(4, 1 / 41)$，$y = pm 3 root(4, 1 / 41)$，$z = 0$，
  - $x = pm 1 / sqrt(13)$，$y = pm 3 / sqrt(13)$，$z = pm 4 / sqrt(13)$。

  在寻找最大值时，我们应当对 $pm$ 一律取 $+$，以使 $f(x,y,z)$ 最大；
  同样地，求最小值时只对 $pm$ 取 $-$。
  还要注意，全部代 $-$ 得到的结果是全部代 $+$ 的相反数。
  于是这把我们 $14$ 种情形减少到只有 $7$ 种。
  如果我们真的把这七种都试一遍，会发现最后一种就是最优的；
  也就是说，最大值与最小值分别是
  $ f(1/sqrt(13), 3/sqrt(13), 4/sqrt(13)) &= 2sqrt(13) \
    f(-1/sqrt(13), -3/sqrt(13), -4/sqrt(13)) &= -2sqrt(13). #qedhere $
]

== [TEXT] 带有 "常识性" 边界的简单情形示例

正如我们前面提到的，所谓 "简单情形" 的边界可能有
无穷多个点，所以你不能只是把它们逐个代进去查看。
在某些情况下，仅凭观察仍然是可行的，
因为函数在边界上很容易描述。
下面我们给出这样一个例子。

#sample[
  求
  $ f(x,y) = 2 x^2 + y^2 + x y - 6 y $
  在第一象限 $x,y >= 0$ 中的最小可能值。
]
#soln[
  按照方法来做：

  0. 第一象限在 $x -> +oo$ 或 $y -> +oo$ 时存在极限情形。
    边界由两条射线组成：正 $x$ 轴（从 $(0,0)$ 向正东）
    与正 $y$ 轴（从 $(0,0)$ 向正北）。

  1. 我们计算临界点。
    梯度为
    $ nabla f = vec(4x + y, 2y + x - 6). $
    令梯度等于零向量，我们需要解方程组
    $ 4x + y = 0 \ x + 2y - 6 = 0 $
    这是一个简单的线性方程组。
      如果我们由第一个方程令 $y = -4x$，就得到
    $ 2 (- 4 x) + x - 6 = 0 ==> - 8 x + x - 6 = 0 ==> - 7 x = 6 ==> x = - 6 / 7 . $

    由于我们在 $x gt.eq 0$ 的第一象限中工作，这个值不合法。
    因此，我们的区域 $cal(R)$ 中没有临界点。

  2. 如我们所见，有两条边界。
    正 $x$ 轴与正 $y$ 轴上有无穷多个点，
    所以不能把它们逐个代进去。
    原则上，你可以对每个边界部分重做整套简单情形的方法，
    那也是可行的。

    不过，幸运的是，函数在每一部分上都很容易分析，
    我们只用高中数学代数就能做到，不需要微积分。
    这样我们就不必再把整套方法走一遍。

    / $x = 0$ 且 $y >= 0$ 的边界:
      把 $x = 0$ 代入函数：
      $f (0 , y) = 2 (0)^2 + y^2 + 0 dot y - 6 y = y^2 - 6 y$。
      换言之，我们需要看看
      $ f(0,y) = y^2-6y " across all " y >= 0 $
      的最小值可能是多少。
      你 _可以_ 对 $y$ 用导数，
      但我认为最快的办法其实是配方：函数
      $ f(0,y) = (y-3)^2 - 9 $
      显然在 $f(0,3) = -9$ 处取到最小值。
      （而 $f(0,0) = 0$ 与 $f(0,+oo) = +oo$ 都更差。）

    / $y = 0$ 且 $x >= 0$ 的边界:
      把 $y = 0$ 代入函数：
      $ f (x , 0) = 2 x^2 + 0^2 + x (0) - 6 (0) = 2 x^2 . $
      显然 $f(x,0)$ 在 $x = 0$ 处取到最小值。
      （注意 $f(+oo,0) = +oo$ 更差。）

  3. 在 $x -> +oo$ 且 $y -> +oo$ 的极限情形中，显然 $f -> +oo$。

  总之，最好的值其实来自第 2 步：$f(0,3) = -9$
  就是最小可能值。
]

== [TEXT] 最终对边界使用拉格朗日乘子的简单情形示例

下面这个情形中，边界需要用到拉格朗日乘子。
所以，它其实是一个简单情形最优化问题 _之中_ 套着一个困难情形最优化问题。
如果你看过电影 _Inception_（《盗梦空间》），没错，就是那种。
#sample[
  求
  $ f(x,y,z) = x^4 + y^4 + z^4 $
  在区域 $x^2 + y^2 + z^2 <= 1$ 上的最小可能值与最大可能值（如果存在）。
]
#soln[
  乍一看，这似乎应该属于简单情形！
  区域 $cal(R)$ 由闭球 $x^2 + y^2 + z^2 <= 1$ 组成，它确实是三维的。
  但这道例题被放在本节，是因为
  我们会发现检查边界情形需要再次应用拉格朗日乘子。

  让我们执行简单情形的方法。

  0. 没有极限情形，但有边界 $x^2 + y^2 + z^2 = 1$，即单位球面（并非实心球）。

  1. 首先求 $f(x,y,z) = x^4 + y^4 + z^4$ 的临界点。
    写出 $ nabla f = vec(4x^3, 4y^3, 4z^3). $
    解这个简单得近乎侮辱人的方程组 $4x^3 = 4y^3 = 4z^3 = 0$，
    我们看到显然唯一的临界点是 $x=y=z=0$。
    该点的值是 $f(0,0,0) = 0$。

  2. $cal(R)$ 的边界是 $x^2 + y^2 + z^2 = 1$，即单位球面；
    我们用 $cal(S)$ 表示这个球面。
    于是现在我们必须在这个边界上求 $f$ 的值。
    问题在于这个单位球面上的点太多了！
    我们不能逐个检查。
    而且与上一个例子不同，
    这个函数不够简单，无法靠常识来处理。

    因此，我们将使用拉格朗日乘子，
    取约束函数 $g(x,y,z) = x^2+y^2+z^2$，
    来求 $f$ 在这个新区域 $cal(S)$ 上的最小可能值。

    0. 新区域 $cal(S)$ 既没有边界也没有极限情形。
    1. 我们来求 $f$ 在 $cal(S)$ 上的 LM 临界点。
      取 $g$ 的梯度得到
      $ nabla g = vec(2x, 2y, 2z). $
      唯一使 $nabla g = bf(0)$ 的点是 $x=y=z=0$，它不在球面 $cal(S)$ 上，
      所以不必担心 $nabla g = bf(0)$ 的情形。
      现在我们转而解
      $ vec(4x^3, 4y^3, 4z^3) = lambda vec(2x, 2y, 2z). $
      这需要一些手工劳动才能解出，因为情形非常多。
      关于 $x$ 的方程说明
      $ 4x^3 = lambda dot 2 x <==> x = 0 " or " x = pm sqrt(lambda/2) $
      对 $y$ 和 $z$ 也类似：
      $ 4y^3 = lambda dot 2 y &<==> y = 0 " or " y = pm sqrt(lambda/2) \
        4z^3 = lambda dot 2 z &<==> z = 0 " or " z = pm sqrt(lambda/2) $

      先验地看，这似乎需要我们考虑很多情形。
      不过，我们可以利用对称性来减少工作量。
      技巧在于按如下方式消去 $lambda$：
      #align(center)[
        _观察：所有非零的变量 $x$、$y$、$z$ 都应当有相同的绝对值。_
      ]
      具体说来：
      - 如果三个变量都非零，那么 $|x| = |y| = |z| = 1/(sqrt(3))$
        （因为同时还有 $x^2+y^2+z^2=1$）。
      - 如果恰有两个变量非零，那么同理它们的绝对值是 $1/(sqrt(2))$。
      - 而如果只有一个变量非零，它就是 $pm 1$。
      （当然要注意 $(0,0,0)$ 不在 $cal(S)$ 上。）
      想一想为什么这样做行得通。

      所以下面这份列表给出了 *26 个 LM 临界点*：
      - $(pm 1/(sqrt(3)), pm 1/(sqrt(3)), pm 1/(sqrt(3)))$；这种情形有 8 个点。
        它们的 $f$ 值都是 $1/3$。
      - $(pm 1/(sqrt(2)), pm 1/(sqrt(2)), 0)$；这种情形有 4 个点。
        它们的 $f$ 值都是 $1/2$。
      - $(pm 1/(sqrt(2)), 0, pm 1/(sqrt(2)))$；这种情形有 4 个点。
        它们的 $f$ 值都是 $1/2$。
      - $(0, pm 1/(sqrt(2)), pm 1/(sqrt(2)))$；这种情形有 4 个点。
        它们的 $f$ 值都是 $1/2$。
      - $(pm 1, 0, 0)$；这种情形有 2 个点。
        它们的 $f$ 值都是 $1$。
      - $(0, pm 1, 0)$；这种情形有 2 个点。
        它们的 $f$ 值都是 $1$。
      - $(0, 0 pm 1)$；这种情形有 2 个点。
        它们的 $f$ 值都是 $1$。

      呼！好了。其余的情形就短得多了：

    2. $cal(S)$ 没有边界需要考虑。
    3. $cal(S)$ 没有极限情形需要考虑。

  3. $cal(R)$ 没有极限情形需要考虑。

  好了，马拉松结束。把所有结果汇总起来。
  我们看到的 $f$ 的取值有 $0$、$1/3$、$1/2$ 和 $1$，
  而且没有任何类型的极限情形。所以：

  - $f(0,0,0) = 0$ 是全局最小值。
  - $f(pm 1,0,0) = f(0,pm 1,0) = f(0,0, pm 1) = 1$ 是全局最大值。 #qedhere
]

== [SIDENOTE] 一点常识就能省下大量工作

如果你在试图投入微积分计算之前先退一步想一想，
你可能会发现一点 "常识" 能帮你省下大量工作。
我的意思是，想象你把这个问题交给你还没学过 _任何_ 微积分时的自己。
那时的你能说出答案可能具有什么性质吗？
答案是：能，而且往往能。

就拿我们刚做的例子来说：我们要求
$ f(x,y,z) = x^4 + y^4 + z^4 $
在区域 $x^2 + y^2 + z^2 <= 1$ 上的最小值与最大值。
为了展示方法，我关掉了大脑，直接跳进了一段很长的计算。
但事实证明，只要用一点常识，你就能砍掉很多步骤，
完全不需要微积分：

- _最小值_ 其实很明显：就是 $0$，
  因为四次方总是非负的！
  所以即使对高中生来说 $f >= 0$ 也是显然的，而且 $f(0,0,0) = 0$。
- 至于 _最大值_，你实际上可以
  _先验地_ 看出它必定出现在边界 $x^2 + y^2 + z^2 = 1$ 上。
  这是为什么？
  假设你在严格内部有一点 $P = (0.1, 0.2, 0.3)$ 且 $f > 0$。
  那么 $f(P) = f(0.1, 0.2, 0.3)$ 是某个数。
  但你可以显然地只通过放大 $P$ 中各量的绝对值
  来提高 $f$ 的值！
  例如，如果我把 $P$ 的所有坐标翻倍得到 $Q = (0.2, 0.4, 0.6)$，
  那么 $f(Q) = 16 f(P)$。
  只要 $Q$ 仍在球面内，这就是一个更好的值。

  所以内部任何一点显然都不是最大值：
  如果你有一个位于严格内部的点 $P$，
  你可以通过让 $P$ 远离原点来提高 $f(P)$。

这意味着，如果我们用了一点常识，
就可以不费任何力气地得到最小值，
并且可以直接跳到求最大值的 LM 步骤。
所以，如果你还没有被这门课已有的材料压垮，
在力所能及时就寻找这样的捷径。

#digression(title: [题外话：给细心学生的更快解法])[
  我们看到了，只要稍微想一想，最小值就显然是 $0$。
  事实上，如果你意识到答案应当是 $f(1,0,0) = 1$，
  你也可以同样地完全不用微积分就求出最大值。

  做法如下。我们要证明的是
  $ x^4 + y^4 + z^4 <= 1 $
  只要 $x^2+y^2+z^2 <= 1$。
  因为 $y^2$ 和 $z^2$ 非负，显然有 $x^2 <= 1$。
  但实际上很容易看出 $ x^4 <= x^2 " is true whenever " x^2 <= 1. $
  同理，$y^4 <= y^2$ 且 $z^4 <= z^2$。
  因此
  $ x^4 + y^4 + z^4 <= x^2 + y^2 + z^2 = 1. $
]

== [SIDENOTE] 用紧性来检查你的作答

这是一个选读小节，其中包含 18.100 中一个能帮你检查作答的漂亮定理，
但如果你从不犯错，理论上并不需要它。
（但在实践中……）

我需要一个新词 "紧"（compact），和之前一样，
给出它的正式定义超出了 18.02 的范围。
不过，我冒昧给出下面这个说法：对于 18.02 的例子，
*$cal(R)$ 是紧的当且仅当没有极限情形*。
也就是说，

- 所有约束都是 $=$、$<=$ 或 $>=$；没有 $<$ 或 $>$，
- 没有任何变量可以趋于 $pm oo$。

那么我答应给你的定理就是：

#tip(title: [提示：紧区域最优化定理])[
  如果 $cal(R)$ 是一个紧区域，
  且 $f$ 是要在该区域上最优化的、处处连续有定义的函数，
  那么必定至少存在一个全局最小值，以及至少一个全局最大值。
]
无论简单情形（不用拉格朗日乘子）还是困难情形（用拉格朗日乘子），这都成立。

下面举几个例子说明这个定理如何帮到你：

#example[
  - 假设有人要你在正方形 $-1 <= x <= 1$、$-1 <= y <= 1$ 上
    最优化一个连续函数 $f(x,y)$。
    我们已看到这个正方形没有极限情形。
    那么紧区域最优化定理向你保证，答案中
    "不存在全局最小值" 或 "不存在全局最大值" 永远不会出现。

  - 假设有人要你在球面 $x^2 + y^2 + z^2 = 1$ 上
    最优化一个连续函数 $f(x,y,z)$
    （这意味着你大概要使用拉格朗日乘子）。
    我们已看到这个球面没有极限情形（甚至没有边界）。
    那么紧区域最优化定理向你保证，答案中
    "不存在全局最小值" 或 "不存在全局最大值" 永远不会出现。

  - 假设有人要你在闭球 $x^2 + y^2 + z^2 <= 1$ 上
    最优化一个连续函数 $f(x,y,z)$，就像上一个例子那样。
    这个闭球同样没有极限情形，
    所以紧区域最优化定理向你保证，答案中
    "不存在全局最小值" 或 "不存在全局最大值" 永远不会出现。
]

== [RECAP] Foxtrot 部“最优化”回顾

- 我们引入了临界点的概念，即满足 $nabla f = bf(0)$ 的点。
  - 我们看到临界点可以是局部最小值、局部最大值或鞍点。
  - 我们引入了二阶导数判别法，用来区分其中的一些情形，
    尽管二阶导数判别法有时无法给出结论。
- 我们讨论了区域如何具有维数、极限情形与边界。
  虽然我们没有给出正式定义，但说明了在 18.02 中管用的经验法则。
- 对于没有 $=$ 约束的最优化问题，
  我们检查临界点、极限情形与边界。
- 对于有一个 $=$ 约束的最优化问题，
  我们检查 LM 临界点、极限情形与边界。
- 求临界点或 LM 临界点都涉及解方程组。
  这没有固定的方法，但我们给出了一些可以尝试的策略，
  具体取决于你得到的方程组的实际形状。

== [EXER] 习题

#exer[
  设 $A B C$ 是 $x y$ 平面中顶点为
  $A = (0,12)$、$B = (-5,0)$、$C = (9,0)$ 的三角形。
  平面中什么点 $P$ 使和
  $ P A ^ 2 + P B ^ 2 + P C ^ 2 $
  尽可能小？
] <exer-opt-geo>

#exer[
  已知 $sin(x)+sin(y)=1$ 且 $x,y >= 0$，求 $x+y$ 的最小可能值。
] <exer-opt-lm-sin>

/*
好吧，我觉得这道习题太烦人了
#exerstar[
  求
  $ 15x^2 + 12 x y + 16 x z + 6 y^2 + 8 z^2 $
  在约束 $x,y,z >= 0$ 且 $x^3 + (x+y)^3 + (x+z)^3 = 1$ 下的最大可能值。
] <exer-opt-lm-hell>
*/

#exer[Suggested by Ting-Wei Chao][
  求函数
  $ f(x,y)=|x^2+y^2-25|-3x-4y. $
  的全局最小值。
] <exer-lm-abs>
