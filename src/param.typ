#import "@local/evan-zh:1.0.0":*

= 参数方程 <ch-param>

== [TEXT] 多元定义域与多元陪域

在 18.01 中，你研究的是函数 $F : RR -> RR$ 的微积分。
因此，一开始"多元微积分"可以指两件事之一：

- 改为研究 $F : RR -> RR^n$（即让陪域变成多元的）。
- 改为研究 $F : RR^n -> RR$（即让定义域变成多元的）。

你现在应当知道的是：*第一件事比第二件事容易得多*。
因此 Part Delta 这一部分非常短。

== [TEXT] 参数图像

从现在起，我们通常会把记号改成
$ bf(r) : RR &-> RR^n \
  bf(r)(t) &= vec("关于 " t " 的函数", dots.v, "关于 " t " 的函数"). $
输入变量选用字母 $t$ 通常表示"时间"；
我们用 $bf(r)$ 作为函数名，以提醒输出是一个向量。
@fig-param-timeline 给出了它的示意图。

#typesig[
  当你看到 $bf(r)(t)$ 或类似的记号时，
  时间变量 $t$ 的类型是标量。
  输出属于 $RR^n$，视上下文而定，
  你可以把它看成点或向量。
]

#figure(
  image("figures/param-timeline.svg", width: auto),
  caption: [我们把 $bf(r)(t)$ 理解为用某个变量 $t$ 的时间轴
    在 $RR^n$ 中描出某种路径。],
) <fig-param-timeline>


#warning(title: [警告：$bf(r)(t)$ 既可以画成点也可以画成箭头，
  但在 Part Delta 中我们仍然使用向量记号])[
  遗憾的是，即使在我们把 $bf(r)(t)$ 看成像 $(3,5)$ 这样的点时，
  我们仍然使用粗体字母 $bf(r)$ 并写成 $vec(3,5)$。
  类型学的爱好者有理由反对这一点，
  但这种写法根深蒂固，如果我过于拘泥，
  反而会与其他资料产生混淆。

  所以，在本章中不要太纠结点和箭头的区别。
  在整个 Part Delta 中，我们都不会把 $(3,5)$ 与 $vec(3,5)$ 视为不同。
]

如果你在画参数函数的图像，
通常所有的轴都是 $bf(r)(t)$ 的分量，而时间变量没有对应的轴。
换言之，在图中，*所有轴变量都是输出分量，
我们对它们一视同仁*。
输入的时间变量根本不出现。
（这与 18.01 中的 $x y$ 图像相反，那里一个轴是输入、一个轴是输出。
在下一节讨论_等值线_时，情况又会反过来：输出变量是匿名的，
而每个轴都是我们一视同仁的输入变量。）

#example[
  经典的例子
  $ bf(r)(t) = vec(cos(t), sin(t)) $
  会被画成单位圆。
  你可以想象一个质点从 $bf(r)(0) = vec(1,0)$ 出发，然后
  以恒定速率沿单位圆逆时针运动。
  它在 $2 pi$ 时间内完成一整圈：$bf(r)(2 pi) = vec(1,0)$。
]

== [TEXT] 永远只用分量

为什么 $bf(r) : RR -> RR^n$ 简单到让 Part Delta 只有一章？
因为基本上你只需要做一件事：
#idea[
  长话短说：永远只用分量。
]
也就是说，如果（比如）$bf(r) : RR -> RR^3$，那么基本上 90% 以上的情况下你要做的就是写成
$ bf(r)(t) = chevron.l x(t), y(t), z(t) chevron.r
  = x(t) ee_1 + y(t) ee_2 + z(t) ee_3 $
然后对每个 $f_i$ 做单变量微积分或计算。

- 需要求 $bf(r)$ 的导数？对每个分量求导。
- 需要求 $bf(r)$ 的积分？对每个分量积分。
- 需要 $bf(r)$ 的绝对值？各分量平方和的平方根。

如此等等。Evan 没能做到这一点的例子见 @fig-brainfart。

#figure(
  rect[#image("media/brainfart.png", width: 95%)],
  caption: [说真的，一切都逐分量做就好了。],
) <fig-brainfart>

== [RECIPE] 参数相关的内容

我会用两个变量来写这个方法，但它对三个变量同样适用。
设给你一个方程 $bf(r)(t) = vec(x(t), y(t))$。
你可能会被问到以下几件事：

#recipe(title: [参数相关内容的方法/定义])[
  - 时刻 $t$ 的*速度向量*定义为导数 $ bf(r)'(t) = vec(x'(t), y'(t)). $
  - 时刻 $t$ 的*速率*定义为速度的绝对值：
    $ |bf(r')(t)| = sqrt(x'(t)^2 + y'(t)^2). $
  - 时刻 $t$ 的*加速度向量*定义为每个分量的
    二阶导数：$ bf(r)''(t) = vec(x''(t), y''(t)). $
  对于三变量的 $bf(r)(t) = chevron.l x(t), y(t), z(t) chevron.r$，用三个分量做同样的事。
]
我不知道加速度向量的绝对值有没有专门的词
（就像速率是速度向量的绝对值那样）。

现在还要再提一件事：
#recipe(title: [弧长的方法/定义])[
  从时刻 $t_"起点"$ 到 $t_"终点"$ 的*弧长*定义为速率的积分：
  $ "arc length" = integral_(t="起始时刻")^("终止时刻") |bf(r)'(t)| dif t. $
]
（严格来说，这里我应当用"定义"框而不是"方法"框，
因为这些其实是相关术语的_定义_，
而所谓方法就是"照定义直接使用"。）

#typesig[
  - 速度 $bf(r)'(t)$ 与加速度 $bf(r)''(t)$ 是向量。
    在这些情况下，你应当_总是_把它们画成箭头（向量）而不是点。
    也就是说，你绝不应当把速度或加速度画成点。

  - 然而，速率 $|bf(r)'(t)|$ 与弧长是标量（数）。
]

#sample[
  设 $ bf(r)(t) = vec(cos(t), sin(t)). $
  计算：

  - 时刻 $t = pi/3$ 的速度向量。
  - 时刻 $t = pi/3$ 的速率。
  - 时刻 $t = pi / 3$ 的加速度向量。
  - 从 $t = 0$ 到 $t = pi / 3$ 的弧长。
]
#soln[
设 $bf(r) (t) = vec(cos (t) , sin (t))$。
我们将计算以下各量。

/ 时刻 $t = pi / 3$ 的速度向量:
  速度向量是位置向量
  $bf(r) (t)$ 关于 $t$ 的导数：
  $ bf(v) (t) = bf(r)' (t) = vec(- sin (t) , cos (t)) . $
  在 $t = pi / 3$ 处，我们有：
  $ bf(v) (pi / 3) = vec(- sin (pi / 3) , cos (pi / 3)) = vec(- sqrt(3) / 2 , 1 / 2) . $
  因此，时刻 $t = pi / 3$ 的速度向量为：
  $ bf(v) (pi / 3) = vec(- sqrt(3) / 2 , 1 / 2) . $
/ 时刻 $t = pi / 3$ 的速率:
  速率是速度向量的模长：
  $ lr(|bf(v) (t)|) = sqrt((- sin (t))^2 + (cos (t))^2) = sqrt(sin^2 (t) + cos^2 (t)) = 1 . $
  因此，时刻 $t = pi / 3$（事实上任何时刻）的速率为：
  $ lr(|bf(v) (pi / 3)|) = 1 . $
/ 时刻 $t = pi / 3$ 的加速度向量:
  对我们之前得到的速度向量求导：
  $ bf(a) (t) = bf(v)' (t) = vec(- cos (t) , - sin (t)) . $
  在 $t = pi / 3$ 处，我们有：
  $ bf(a) (pi / 3) = vec(- cos (pi / 3) , - sin (pi / 3)) = vec(- 1 / 2 , - sqrt(3) / 2) . $
  因此，时刻 $t = pi / 3$ 的加速度向量为：
  $ bf(a) (pi / 3) = vec(- 1 / 2 , - sqrt(3) / 2) . $
/ 弧长:
  参数曲线的弧长由下式给出：
  $ L = integral_0^(pi / 3) lr(|bf(r)' (t)|) dif t = integral_0^(pi / 3) 1 dif t = pi / 3 . $
  因此，从 $t = 0$ 到 $t = pi / 3$ 的弧长为：
  $ L = pi / 3. #qedhere $
]

== [TEXT] 匀速与角速度

在 18.02 中，我们会看到一些复杂的轨迹，
它们实际上是两个更简单轨迹之和。
因此本节先描述一些简单轨迹的例子；
下一节我们再开始把其中一些加起来。

匀速运动很简单：
如果一个点从点 $A_0$ 出发，
以速度 $bf(v)$ 沿直线运动，
那么参数化为 $ bf(A)(t) = A_0 + t bf(v). $

#sample[
  点 $P$ 从 $(1,2,3)$ 出发，沿 $+x$ 方向以恒定速度 $5$ 运动。
  给出位置 $bf(P)(t)$ 的参数化。
]
#soln[
  直接写成
  $ bf(P)(t) = vec(1,2,3) + t vec(5,0,0) = vec(5t+1, 2, 3). #qedhere $
]

旋转其实也相当简单，
只是它改用"角速度"这个词。
如果你没见过角速度这个术语，我们现在来描述它。
#definition[
  若一个物体以每单位时间 $omega$ 弧度的速率旋转，
  则称它具有_角速度_ $omega$。
  例如，角速度为"每秒 $10 pi$"
  意味着该物体每秒完成五圈旋转（每圈 $2 pi$ 弧度）。
]

设点 $P$ 以恒定角速度 $omega$ 沿以 $(0,0)$ 为圆心、半径为 $r$ 的圆周运动。
那么该点总可以写成
$ (r cos(theta), r sin(theta)) $
的形式，其中 $theta$ 是随 $t$ 变化的某个角度。
逆时针的角速度对应于 $theta$
每单位时间增加 $omega$
（因此在时刻 $t$ 角度为 $theta + t omega$）；
顺时针则相反，是每单位时间减少 $omega$
（因此在时刻 $t$ 角度为 $theta - t omega$）。
见 @fig-angular。

#figure(
  image("figures/param-angular.svg", width: auto),
  caption: [点以恒定角速度旋转。],
) <fig-angular>

#recipe(title: [匀角速度运动的方法])[

  1. 求出时刻 $t = 0$ 位置所对应的初始角 $theta_0$。
  2. 如果运动是逆时针的，输出
    $ bf(P)(t) = vec(r cos(theta_0 + omega t), r sin(theta_0 + omega t)). $
    如果是顺时针的，则输出
    $ bf(P)(t) = vec(r cos(theta_0 - omega t), r sin(theta_0 - omega t)). $
    （注意 $+$ 变成了 $-$。）
]


#sample[
  点 $P$ 沿以 $(0,0)$ 为圆心、半径为 $2$ 的圆 $x^2+y^2=4$ 运动。
  它从 $(sqrt(3), 1)$ 出发，并以角速度 $omega$ 顺时针运动。
  给出位置 $bf(P)(t)$ 的参数化。
]
#soln[
  该点从 $pi/6 = 30 degree$ 的角度出发。
  所以
  $ bf(P)(t) = vec(2cos(pi/6 - omega t), 2sin(pi / 6 - omega t)). $
  注意当 $t = 0$ 时，这确实给出我们最初的起点。
]

== [RECIPE] 通过把两个更简单的轨迹相加来求看起来复杂的轨迹的参数化

一旦你有了 $bf(r)(t)$ 的方程，一切都变得非常机械，
因此 18.02 中会出现一类考题：
题目给你一条看起来很奇怪的路径，你需要自己求出它的方程 $bf(r)(t)$，
才能进一步求速度、速率等等。

大约 90% 以上的情况下，图形之所以奇怪，
是因为它是另外两个向量之和，你只要把它们相加即可。
我纯粹为了搞笑也写一个方法：
#recipe(title: [把路径分解为两项之和的方法])[
  设 $P$ 是沿某条奇怪轨迹运动的点。
  要对 $bf(P)(t)$ 参数化，一种常见做法是：

  1. 求出另一个关注的点 $Q$ 的表达式，记为 $bf(Q)(t)$。
  2. 求出 $bf(v)(t)$ 的表达式，即从 $Q$ 指向 $P$ 的向量。
  3. 输出 $bf(P)(t) = bf(Q)(t) + bf(v)(t)$。
]

下面我们给出一些这样的例子。
仅在本节讲义中，如果 $P$ 是一个点，
我就用 $bf(P)(t)$ 表示对应的参数曲线。
#tip[
  这一节会让人觉得重复。
  看了一会儿之后，几乎所有的例子看起来都一样。
  不是游乐园的游乐设施，就是飞盘，或者旋转的行星之类的，
  或者朝某个方向滚动的轮子……
  它们都只是对同一件事反复套上一层薄薄的背景故事。
]

好，下面是一些例子。

#figure(
  image("figures/param-frisbee.svg", width: auto),
  caption: [旋转的飞盘。],
) <fig-param-frisbee>

#sample[
  一个飞盘是半径为 $r$ 的圆形，飞盘边缘上标记了一个点 $P$。
  它绕其中心以角速度 $omega$ 逆时针、半径 $r$ 做圆周旋转，
  同时又在平面内以恒定
  速度 $vec(v_x , v_y)$ 沿直线运动。
  飞盘最初位于 $(0 , 0)$，标记点在 $(0 , r)$。

  把飞盘边缘上标记点的位置 $bf(P)(t)$
  参数化为时间的函数。
  （见 @fig-param-frisbee。）
]

#soln[
  飞盘以恒定速度 $vec(v_x , v_y)$ 运动。

  1. 飞盘中心的位置作为时间的函数是：
    $ bf(O) (t) = vec(v_x t , v_y t) . $
    这给出了飞盘中心在平面内的轨迹。

  2. 飞盘还绕其中心以角速度
    $omega$ 旋转。飞盘边缘上的标记点
    绕飞盘中心沿半径为 $r$ 的圆周运动。

    由于标记点在 $t = 0$ 时位于 $(0 , r)$，它绕中心的旋转
    运动可以参数化地描述为：
    $ bf(v) (t) = vec(r cos(pi / 2 + omega t), r sin(pi / 2 + omega t))
      = vec(- r sin (omega t) , r cos (omega t)) . $
    这里 $omega$ 是角速度（以弧度每秒为单位），
    正弦和余弦项描述了标记点绕中心的逆时针圆周运动。
    （注意 $t = 0$ 时我们得到 $vec(0, r)$，正是我们想要的。）

  3. 要求出标记点作为时间的函数的总位置，我们
    需要把飞盘中心的平动
    $bf(O) (t)$ 与旋转运动 $bf(v) (t)$ 结合起来。
    因此，时刻 $t$ 标记点的位置是两者之和：
    $ bf(P) (t) = bf(O) (t) + bf(v) (t) . $

    代入 $bf(O) (t)$ 与 $bf(v) (t)$ 的表达式，得到：
    $ bf(P) (t) = vec(v_x t , v_y t) + vec(- r sin (omega t) , r cos (omega t)) . $
  化简得：
  $ bf(P) (t) = vec(v_x t - r sin (omega t) , v_y t + r cos (omega t)) . #qedhere $
]

#sample[
  一颗行星以半径 $R_s$、_逆时针_角速度 $omega_s$ 沿圆形轨道绕太阳运动。
  一颗卫星以半径 $R_m$、_顺时针_角速度 $omega_m$ 沿圆形轨道绕该行星运动。
  把卫星相对于太阳的运动 $bf(M)(t)$ 参数化，
  假设太阳位于原点、行星从 $(R_s , 0)$ 出发、
  卫星从 $(R_s , -R_m)$ 出发。
  （见 @fig-param-planet。）
]

#figure(
  image("figures/param-planet.svg", width: auto),
  caption: [行星绕卫星做圆周运动。
    （在现实中，我想它们大概是椭圆而不是圆。）],
) <fig-param-planet>

#soln[
  1. 行星以半径 $R_s$、角速度 $omega_s$ 逆时针绕太阳运动。
    行星的位置作为时间的函数是：
    $ bf(P) (t) = vec(R_s cos (omega_s t) , R_s sin (omega_s t))  $
    因为行星从太阳正东方向出发并逆时针旋转。
    这描述了行星周期为 $(2 pi) / (omega_s)$ 的逆时针圆周运动。

  2. 由于卫星顺时针绕行星运动，其运动方向
    与行星的轨道相反。卫星相对于行星从
    $chevron.l 0 , - R_m chevron.r$（正南方向）出发，
    并以角速度 $omega_m$ 运动。

    卫星相对于行星的位置（顺时针运动）
    由下式给出：
    $ bf(v) (t) = vec(R_m cos (3 pi / 2 - omega_m t) , R_m sin (3 pi / 2 - omega_m t))
      = vec(- R_m sin (omega_m t) , - R_m cos (omega_m t)) . $
    这描述了卫星绕行星的顺时针运动。

  3. 为求卫星相对于太阳的总位置，我们把
    行星的位置 $bf(P) (t)$ 与卫星相对于行星的位置
    $bf(v) (t)$ 结合起来。因此，卫星
    相对于太阳的位置是：
    $ bf(M) (t) = bf(P) (t) + bf(v) (t) . $
    代入 $bf(P) (t)$ 与
    $bf(v) (t)$ 的表达式，得到：
    $ bf(M) (t) = vec(R_s cos (omega_s t) , R_s sin (omega_s t)) + vec(- R_m sin (omega_m t) , - R_m cos (omega_m t)) . $
  化简得：
  $ bf(M) (t) = vec(R_s cos (omega_s t) - R_m sin (omega_m t) , R_s sin (omega_s t) - R_m cos (omega_m t)). #qedhere $
]

#sample[
  一个半径为 $r$ 的轮子初始时圆心在 $(0,r)$，
  并以恒定速率 $v$ 沿 $+x$ 方向运动。
  设 $P$ 是轮缘上最初位于 $(0,0)$ 的点。
  把点 $bf(P)(t)$ 的轨迹参数化。
  （图见 @fig-cycloid。）
]

#figure(
  image("figures/param-cycloid.svg", width: auto),
  caption: [轮子向右滚动所形成的摆线。],
) <fig-cycloid>

#soln[
  这道题稍难一些，因为虽然写出轮子中心的运动很容易，
  但轮子的角速度 $omega$ 是多少并不显然。
  这需要一个想法：用两种方式写出地面上轮胎痕迹的长度。

  1. 简单的一步：轮子以恒定速度 $v$ 沿直线滚动。时刻 $t$
    轮子中心的位置为：$ bf(C) (t) = vec(v t , r) . $
    这描述了轮心沿水平轴的平动。

  2. 题目的难点在于确定轮子的角速度。
    关键想法是考察地面上留下的轮胎痕迹的长度。
    见 @fig-param-tire-track-cycloid。

    - 一方面，经过时间 $t$ 后，轮胎痕迹的长度为
      $ L_("tire track") = v t $，
      因为轮子在地面上走过了这么多距离。
      这在 @fig-param-tire-track-cycloid 中用棕色画出。

    - 另一方面，经过时间 $t$ 后，轮胎痕迹的长度也应当是
      $ L_("tire track") = omega t dot r. $
      （对你们中的一些人来说，如果我写成
      $((omega t) / (2 pi)) dot (2 pi r)$ 可能更自然，
      因为 $(omega t) / (2 pi)$ 是完成的整圈数，
      而 $2 pi r$ 是轮子的总周长。）
      这在 @fig-param-tire-track-cycloid 中用深蓝色画出。

    #figure(
      image("figures/param-tire-track-cycloid.svg", width: auto),
      caption: [通过计算地面上轮胎痕迹的长度
      来确定角速度 $omega$。],
    ) <fig-param-tire-track-cycloid>

    令两个表达式中的 $L_("tire track")$ 相等，得到
    $ v t = omega t dot r ==> omega = v / r. $
    （现在我们就把 $L_("tire track")$ 忘掉。
    它唯一的用途就是给我们一个抓住 $omega$ 的办法。）

    既然我们已经跨过了这个障碍，这道例题剩下的部分就和前面两道完全一样了。
    轮缘上的点在 $t = 0$ 时位于轮子的最下点，即正南方向。
    轮子的旋转是顺时针的（想象真的把轮子滚起来）。
    因此，时刻 $t$ 轮缘上的点相对于轮心的位置
    可以参数化为：
    $ bf(R) (t) = vec(r cos(3 pi / 2 - omega t), r sin(3 pi / 2 - omega t))
      = vec(-r sin (v / r t) , -r cos (v / r t)) . $
    这描述了轮缘上的点以半径 $r$、角速度 $v / r$ 绕轮心的
    圆周运动。

  3. 为求轮缘上点的总位置，我们
    把轮心的平动
    $bf(C) (t)$ 与轮缘上点的旋转运动
    $bf(R) (t)$ 结合起来。时刻 $t$ 轮缘上点的
    总位置为：
    $ bf(P) (t) = bf(C) (t) + bf(R) (t) . $
    代入 $bf(C) (t)$ 与 $bf(R) (t)$ 的表达式，得到：
    $ bf(P) (t) = vec(v t , r) + vec(-r sin (v / r t) , -r cos (v / r t)) . $
  化简得：
  $ bf(P) (t) = vec(v t - r sin (v / r t) , r - r cos (v / r t)) . #qedhere $
]

现在你已经有其中每一条的参数方程了，
你也能回答前面那些方法所能解决的任何问题，比如
"总共走了多少距离"或"此时速率是多少"等等。
例如：

#remark[
  $bf(P)$ 的图形称为_摆线_，见 @fig-cycloid。
  这个图形看起来相当吓人！
  然而，要计算弧长之类的东西（见下一道例题），
  你其实根本不需要了解这个图形的任何东西。
  几何图像只用来提取 $bf(P)(t)$ 的代数表达式。
  之后你就可以完全忘掉图像，
  直接对你提取出的表达式做微积分。
]

我们来看看。
#sample[
  一个半径为 $1$ 的轮子初始时圆心在 $(0,1)$，
  并以恒定速率 $1$ 沿 $+x$ 方向运动。
  设 $P$ 是轮缘上最初位于 $(0,0)$ 的点。
  计算点 $P$ 的轨迹
  从时刻 $t = 0$ 到 $t = 2 pi$ 的总弧长。
]
#soln[
  我们刚刚得到了摆线的一般方程
  $ bf(P) (t) = vec(v t - r sin (v / r t) , r - r cos (v / r t)) $。
  当 $v = 1$、$r = 1$ 时，它就是
  $ bf(P) (t) = vec(t - sin (t) , 1 - cos (t)). $
  我们求导得到速度向量
  $ bf(P)' (t) = vec(1 - cos (t) , sin (t)). $
  因此，弧长由公式给出
  $ L = integral_0^(2pi) sqrt((1-cos(t))^2 + sin(t)^2) dif t. $

  现在这就变成了一道 18.01 的积分题。
  在这个具体情形中，平方根可以用三角计算化简。
  我们可以把根号内的各项展开：
  $ (1 - cos (t))^2 + sin^2 (t) = 1 - 2 cos (t) + cos^2 (t) + sin^2 (t) . $
  利用恒等式 $sin^2 (t) + cos^2 (t) = 1$，它化简为：
  $ 1 - 2 cos (t) + 1 = 2 - 2 cos (t) . $
  技巧在于使用半角公式把它化为
  $ 1 - cos(t) = 2sin^2(t/2) ==> sqrt(2 - 2 cos(t)) = sqrt(4sin^2(t/2)) = lr(|2 sin(t/2)|). $
  因此，积分现在变为：
  $ L = integral_0^(2 pi) sqrt(2 (1 - cos (t))) dif t
    = integral_0^(2 pi) lr(|2 sin(t/2)|) dif t. $
  在区间 $0 <= t <= 2 pi$ 上我们总有 $sin(t/2) >= 0$，
  所以去掉绝对值：
  $ L = integral_0^(2 pi) 2 sin(t/2) dif t = lr([-4cos(t/2)])_0^(2pi)
    = -4cos(pi) + 4cos(0) = 8. #qedhere $
]

== [TEXT] 时间可灵活选取的参数化 <sec-flex-param>

有时你会被要求把某条路径参数化，但不要求遵循确切的时间。
（这在 @ch-work 中经常出现，当我们引入功的积分时。）
在这种情况下，你尽可以任意选取一个能描出所要求路径的参数化，
连起始和结束时间也可以任选。
通常的策略是选一个让后续计算更简单的。

#sample[
  设 $cal(C)$ 是从 $(0,0,0)$ 出发、到 $(100, 200, 300)$ 结束的线段。
  给出 $cal(C)$ 的任意一个参数化 $bf(r)(t)$。
]
#soln[
  参数化应当从 $(0,0,0)$ 开始，在 $(100, 200, 300)$ 结束，
  并且经过这条线段。
  其轨迹上一些示例点的取样是
  $ (0,0,0) --> (1,2,3) --> (10, 20, 30) --> (50, 100, 150) --> (100, 200, 300) $，
  此外还有很多其他点（比如 $(8pi, 16pi, 24pi)$ 等）。
  总之，以下所有参数化都是可接受的：

  - $bf(r)(t) = (t, 2t, 3t)$，其中 $0 <= t <= 100$
  - $bf(r)(t) = (100t, 200t, 300t)$，其中 $0 <= t <= 1$
  - $bf(r)(t) = (100t^7, 200t^7, 300t^7)$，其中 $0 <= t <= 1$，
    如果你喜欢给自己找麻烦的话。

  再说一次，通过例子最容易内化这一点：
  在第一例中，试着写出
  $t = 0$、$t = 1$、$t = 2$、……、$t = 100$ 时点的位置，
  并验证它描出的确实是我们想要的东西。

  实际上，大多数人会更愿意用第一个或第二个。
]

#sample[
  设 $cal(C)$ 是从 $(7,8,9)$ 出发、到 $(107, 208, 309)$ 结束的线段。
  给出 $cal(C)$ 的任意一个参数化 $bf(r)(t)$。
]
#soln[
  例如，参数化应当从 $(7,8,9)$ 开始，在 $(107, 208, 309)$ 结束。
  路径上的一些示例点：
  $ (7,8,9) --> (8,10,12) --> (17, 28, 39) --> (57, 108, 159) --> (107, 208, 309). $
  因此以下所有都是可接受的参数化示例：

  - $bf(r)(t) = (t+7, 2t+8, 3t+9)$，其中 $0 <= t <= 100$
  - $bf(r)(t) = (100t+7, 200t+8, 300t+9)$，其中 $0 <= t <= 1$
  - $bf(r)(t) = (100sin(t)+7, 200sin(t)+8, 300sin(t)+9)$，其中 $0 <= t <= pi/2$，
    如果你真的很喜欢三角函数的话。

  我推荐第一个。
]

#sample[
  设 $cal(C)$ 表示抛物线 $y = x^2$ 上从 $(-1,1)$ 出发
  向右到 $(1,1)$ 的弧。
  给出 $cal(C)$ 的任意一个参数化 $bf(r)(t)$。
]
#soln[
  为了具体一些，我们预期路径会经过的一些示例点是
  $ (-1,1) --> (-1/2, 1/4) --> (-1/3, 1/9) --> (0,0) --> (1/3, 1/9) --> (1/2, 1/4) --> (1,1). $
  因此以下全部都是例子：

  - $bf(r)(t) = (t, t^2)$，其中 $-1 <= t <= 1$。（没错，负的时间是可以的！）
  - $bf(r)(t) = (t-1, (t-1)^2)$，其中 $0 <= t <= 2$，
    如果你对负时间过敏的话。
  - $bf(r)(t) = (2t-1, (2t-1)^2)$，其中 $0 <= t <= 1$。
  - $bf(r)(t) = (log(t), log(t)^2)$，其中 $1/e <= t <= e$，如果你这一天没别的更好的事可做。

  我推荐第一个。
]

#sample[
  设 $cal(C)$ 是沿抛物线 $y = x^2 / 10 + 1$
  从 $(-2,1.4)$ 出发、到 $(3,1.9)$ 结束所描出的路径。
  （见 @fig-param-parabola。）
  给出 $cal(C)$ 的任意一个参数化 $bf(r)(t)$。
]
#figure(
  image("figures/param-parabola.svg", width: auto),
  caption: [沿抛物线 $y = x^2 / 10 + 1$ 行走。
    我推荐参数化 $bf(r)(t) = (t, t^2/10+1)$，其中 $-2 <= t <= 3$。],
) <fig-param-parabola>

#soln[
  这条轨迹经过的一些示例点是：
  $ (-2,1.4) --> (-1, 1.1) --> (0,1) --> (1,1.1) --> (2,1.4) --> (3,1.9). $
  在这类情形中，某一个坐标沿路径从一端变到另一端，
  一种常见的策略就是直接用那个坐标作为 $t$，
  再由此求出其他坐标。

  以下全部都是可接受的参数化示例：

  - $bf(r)(t) = (t, t^2 / 10 + 1)$，其中 $-2 <= t <= 3$。
  - $bf(r)(t) = (t-2, (t-2)^2 / 10 + 1)$，其中 $0 <= t <= 5$，如果你对负时间过敏的话。
  - $bf(r)(t) = (5t-2, (5t-2)^2 / 10 + 1)$，其中 $0 <= t <= 1$，如果你非常希望结束时间是 $1$ 的话。
  - $bf(r)(t) = (5 dot 2^t - 7, (5 dot 2^t - 7)^2 / 10 + 1)$，其中 $0 <= t <= 1$，如果你想折磨阅卷人的话。

  我认为实际上大多数人会更愿意用第一个。
]

#sample[
  设 $cal(C)$ 是单位圆上从 $(0, -1)$ 出发、
  到 $(sqrt(3)/2, 1/2)$ 结束、逆时针方向的 $120 degree$ 弧。
  （见 @fig-param-arc。）
  给出 $cal(C)$ 的任意一个参数化 $bf(r)(t)$。
]
#figure(
  image("figures/param-circle.svg", width: auto),
  caption: [单位圆上一段 $120 degree$ 的弧，由
    $bf(r)(t) = (cos t, sin t)$（$-pi/2 <= t <= pi/6$）参数化。],
) <fig-param-arc>

#soln[
  以下全部都是可接受的参数化示例：
  - $bf(r)(t) = (cos t, sin t)$，其中 $-pi/2 <= t <= pi/6$。
  - $bf(r)(t) = (cos t, sin t)$，其中 $3/2 pi <= t <= 13/6 pi$，
    如果你坚持使用非负的 $t$ 的话。
  - $bf(r)(t) = (cos(t + 3/2 pi), sin (t + 3/2 pi))$，其中 $0 <= t <= 2/3 pi$。
  - $bf(r)(t) = (sqrt(1-t^2), t)$，其中 $-1 <= t<= 1/2$（不推荐）。

  再说一次，我推荐最简单的（第一个）。
]

== [EXER] 习题

#exer[
  计算抛物线 $y = x^2 - x - 12$
  在 $(-3,0)$ 与 $(4,0)$ 之间那一段的弧长。

  你大概会需要下面这个在 18.01 中不常见的原函数事实：
  $ integral sqrt(u^2+1) dif u = u/2 sqrt(u^2+1) + log(u + sqrt(u^2+1))/2 + C. $
] <exer-param-parabola>

#exer[
  在游乐园里，一种旋转茶杯游乐设施由
  绕固定中心顺时针旋转的茶杯组成，而每个茶杯自身又逆时针旋转。
  （如果你从没见过这种东西，见 @fig-teacups。）
  该旋转茶杯设施在 $RR^2$ 中如下指定：

  - 该设施绕 $(0,0)$ 以半径 $R$、
    角速度 $omega_(upright("ride"))$ _顺时针_旋转。
  - 每个茶杯以角速度
    $omega_(upright("cup"))$、半径 $r$ _逆时针_旋转。
  - 初始时，在 $t = 0$，茶杯中心位于 $(R , 0)$，
    而一个幼儿位于茶杯边缘相对于其中心的最右点。

  计算时刻 $t$ 该幼儿的_速度_向量。
] <exer-param-teacup>

#figure(
  image("media/teacups.jpg", width: 60%),
  caption: [你知道的，就是那种旋转茶杯游乐设施。
  图片来源：#link("https://dreamlandamusements.com/carnival-ride-rentals/tea-cups/")[Dreamland Amusements]。],
) <fig-teacups>

#exer[
  $RR^3$ 中的一架直升机以恒定速率 $5$ 沿 $+z$ 方向向上运动，
  同时其旋翼叶片
  在水平面内以 _顺时针_ 角速度 $pi /3$
  和半径 $2$ 旋转。
  设 $P$ 是叶片尖端上的一个点，初始位于 $(2,0,0)$。

  - 把其中一个叶片尖端上某点的运动
    参数化为时间的函数，
    假设直升机从高度 $z = 0$ 开始，
    且在 $t = 0$ 时叶片沿正 $x$ 轴方向。
  - 计算 $P$ 从时刻 $t = 0$ 到时刻 $t = 18$ 走过的距离。
] <exer-param-helicopter>

#figure(
  image("figures/param-clockblock.svg", width: auto),
  caption: [2015 年 AMC 10A 的时钟问题。],
) <fig-clockblock>

#exerstar[#link("https://aops.com/community/p3734621")[AMC 10A 2015]][
  在 @fig-clockblock 中，有一个半径为 20 cm 的圆形钟面
  和一个半径为 10 cm 的圆盘，二者在 12 点位置外切。
  圆盘上画有一个正朝上的箭头，并顺时针滚动。
  当箭头下一次指向竖直向上方向时，
  圆盘将与钟面在何处相切？
] <exer-param-clockblock>
