#import "@local/evan-zh:1.0.0":*

= 向量复习

== [TEXT] 标量、向量与点的记号

如果你以前没见过符号 $RR$，我们现在就引入它：
#definition[
  我们用 $RR$ 表示实数集，因此 $3, sqrt(2), -pi$ 都是 $RR$ 的元素。
  有时我们也会把一个实数称为*标量*（scalar）。
]
符号 "$in$"，如果你以前没见过，意思是“是……的成员”。
因此 $3 in RR$ 是命题“$3$ 是实数”。
而 $x in RR$ 表示 $x$ 是实数。

遗憾的是，一上来我就必须指出，记号 $RR^n$ 可能有两种含义：
#definition[
  视上下文而定，$RR^n$ 可能指以下两者之一：

  - 长度为 $n$ 的向量，例如向量 $vec(pi, 5)$ 是 $RR^2$ 中的一个向量。
  - $n$ 维空间中的点，例如 $(sqrt(2), 7)$ 是 $RR^2$ 中的一个点。
]

为了绕开 $RR^n$ 有两种可能含义这一尴尬之处，
本书对变量命名采用以下约定：

#typesig[
- 像 $bf(u)$ 和 $bf(v)$ 这样的粗体小写字母用于表示向量。
  当我们画出向量的图像时，总是把它们画成*箭头*。

- 像 $P$ 和 $Q$ 这样的大写字母用于表示点。
  当我们画出点的图像时，总是把它们画成*圆点*。

- 有时，如果我们需要指代从点 $P$ 出发、到 $Q$ 结束的箭头所表示的那个向量，
  我们把它记作 $arrow(P Q)$。
]

对于实际的元素，我们也会使用不同的记号：

#typesig[
- 向量要么写成 $vec(1,2,3)$ 这样的列向量形式，
  要么在列向量形式太高、放不下时写成 $chevron.l 1,2,3 chevron.r$ 这样的尖括号形式。
- 但点总是写成 $(1,2,3)$ 这样带圆括号的形式。
]

$RR^3$ 中有一些向量足够特殊，值得拥有自己的简写记号。
（记号 "$:=$" 的意思是“定义为”。）
#definition[
  在 $RR^2$ 中工作时，我们定义
  $ ee_1 := vec(1,0), #h(1em) ee_2 := vec(0,1), #h(2em) "以及" #h(2em) bf(0) := vec(0,0). $
]
#definition[
  在 $RR^3$ 中工作时，我们定义
  $ ee_1 := vec(1,0,0), #h(1em) ee_2 := vec(0,1,0), #h(1em) ee_3 := vec(0,0,1),
    #h(2em) "以及" #h(2em) bf(0) := vec(0,0,0). $
  在其他地方，你有时会看到用 $bf(i)$、$bf(j)$、$bf(k)$ 代替，
  但本书将始终使用 $ee_i$。
]

== [TEXT] 模长

#definition[
  向量的*模长*记作 $|bf(v)|$，
  对应于所画箭头的长度。
  它由勾股定理给出。

  - 在二维中：
    $ bf(v) = vec(x,y) ==> |bf(v)| := sqrt(x^2+y^2). $
  - 在三维中：
    $ bf(v) = vec(x,y,z) ==> |bf(v)| := sqrt(x^2+y^2+z^2). $

  在 $n$ 维中，若 $bf(v) = chevron.l a_1, ..., a_n chevron.r$，
  则模长为 $|bf(v)| := sqrt(a_1^2 + ... + a_n^2)$。
]

#typesig[
  模长 $|bf(v)|$ 的类型是标量。除非 $bf(v) = bf(0)$，它总是正的；
  在后一种情形下模长为 $0$。
]

== [TEXT] 方向与单位向量

记住，一个向量总是具有

- 一个*模长*，即它在图中箭头的长度，以及
- 一个*方向*，即箭头指向哪一边。

换言之，向量的几何图像总是携带两条信息。
（这里我设想我们把向量画成一个箭头，
一个端点在原点，并指向某个方向。）

在许多几何情境中我们只关心方向，
而想忽略模长。
这时有一种约定就是把模长直接取为 $1$：
#definition[
  一个*单位向量*是指模长为 $1$ 的向量。
]
因此我们用单位向量的概念来刻画方向。
于是在 $RR^2$ 中，$vec(1,0)$ 被视为“正东”，
$vec(-1,0)$ 是“正西”，而 $vec(0,1)$ 是“正北”，
$vec(1/sqrt(2), 1/sqrt(2))$ 是“东北”。

#definition[
  给定 $RR^n$ 中除零向量以外的任意向量 $bf(v)$，
  *沿 $bf(v)$ 的方向*是单位向量
  $ bf(v) / (|bf(v)|) $
  即与 $bf(v)$ 指向相同的那个单位向量。
]
（取决于你参考的是哪本书，
更拘泥的作者可能会写“$bf(v)$ 方向上的单位向量”，
甚至“与 $bf(v)$ 同向的单位向量”，
而不是“沿 $bf(v)$ 的方向”。
这打起来太长了，所以我采用了更短的措辞。
我想大家都知道你是什么意思。）

我们将避免提及零向量 $bf(0)$ 的方向，那没有意义。
（如果你试图在这里套用公式，会得到除以 $0$，
因为 $bf(0)$ 是唯一模长为 $0$ 的向量。）
如果你需要这个说法，约定是它具有*每一个*方向。

#typesig[
  若 $bf(v)$ 是 $RR^n$ 中的非零向量，
  则沿 $bf(v)$ 的方向是 $RR^n$ 中的一个（单位）向量。
]

#example[
  我们先在 $RR^2$ 中做几个例子，这样就能画些图。

  - 沿向量 $vec(1,0)$、$vec(5,0)$ 或 $vec(1337,0)$ 的方向
    都是 $vec(1,0)$，即视作*正东*。

  - 而沿向量 $vec(-1,0)$ 或 $vec(-9,0)$ 的方向
    都是 $vec(-1,0)$，即视作*正西*。

  - 沿向量 $vec(0,-2)$、$vec(0,-17)$ 的方向
    都是 $vec(0,-1)$，即视作*正南*。
]

#example[
  那么沿 $vec(3, -4)$ 的方向呢？
  我们需要先求出这个向量的模长，才能把它按比例缩小。
  当然，这由勾股定理给出：$ lr(|vec(3,-4)|) = sqrt(3^2+4^2) = 5. $
  所以沿 $vec(3,-4)$ 的方向就是
  $ 1/5 vec(3,-4) =  vec(3 slash 5, -4 slash 5). $
  见 @fig-vec-3-4。这个方向介于正南与东南之间。
]

#figure(
  image("figures/vectors-vec-3-4.svg", width: auto),
  caption: [沿 $vec(3,-4)$ 的方向（蓝色）是 $vec(3 slash 5, -4 slash 5)$（红色）。
  按定义，单位向量总是落在灰色圆（单位圆）上。],
) <fig-vec-3-4>

当像 @fig-vec-3-4 那样在二维图像 $RR^2$ 中画出时，
沿 $vec(x,y)$ 的方向这一概念*几乎*就像
高中代数中斜率 $y / x$ 的概念
（即 @fig-vec-3-4 中蓝色射线的斜率）。
但我们的方向概念比只用蓝色射线的斜率更灵活，
原因有以下几点。

- 方向的概念能区分 $vec(3,-4)$（指向东南）
  与 $vec(-3,4)$（指向西北）。
  斜率做不到；它会把这两者都判定为“斜率 $-4/3$”。
- 正北与正南方向 $vec(0,1)$ 和 $vec(0,-1)$ 的斜率
  会因除以零而无定义，
  所以你总得操心这个额外的边界情形。
  用单位向量，正北与正南就不会带来额外的麻烦。
- 我们的方向定义在更高维的 $RR^3$ 中也适用。
  那里没有斜率的好类比；
  当 $n >= 3$ 时，单个数无法有效地刻画 $RR^n$ 中的方向概念。

#example[
  沿三维向量 $vec(12, -16, 21)$ 的方向
  是 $ vec(12 slash 29, -16 slash 29, 21 slash 29). $
  看看你能否弄明白 $29$ 是从哪里来的。
]

== [RECIPE] 面积与体积 <sec-det-area-vol>

若 $bf(v)_1 = vec(x_1, y_1)$ 与 $bf(v)_2 = vec(x_2, y_2)$ 是
画成具有共同起点的箭头的向量，
则 $bf(v)_1$、$bf(v)_2$ 以及它们的和 $bf(v)_1 + bf(v)_2$
与 $bf(0)$ 一起在平面中构成一个平行四边形，
如 @fig-parallelogram 所示。

#figure(
  image("figures/vectors-area.svg", width: auto),
  caption: [$RR^2$ 中的向量加法。],
) <fig-parallelogram>

下面的定理是正确的，但我们无法在 18.02 中证明它。

#recipe(title: [平行四边形面积的方法])[
  由 $bf(v)_1 = vec(x_1, y_1)$ 与 $bf(v)_2 = vec(x_2, y_2)$ 所构成的
  平行四边形的有向面积等于
  $ detmat(x_1, x_2; y_1, y_2) = x_1 y_2 - x_2 y_1. $
]

对于 $RR^3$ 中三个向量构成的平行六面体#footnote[我实在讨厌拼这个词。]
也有类似的定理；见 @fig-parallelepiped。

#recipe(title: [平行六面体体积的方法])[
  由 $bf(v)_1 = vec(x_1, y_1, z_1)$、
  $bf(v)_2 = vec(x_2, y_2, z_2)$、
  $bf(v)_3 = vec(x_3, y_3, z_3)$
  所构成的平行六面体的有向体积
  等于
  $ detmat(x_1, x_2, x_3; y_1, y_2, y_3; z_1, z_2, z_3). $
]

#figure(
  image("figures/vectors-parallelepiped.svg", width: auto),
  caption: [$RR^3$ 中三个向量构成一个平行六面体。],
) <fig-parallelepiped>

你可能已经注意到，“有向”这个词溜到了“面积”和“体积”前面。
那是什么意思呢？
如果你只关心面积或体积本身的大小，
那对你来说无所谓；你只需取行列式的绝对值即可。
但这个符号还携带了一点额外的信息。

- 在二维中，考虑 $bf(v)_1$ 与 $bf(v)_2$ 之间的角，
  取值在 $0 degree$ 与 $180 degree$ 之间。
  若该角从 $bf(v)_1$ 到 $bf(v)_2$ 是逆时针方向，
  我们认为符号为 $+$，
  （就像 @fig-parallelogram 中的例子那样），否则为负。
  因此在 @fig-parallelogram 中，我们有
  $ detmat(x_1, x_2; y_1, y_2) = +"面积", #h(1em)
    detmat(x_2, x_1; y_2, y_1) = -"面积". $

- 在三维中，约定遵循右手定则：
  设有按此顺序给出的向量 $bf(v)_1$、$bf(v)_2$、$bf(v)_3$。
  把右手指从 $bf(v)_1$ 弯向 $bf(v)_2$；
  那么若你的拇指指向 $bf(v)_3$ 的方向
  （例如 @fig-parallelepiped 中那样），有向体积为正，否则为负。

#digression[
  如果你对这些结果的证明
  及其 $n$ 维推广感兴趣，所需的工具是*楔积*（wedge product），
  它记作 $ and.big^k (RR^n). $
  这远远超出 18.02 的范围，
  但在我的 #link("https://web.evanchen.cc/napkin.html")[Napkin] 第 12 章中有记载，
  供想了解它的人阅读。

  或者，据我所知，Wikipedia 和 Axler#footnote[他
    有一篇论文叫 #link("https://www.axler.net/DwD.html")[Down with Determinants!]，
    我很赞赏。
  ] 等人，
  把行列式定义为
  $RR^n$ 中列向量的 $n$ 元组上唯一的、对单位阵取值 $1$ 的
  多重线性交错映射。
  这个定义可行，能让你推导出行列式的公式，
  也给了你一个理由相信它应当与你的面积和体积概念吻合。
  它大概也比楔积更容易理解。
  然而从长远看，我认为楔积更灵活，
  尽管它们需要长得多的时间来建立。
]

== [EXER] 习题

#exer[
  计算单位向量，使其方向沿向量
  $ vec(-0.0008 pi, -0.0009 pi, -0.0012 pi). $
] <exer-vectors-dir>

#exer[
  若 $A$ 是一个行列式为 $2$ 的 $3 times 3$ 矩阵，$det(10 A)$ 可能取哪些值？
] <exer-vectors-scaledet>

#exer[
  求实数 $a$，使得点 $(0,0,0)$、
  $(1,0,1)$、$(0,1,2)$ 与 $(1,2,a)$ 全都落在同一个平面上。
] <exer-vectors-coplanar>
