#import "@local/evan-zh:1.0.0":*

= Charlie 部习题解答 <ch-sol-charlie>

== @exer-complex-101-401-901 的解答（$101 dot 401 dot 901$）

#recall-thm(<exer-complex-101-401-901>)

思路是考虑复数
$ (10+i)(20+i)(30+i) = (199 + 30i)(30+i) = 5940 + 1099 i. $
因此一个可能的取法是 $ #boxed[$ (a,b) = (5940, 1099). $] $

== @exer-arctan-123 的解答（$arctan 1/1 +arctan 1/2 +arctan 1/3$）

#recall-thm(<exer-arctan-123>)

答案是 $pi / 2 = 90 degree$。
要点在于每个反正切值都是 $(0, 90 degree)$ 内的锐角，
而这些角之和将等于复数
$ z = (1+i)(2+i)(3+i). $
的辐角。
也就是说，$1+i$、$2+i$、$3+i$ 的辐角
分别是 $arctan 1/1$、$arctan 1/2$ 与 $arctan 1/3$，
因此 $z$ 的辐角就是这三个锐角之和。

然而，我们可以直接相乘算出 $z$：
$ z = (1+i)(2+i)(3+i) = (1+3i)(3+i) = 10i. $
因为这个数是纯虚数，所以它的辐角正如所需为 $90 degree$。

== @exer-mt1p1 的解答（向平面投影）

#recall-thm(<exer-mt1p1>)

*答案*：$vec(1/2, 3/2, -1)$。

#h3[第一种做法：用向量投影]

之前你要计算的是一个向量到平面的距离。
本题只在此基础上多一步：
你需要再沿法向量的那个倍数平移。
见图 @fig-bonus1p1，其中 $bf(a)$ 表示所求的答案。
为完成计算，令 $bf(v) = vec(4,5,6)$，$bf(n) = vec(1,1,2)$。
标量分量为
$ op("comp")_(bf(n)) (bf(v)) = (bf(v) dot bf(n)) / (|bf(n)|) = (21) / sqrt(6). $
向量投影则为
$ (op("comp")_(bf(n)) (bf(v))) (bf(n))/(|bf(n)|)
  = 21/sqrt(6) vec(1,1,2)/sqrt(6)
  = vec(7/2,7/2,7). $
那么所求的投影就是
$ bf(v) - op("proj")_(bf(n)) (bf(v)) = vec(1/2, 3/2, -1). $

#figure(
  image("figures/bonus1p1.svg", width: 70%),
  caption: [向平面投影。],
) <fig-bonus1p1>

#h3[第二种做法：只用法向量（不涉及投影）]

你们中很多人觉得向量投影不自然（我当然也这么觉得）。
所以或许更容易的做法是，想象把 $bf(v)$ 沿 $bf(n) = vec(1,1,2)$ 平移_某个_倍数，
然后再求出这个倍数是多少。

具体来说，我们要找#footnote[与第一种解法相比，
  这里的 $t$ 恰好等于
  $ t = (op("comp")_(bf(n)) (bf(v))) /(|bf(n)|). $
  但第二种解法背后的想法是：你并_不需要知道_
  $t$ 的几何公式是什么。
  你只需断言 $bf(a)$ 落在 $x+y+2z=0$ 上，就可以间接解出 $t$。]
一个实数 $t in RR$，使得向量
$ bf(a) = bf(v) - t bf(n) = vec(4-t, 5-t, 6-2t) $
落在平面 $x+y+2z = 0$ 上。
但其实只要把这个 $bf(a)$ 代入平面的方程，我们就能解出 $t$：
$ (4-t) + (5-t) + 2(6-2t) = 0 ==> 21 - 6t = 0 ==> t = 7/2. $
因此答案是
$ bf(a) = vec(4-7/2, 5-7/2, 6-2(7/2)) = vec(1/2, 3/2, -1). $

== @exer-mt1p2 的解答（几何解释）

#recall-thm(<exer-mt1p2>)

*答案*：量
$ 1/6 |arrow(D A) dot (arrow(D B) times arrow(D C))|. $
等于四面体 $A B C D$ 的体积。

一般来说，四面体的体积是由 $arrow(D A)$、$arrow(D B)$、$arrow(D C)$
构成的平行六面体体积的 $1/6$。
所以我们将证明
$ |arrow(D A) dot (arrow(D B) times arrow(D C))| $
给出的正是那个平行六面体的体积。
下面是两种证明它的方法。

#h3[第一种做法：用坐标]

令 $D = (0,0,0)$，$A = (x_A, y_A, z_A)$，$B = (x_B, y_B, z_B)$，$C = (x_C, y_C, z_C)$。
那么展开叉积得到

$ (x_A ee_1 + y_A ee_2 + z_A ee_3) dot
  detmat(ee_1, ee_2, ee_3; x_B, y_B, z_B; x_C, y_C, z_C). $

如果你想一想，用公式计算这个行列式
再配合点积会得到什么，你会发现它其实正好就是
$ detmat(x_A, y_A, z_A; x_B, y_B, z_B; x_C, y_C, z_C) $
这正是平行六面体的体积。

#h3[第二种做法：用几何图景]

叉积 $arrow(D B) times arrow(D C)$ 是一个向量，它的模长等于
由 $arrow(D B)$ 与 $arrow(D C)$ 构成的平行四边形的面积。
该叉积与 $arrow(D A)$ 的点积等于
$A$ 到平面 $B C D$ 的_高_乘以这个面积，
而体积就是高乘以面积。
见下面这张来自 #url("https://en.wikipedia.org/wiki/Triple_product") 的图
（在维基百科的图中，$bf(a)$ 表示我们的 $arrow(D A)$，以此类推）。

#figure(
  image("media/parallelepiped.png", width: 40%),
  caption: [取自维基百科的三重积图片。],
)

== @exer-mt1p3 的解答（投影的行列式）

#recall-thm(<exer-mt1p3>)

*答案*：0，无论选取哪个平面 $cal(P)$。

#h3[第一种做法：用基向量]

设 $ee_1$, $ee_2$, $ee_3$ 为三个基向量。那么：

- 矩阵 $M$ 是把 $f(ee_1)$、$f(ee_2)$、$f(ee_3)$ 拼在一起得到的。
- 我断言向量 $f(ee_1)$、$f(ee_2)$、$f(ee_3)$ 线性相关。
  毕竟按定义它们都包含在二维平面 $cal(P)$ 中，
  而平面中的三个向量不可能线性无关。
- 所以行列式等于零（这条定理正是我们用来
  判断向量是否线性无关的判据之一）。

#h3[第二种做法：用特征向量]

设 $bf(n)$ 是 $cal(P)$ 的任意非零法向量。
那么 $f(bf(n)) = bf(0)$，所以 $bf(n)$ 是特征值为 $0$ 的特征向量。
由于行列式等于特征值之积，行列式也必定为 $0$。

#h3[第三种做法：换坐标]

这种做法要求你知道这样一个事实：把矩阵用新的基改写，
行列式不变。

设 $bf(n)$ 是 $cal(P)$ 的任意非零法向量。
再取两个与 $bf(n)$ 垂直、且张成 $cal(P)$ 的单位向量 $bf(b)_1$ 与 $bf(b)_2$。
那么 $bf(b)_1$、$bf(b)_2$ 与 $bf(n)$ 线性无关且张成整个空间，即构成 $RR^3$ 的一组基。
所以我们可以换用它们来作坐标。

我们知道
$
  M(bf(b)_1) &= bf(b)_1 \
  M(bf(b)_2) &= bf(b)_2 \
  M(bf(n)) &= bf(0).
$

如果我们把 $M$ 写成_在这个新基_ $chevron.l bf(b)_1, bf(b)_2, bf(n) chevron.r$ 下的矩阵
（而不是通常的基），我们会得到
矩阵
$ M = mat(1,0,0;0,1,0;0,0,0) $
它的行列式为 $0$。

#remark[
  事实上，如果你还知道把 $M$ 用不同的基改写时迹不变，
  这种做法还说明 $M$ 的迹总是恰好等于 $1+1+0 = 2$，
  无论选取哪个平面 $cal(P)$。
]

== @exer-mt1p4 的解答（垂直的单位向量）

#recall-thm(<exer-mt1p4>)

*答案*：$|bf(a) times bf(v)| = 3$ 且 $|bf(b) times bf(v)| = 2$。

由于 $bf(v)$ 包含在 $bf(a)$ 与 $bf(b)$ 的张成中，
我们只需关注这两个互相垂直的单位向量所张成的平面。
所以几何图景是：以 $bf(a)$ 与 $bf(b)$ 为基，
$bf(v)$ 可以画在一个矩形中，如图所示。
因为 $bf(v) dot bf(a) = 2$ 且 $bf(v) dot bf(b) = 3$，这个矩形是 $2$ 乘 $3$ 的。

#figure(
  image("figures/bonus1p4.svg", width: 30%),
  caption: [在 $bf(a)$ 与 $bf(b)$ 的张成中画出 $bf(v)$。],
)

现在，叉积 $bf(a) times bf(v)$ 的模长应当等于
由 $bf(a)$ 与 $bf(v)$ 构成的平行四边形的面积，即 $3$
（因为这个平行四边形的底为 $|bf(a)|=1$，高为 $|bf(v) dot bf(b)|=3$）。
类似地，$bf(b) times bf(v)$ 的模长为 $2$。

== @exer-mt1p5 的解答（矩阵的迹）

#recall-thm(<exer-mt1p5>)

*答案*：$0$。

可以有几种做法。
前两种说明如何求出矩阵 $M$ 的四个元素。
后两种则完全绕开这一点，说明这个矩阵的迹其实总是 $0$。

#h3[第一种做法：暴力计算]

就像我 R04 讲义中的随堂测验一样，我们将设法算出 $M vec(1,0)$ 与 $M vec(0,1)$。
我们要找常数 $c_1$ 与 $c_2$，使得
$c_1 vec(4,7) + c_2 vec(5,9) = vec(1,0)$。

- 用你喜欢的方法解方程组 $4c_1 + 5c_2 = 1$ 与 $7c_1 + 9c_2 = 0$，
  得到系数 $c_1 = 9$ 与 $c_2 = -7$，即

  $ 9 vec(4,7) - 7 vec(5,9) = vec(1,0). $

  由此我们得到

  $ M( vec(1,0) ) = 9 M( vec(4,7) ) - 7 M( vec(5,9) ) = 9 vec(5,9) - 7 vec(4,7) = vec(17, 32). $

- 通过解类似的方程组，我们可以得到恒等式
  $ -5 vec(4,7) + 4 vec(5,9) = vec(0,1), $
  从而：

  $ M( vec(0,1) ) = -5 M( vec(4,7) ) + 4 M( vec(5,9) ) = -5 vec(5,9) + 4 vec(4,7) = vec(-9, -17). $

把它们拼起来 $ M = mat(17,-9;32,-17). $
因此迹为 $17 + (-17) = 0$。

#h3[第二种做法：逆矩阵]

我们可以把已知的两个方程合并写成
$ M mat(4,5;7,9) = mat(5,4;9,7). $
因此也可以通过乘以其逆矩阵来还原 $M$：
$ M = mat(5,4;9,7) mat(4,5;7,9)^(-1)
  = mat(5,4;9,7) 1/(4 dot 9-7 dot 5) mat(9,-5;-7,4) = mat(17,-9;32,-17). $
（当然，我们得到的 $M$ 的元素与上一种做法相同。）
同样，迹为 $17 + (-17) = 0$。

#h3[第三种做法：猜特征向量与特征值]

令 $bf(b)_1 = vec(4,7)$，$bf(b)_2 = vec(5,9)$。
把已知的两个方程相加、相减得到
$
  M(bf(b)_1 + bf(b)_2) &= bf(b)_1 + bf(b)_2 \
  M(bf(b)_1 - bf(b)_2) &= -(bf(b)_1 - bf(b)_2).
$

所以 $bf(b)_1 pm bf(b)_2$ 是特征值为 $pm 1$ 的特征向量。
由于 $M$ 是 $2 times 2$ 矩阵，至多有两个特征值：我们已经把它们全找到了！

$M$ 的迹等于特征值之和。
于是答案就是 $1 + (-1) = 0$。

#h3[第四种做法：换坐标]

这种做法要求你知道这样一个事实：把矩阵用新的基改写，
迹不变。

由于 $bf(b)_1 = vec(4,7)$ 与 $bf(b)_2 = vec(5,9)$ 构成 $RR^2$ 的一组基，
我们可以换用 $bf(b)_i$ 来作坐标。
在这种情况下，
$ M(bf(b)_1) = bf(b)_2 " 与 " M(bf(b)_2) = bf(b)_1. $

如果我们把 $M$ 写成_在这个新基_ $chevron.l bf(b)_1, bf(b)_2 chevron.r$ 下的矩阵
（而不是通常的基），我们会得到矩阵
$ M = mat(0,1;1,0) $
它的迹为 $0+0=0$。

== @exer-mt1p6 的解答（复三角形）

#recall-thm(<exer-mt1p6>)

*答案*：$(3 sqrt(3))/4 root(3, 61)$。

我们先把复数 $5 + 6 i$ 化为极坐标形式。
$5 + 6 i$ 的模 $r$ 为：
$ r = lr(|5 + 6 i|) = sqrt(5^2 + 6^2) = sqrt(25 + 36) = sqrt(61). $

辐角 $theta$ 是某个我们不打算使用其精确值的角：
$theta = arg (5 + 6 i) = tan^(- 1) (6 / 5)$。

现在为求 $z^3 = 5 + 6 i$ 的立方根，我们使用极坐标形式：
$ z = root(6, 61) (cos (frac(theta + 2 k pi, 3)) + i sin (frac(theta + 2 k pi, 3))) $
其中 $k = 0 , 1 , 2$。
这给出对应不同 $k$ 值的三个根。

这看起来像一个以原点为中心的等边三角形，
其中从原点出发的每条辐条长度都是 $s$，而
$ s = root(6, 61). $
见图 @fig-bonus1p6。

#figure(
  image("figures/bonus1p6.svg", width: 60%),
  caption: [$z^3 = 5+6i$ 的三个解],
) <fig-bonus1p6>

如果我们用上面三个箭头把这个等边三角形切开，
就得到三个顶角为 $120 degree$ 的小等腰三角形。
每个三角形的面积都是 $s^2/2 sin(120 degree)$。

所以最终答案是
$ 3 dot root(3,61)/2 dot sin(120 degree) = (3 sqrt(3))/4 root(3, 61). $
