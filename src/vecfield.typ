#import "@local/evan-zh:1.0.0":*

= 向量场

== [TEXT] 向量场

在 Golf 部中，我们只考虑了标量值函数的积分。
然而，在 India 部与 Juliett 部中，我们将遇到*向量场*（vector field），
它是“输入点、输出向量”的函数的另一个名字。
#definition[
  $RR^n$ 上的*向量场*是一个函数 $bf(F) : RR^n -> RR^n$，
  它把每个点 $P in RR^n$ 对应到向量 $bf(F)(P) in RR^n$。
]
作为对比，对于旧的那类函数 $f : RR^n -> RR$，我们可能会用*标量场*这个词，
它们在每个输入点处给出一个数而不是一个向量。

你其实以前已经遇到过很多向量场：
#example[
  每个梯度都是一个向量场的例子！
  也就是说，若 $f : RR^n -> RR$，那么 $nabla f$ 是 $RR^n$ 上的一个向量场。
]
这种情形如此重要，以至于它有一个专门的词：
#definition[
  $RR^n$ 上的向量场，若恰好等于某个函数 $f : RR^n -> RR$ 的 $nabla f$，
  则称为*保守的*（conservative）。
]
在 India 部与 Juliett 部中，我们也会遇到不是保守的向量场。
#typesig[
  对于独立的向量场，我们总是用 $bf(F)$ 这样的大写粗体字母来表示它们。
  话虽如此，记住 $nabla f$ _也_是一个向量场。
  这就是为什么算子 $nabla$ 本身被排版得稍微粗一点。

  和梯度一样，你应当把 $bf(F)$ 的输入画成_点_（圆点），
  而把输出画成_向量_（箭头）。不要把它们弄混。
]

对于二维与三维向量场，我们常常写
$ bf(F) = vec(p(x,y), q(x,y)) quad bf(F) = vec(p(x,y,z), q(x,y,z), r(x,y,z)). $
我想其他资料常常改用 $P$、$Q$、$R$，
但我现在要用它们表示点，所以我用小写字母。

== [TEXT] 我们如何想象一个向量场？

想象向量场的方式有很多，尤其是在物理中。
为了保持一致，我会选定其中_一种_框架，并把所有例子都用它来写。
所以*在我的书里，所有例子在性质上都是水中的*；
但如果你不会游泳#footnote[不过，MIT 不是要求你必须通过游泳测试吗？]，
你完全可以随意替换成你自己喜欢的。
想象一个电场。或者外太空中的黑洞。
或者大气中的气流。什么对你有用就用什么！

总之，在我的书里，我们使用下面这幅图景：
#idea[
  想象 $RR^2$ 或 $RR^3$ 中一片流动的水体（海洋、河流、漩涡、喷泉等）。
  于是在任意点 $P$ 处，我们画一个小箭头 $bf(F)(P)$，
  它表示点 $P$ 处水的方向与速度。
  你可以想象，如果在点 $P$ 处放一个小球，
  水流会沿着那个箭头推动小球。
]

听起来很像梯度，对吧？
确实，保守向量场是向量场中很大的一族，
因此我们应当预期它们能相当完美地契合这幅图景。
但保守向量场的特点在于：$nabla f$ 作为一个向量场，
总是_朝着_使 $f$ 的值变大的方向奔涌。
而一般的向量场则可能，比如说，绕圈流动。
让我们把这些例子用水中运动的说法表述出来。

#example(title: [保守向量场的例子：顺河而下])[
  让我们想象有一条水流湍急的河。
  我们要做一个重要的假设：这条河只朝一个方向流：
  也就是说，沿着水流走，你永远不会回到起点。
  在现实中，如果河流沿着山向下流，就常常出现这种情况，
  于是当你顺流而下时，你在不断降低海拔。

  如果这样做，你可以如下定义一个“下游函数”$f : RR^3 -> RR$：
  对河中每个点 $P$，$f(P)$ 度量你处在多下游的位置。
  例如，如果这条河有源头，也许我们可以把那里的 $f$ 值取为零，
  于是当你离源头越远，$f$ 就越大，
  在河口处取到最大值。
  （对于山地河流，$f$ 也可以理解为随海拔递减。）

  那么对应于这条河的向量场就是梯度 $nabla f$。
  记住，梯度 $nabla f$ 告诉你朝哪个方向移动能使 $f$ 增大。
  而如果你把球扔进河里，它的运动可以简单地描述为：
  球向下游移动。
]

#figure(
  image("media/waterfall.png", width: 50%),
  caption: [一条从瀑布底部流出的河。
    红色箭头沿梯度的方向指向。
    （用 ProCreate 绘制。）],
)

#example(title: [非保守向量场的例子：漩涡])[
  现在换一种想象，你有一个漩涡。
  如果你把球扔进去，它会围着漩涡的涡心一圈圈转。
  这看起来和河流完全不像！
  如果是河流，你永远不会指望球过一会儿又回到同一个点，
  因为它一心要顺流而下。
  但在漩涡里，你会一遍又一遍地绕圈。

  如果你把对应于漩涡的向量场画出来，
  它看起来像许多由小箭头构成的同心圆环。
  这就是一个非保守向量场的例子。
]

#figure(
  image("media/whirlpool.png", width: 50%),
  caption: [一个漩涡。
    我们一圈又一圈地转。不是保守向量场。
    （用 ProCreate 绘制。）],
)

== [TEXT] 如何画向量场

如果你确实拿到了 $bf(F)(x,y)$ 的公式，
你可以按下面的步骤画出对应的向量场的草图。

#recipe(title: [画向量场草图的方法])[
  要画出向量场的示意图：
  1. 选取一堆你想画箭头的点 $P$。
  2. 对每个 $P$，从 $P$ 出发画一个小箭头，方向与 $bf(F)(P)$ 相同。
    （为了让示意图易读，你通常会把箭头的模长按比例缩小。）
]

在实践中，为了让图看起来不荒唐，
人们通常会把箭头画得比它们真实的尺寸小得多。
例如，如果你算出 $bf(F)(10,10) = vec(2,3)$，
那么严格说来，从 $(10,10)$ 出发的箭头应当一直伸到 $(12,13)$。
但如果你这么做，你的示意图就会看起来很荒唐。
所以人们通常会把示意图中箭头的尺寸调成_很小的_箭头，
方向仍然正确，但模长小得多。
对于示意图来说，通常更重要的是箭头的_相对_大小正确；
按真实比例画出绝对值是不必要的。

经典且容易画的例子是 $bf(F) = vec(x,y)$，
其中对每个点 $P$，箭头都笔直地背离原点。
见 @fig-vecfield-id。
顺便说明，在这幅图（以及本节的其他图）中，
所有小箭头的长度都恰好按真实长度的 30% 缩放。

#figure(
  image("figures/vecfield-ex.svg", width: auto),
  caption: [每门课上都会画的第一幅向量场的图，
    老朋友 $bf(F)(x,y) = vec(x,y)$。],
) <fig-vecfield-id>

好，这里还有八幅图用来训练你的直觉。
对每一幅，试着挑几个像 $(2,0)$ 或 $(3,-1)$ 这样的点，
并验证从该点出发的箭头指向与你预期的一致。

#figure(
  image("figures/vecfield-ex2.svg", width: auto),
  caption: [$RR^2$ 中另外四个向量场的例子。
    例子 $bf(F)(x,y) = vec(x,0)$ 只有水平箭头，
    它们随 $|x|$ 增大而变长。
    例子 $bf(F)(x,y) = vec(0,x)$ 只有竖直箭头，情况类似。
    例子 $bf(F)(x,y) = vec(y,-x)$ 呈漩涡状。
    例子 $bf(F)(x,y) = vec(x,y/3)$ 是倾斜的。
  ],
)

#figure(
  image("figures/vecfield-ex3.svg", width: auto),
  caption: [$RR^2$ 中另外四个向量场的例子。
    向量场 $bf(F)(x,y) = vec(1,1)$ 是常值的。
    向量场 $bf(F)(x,y) = vec((x+y)/2,(x-y)/2)$ 有一些倾斜的箭头。
    向量场 $bf(F)(x,y) = vec(x,x)$ 的所有箭头都成 $45 degree$ 角，
    并随 $|x|$ 增大而变长。
    而那个古怪的 $bf(F)(x,y) = vec(sin(pi/2 x), sin(pi/2 y))$
    则呈现出振荡的行为。
],
)

== [TEXT] 向量场上积分的预览

正如我们在 @sec-warning-trio 中提到的，
我们在 Golf 部中遇到的线积分与面积分
（其被积对象是标量值函数）其实是无人问津的丑小鸭。
在大多数情况下，如果你要做线积分或面积分，
你真正想要的是*向量值的线积分与面积分*，
也就是对整片场做线积分或面积分。

这时候类型签名就变得疯狂了。

为了让它哪怕稍微好记一点，
我要做的是，把之前的 @poster-triangle
用对应于我们可能对向量场积分的各种情形的图来扩充。
这张新图表也可以下载为
#link("https://web.evanchen.cc/textbooks/poster-stokes.pdf")[大尺寸 PDF 版本]。

#figure(
  image("figures/integrals-stokes.svg", width: 100%),
  caption: [
    用向量场升级后的 @poster-triangle。
    下载地址 #url("https://web.evanchen.cc/textbooks/poster-stokes.pdf")。
  ]
) <poster-stokes>

与旧版本相比，@poster-stokes 有两个新特点：
三幅紫色的图与六个红色箭头。
我们会在接下来几节中把它们全部定义，所以现在只说几句。

=== 三幅紫色的图

有*三幅新的紫色图*：它们是*功*（在 India 部中讨论）
与*通量*（在 Juliett 部中讨论）。
基本上，这就是我们对向量场积分的仅有的两种情形：

- 我们有一条沿向量场的路径，想度量向量场_沿_该路径的_功_
  （物理意义上的）；这是 India 部的重点。
- 我们在三维向量场中有一个曲面，想度量向量场_穿过_该曲面的_通量_；
  这是 Juliett 部的重点。

这些术语将在下一章定义。
#typesig[
  新的紫色东西_仍然_全都是标量，
  即功与通量都是数，而不是向量。
]

正如我们在 @sec-warning-trio 中提到的，
紫色的图基本上会取代对应的绿色图。
反过来，向量场通常只会在
紫色图所描述的情形中被积分。
这在 @table-purples 中作了总结。

#figure(
  table(
    columns: 3,
    align: left,
    table.header([], [对于标量场 $f : RR^n -> RR$], [对于向量场 $bf(F): RR^n -> RR^n$]),
    [*18.01 积分*], [✅ 一直在用], [❌ 在 18.02 中从不使用],
    [*线积分*], [🔵 只用于弧长], [✅ 一直在用（功）],
    [*二重/面积分*], [✅ 一直在用], [❌ 在 18.02 中从不使用],
    [*面积分*], [🔵 只用于表面积], [✅ 一直在用（通量）],
    [*三重/体积分*], [✅ 一直在用], [❌ 从不使用（除了在 @eqn-gravity 中）],
  ),
  caption: [各种积分分别用来做什么。
    带 ❌ 标记的积分在 18.02 中从不出现，
    所以它们也不会出现在图表 @poster-stokes 中。
    （在 18.02 中，如果你需要一根金属丝或一块金属板的质量，
    线积分与面积分也可能用于标量场，但我认为这相当罕见。）],
  kind: table
) <table-purples>

这一点值得反复强调：
#idea[
  除了弧长与表面积之外，我们基本上不会使用标量场的积分。
  换言之，你可以大致忽略
  海报 @poster-stokes 中被紫色图取代的那些绿色图。
]

=== 六个红色箭头

还有*六个新的红色箭头*。
它们表示函数之间的变换：
一种取某类函数并用它构造出另一类函数的方式。

例如，我们讨论过的那个就是梯度 $nabla$：
如果你从一个标量值函数 $f : RR^n -> RR$ 出发，
梯度会造出一个向量场 $nabla f : RR^n -> RR^n$。
（在 $f : RR^1 -> RR$ 的情形中，$(dif f) / (dif x)$ 其实也就是梯度，
只是稍微退化一些。）

我们很快就会遇到另外三个变换：

- *二维旋度*，它把 $RR^2$ 上的向量场_变回_标量值函数；
- *三维旋度*，它把 $RR^3$ 上的向量场变成_另一个_向量场；
- *散度*，它把 $RR^3$ 上的向量场_变回_标量值函数。

== [TEXT] 斯托克斯定理的预告

所有红色箭头都有两个重要性质，我_现在_就告诉你。
因为我还没有怎么讲过任何红色箭头，
*你现在还无法理解这意味着什么*。
没关系。
等我们讲到每个红色箭头时，我们会逐个讨论这意味着什么。
然而，我想_提前_告诉你：每当我们遇到一个红色箭头，
都会有一个适用于它的斯托克斯定理情形。

#memo(title: [记住：两个红色箭头得到零])[
  在 @poster-stokes 中，
  如果你连续沿着两个红色箭头走，得到的是零。
]

#memo(title: [记住：18.02 版的广义斯托克斯定理])[
  在 @poster-stokes 中，任取六个红色箭头中的一个
  #text(rgb("ff0000"))[$X -> Y$]。
  设 $cal(R)$ 是一个紧区域。
  那么 $X$ 在 $cal(R)$ 的*边界*上的积分
  等于 $Y$ 在 $cal(R)$ 上的积分：
  $ integral_(op("boundary")(cal(R))) X = integral_(cal(R)) Y. $
]

实际上，让我用模糊而不精确的说法（我们以后再把它精确化），
对每一个红色箭头（共六个）告诉你广义斯托克斯定理说的是什么：

/ 求值 $->$ 18.01 积分:
  对于连接求值与 18.01 积分的 $(dif f)/(dif x)$ 箭头，
  它就是微积分基本定理。
  区域 $cal(R)$ 是线段 $[a,b]$，而边界是两个端点 $a$ 与 $b$。
  于是我们得到 18.01 中的_微积分基本定理_：
  $ f(b) - f(a) = integral_a^b (dif f) / (dif x) dif x. $

/ 求值 $->$ 线积分（$times 2$）:
  这样的红色箭头有两个，但两者的陈述是相同的。
  设 $bf(r)(t)$ 参数化一条连接点 $P$ 与 $Q$ 的路径
  （比如线段 $P Q$，或者某条更弯曲的路径）。
  区域 $cal(R)$ 就是这条路径，而边界是端点 $P$ 与 $Q$：
  于是我们得到_线积分的微积分基本定理_：
  $ f(Q) - f(P) = integral_(t="起始时刻")^("终止时刻") nabla f(bf(r)(t)) dot bf(r)'(t) dif t. $
  右端是 $nabla f$ 在路径 $bf(r)$ 上所做的功。

  如果你使用简写记号，把由 $bf(r)$ 形成的曲线记作 $cal(C)$，
  这可以改写为
  $ f(Q) - f(P) = integral_(cal(C)) nabla f dot dif bf(r). $

/ 线积分 $->$ 二重/面积分:
  现在设 $bf(F) : RR^2 -> RR^2$ 是一个向量场。
  记 $bf(F) = (p(x,y), q(x,y))$。
  设 $cal(R)$ 是某个二维区域，比如一个圆盘。
  再设 $cal(R)$ 的_边界_由曲线 $bf(r)(t)$ 参数化
  （例如圆盘的圆周）。
  那么_格林定理_说的是
  $ integral_(t="起始时刻")^("终止时刻") bf(F)(bf(r)(t)) dot bf(r)'(t) dif t
    = integral.double_(cal(R)) ((partial q) / (partial x) - (partial p) / (partial y)) dif x dif y. $
  右端那个奇怪的表达式 $(partial q) / (partial x) - (partial p) / (partial y)$
  称为二维标量旋度，但我们还没有定义这个术语。

  如果你像 @table-shorthand 中那样使用简写，这可以化简。
  设 $cal(C)$ 是由 $bf(r)$ 形成的曲线，
  这可以改写为
  $ integral_(cal(C)) bf(F) dot dif bf(r)
    = integral.double_(cal(R)) ((partial q) / (partial x) - (partial p) / (partial y)) dif A. $
  格林定理还有第二种形式，等我讲到时再给你看。

/ 线积分 $->$ 面积分:
  现在设 $bf(F) : RR^3 -> RR^3$ 是一个向量场。
  设 $bf(r)_2(u,v)$ 参数化某个二维曲面 $cal(S)$（比如一块金属板），
  再设这个曲面的_边界_由曲线 $bf(r)_1(t)$ 参数化
  （例如这块板的边缘）。
  那么_斯托克斯定理_的经典版本说的是
  $ integral_(t="起始时刻")^("终止时刻") bf(F)(bf(r)_1(t)) dot bf(r)'_1(t) dif t
    = integral.double_(cal(S)) (nabla times bf(F))(bf(r)_2(u,v)) dot lr(((partial bf(r)_2)/(partial u) times (partial bf(r)_2)/(partial v))) dif u dif v. $
  那个莫名其妙的表达式 $nabla times bf(F)$ 称为旋度，将在下一章 @ch-gcd 中定义。

  遵循 @table-shorthand 的简写版本要好读得多，
  因为简写 $bf(n) dif S$ 代表的是整块东西
  $lr(((partial bf(r)_2)/(partial u) times (partial bf(r)_2)/(partial v))) dif u dif v$。
  设 $bf(r)_1$ 对应的曲线记作 $cal(C)$。
  那么上面的方程一直压缩到
  $ integral_(cal(C)) bf(F) dot dif bf(r)_1 = integral.double_(cal(S)) (nabla times bf(F)) dot bf(n) dif S. $
  所以是的，这确实省下不少字符。

/ 面积分 $->$ 三重/体积分:
  现在设 $bf(F) : RR^3 -> RR^3$ 是一个向量场。
  设 $cal(T)$ 是某个三维立体（例如金属球）。
  再设 $cal(R)$ 的边界由某个二维曲面 $bf(r)(u,v)$ 参数化
  （例如金属球面），我们把它记作 $cal(S)$。
  那么_散度定理_说的是
  $ integral.double_(cal(S)) bf(F)(bf(r)(u,v)) dot lr(((partial bf(r))/(partial u)
    times (partial bf(r))/(partial v))) dif u dif v
    =  integral.triple_(cal(T)) (nabla dot bf(F))(x,y,z) dif x dif y dif z. $
  如果我们再次采用简写，这就简读作
  $ integral.double_(cal(S)) bf(F) dot bf(n) dif S = integral.triple_(cal(T)) nabla dot bf(F) dif V. $

  那个莫名其妙的表达式 $nabla dot bf(F)$ 称为散度，将在下一章 @ch-gcd 中定义。

再次说明，这些条目你现在还看不懂（除了第一条，
也就是 18.01 的微积分基本定理），
因为其中有若干未定义的术语。
不如把它当作你即将学到的所有定理陈述的一个模板：
#idea[
  海报 @poster-stokes 中的每个红色箭头都有一个与之对应的斯托克斯结果。
]
在进入接下来每个概念时都要记住这一点。

#figure(
  image("media/all-stokes.png", width: 80%),
  caption: [这一切都是斯托克斯定理。],
)

== [TEXT] 保持坚定

现在这些内容大概是让人应接不暇的，
而 @poster-stokes 看起来可能很吓人，
因为里面的信息太多了。
别担心，在这个学期剩下的时间里，
我们会一次一块地把 @poster-stokes 拆开来看。

- 在 @ch-gcd 中，我会告诉你如何计算 grad、curl、div 中的每一个。
  这一章里没有积分，所以如果你讨厌积分，你会喜欢这一章。

- 我会在 @ch-work 中开始讲功。
  我会定义它，然后展示它如何与微积分基本定理联系起来
  （后者是广义斯托克斯定理中的某些情形）。

- 然后 @ch-flux 会定义通量。
  这会让我们能够讨论广义斯托克斯定理剩下的那些情形。

== [EXER] 习题

#exer[
  做几次深呼吸，去摸摸草，好好喝一杯水，
  这样你就能看着 @poster-stokes 而不感到恐惧。
] <exer-touchgrass>

#exer[
  打印一份 @poster-stokes 的高分辨率版本
  （可在 #url("https://web.evanchen.cc/textbooks/poster-stokes.pdf") 下载）
  并把它挂在你的房间里。
] <exer-poster>
