#import "@local/evan-zh:1.0.0":*

= 等值线（又称等高线图） <ch-level>

== [TEXT] 等值线取代 $x y$ 图像

在高中和 18.01 中，你通常学到的做法是把单变量函数画在二维平面上，
所以 $f(x) = x^2$ 会被画成抛物线 $y = x^2$，依此类推。
你也许已经被反复灌输：$x$ 是输入，$y$ 是输出。

然而，对于 18.02，我们通常想用另一种方式来画
像 $f(x,y) = x^2+y^2$ 这样的函数的图像#footnote[
  这很像我们之前用对称形式画平面的做法。
  在高中数学里，你画的是单变量函数的二维图像，
  例如 $y = 2 x + 5$ 或 $y = x^2 + 7$。
  所以当我们改为把平面写成 $2 x + 5 y + 3 z = 7$
  而不是 $z = (7 - 2 x - 5 y) / 3$ 时，你也许觉得有点奇怪。
  但这种形式最终证明更好，因为它让我们能轻松读出法向量
  （这里就是 $chevron.l 2, 5, 3 chevron.r$）。
  这里的类比是相通的。
]，用的就是所谓的_等值线_（level curve）。

#definition[
  对任意数 $c$ 和函数 $f(x,y)$，
  值为 $c$ 的等值线是满足 $f(x, y) = c$ 的那些点所构成的图像。
]

与你习惯的做法相比，区别在于：

- 在高中和 18.01 中，变量 $x$ 和 $y$ 扮演不同的角色，
  其中 $x$ 代表输入，而 $y = f(X)$ 代表输出。
- 在 18.02 中，当我们画函数 $f(x,y)$ 时，
  $x$ 和 $y$ 都是输入；我们对它们一视同仁。
  同时，函数的_输出_并_没有_变量名。
  如果真的想提到它，我们有时会写 $f = 2$，作为
  “输出为 $2$ 的等值线”的简写。

用表格重述一遍：
#figure(
  table(
    columns: 2,
    align: left,
    table.header([18.01 $x y$ 图像], [18.02 等值线]),
    [$x$ 是输入], [两个变量都是输入],
    [$y$ 是输出], [输出没有变量名],
  ),
  kind: table,
  caption: [18.01 的 $x y$ 图像与 18.02 的等值线图的比较。],
)

我们给出一些例子。

#example(title: [例：$f(x,y) = y - x^2$ 的等值线])[
  要画函数 $f(x, y) = y - x^2$ 的等值线，
  我们先回顾：一条等值线对应那些使函数取某个常数值
  （记为 $c$）的点 $(x, y)$。
  对我们的函数来说，这就变成
  $ y - x^2 = c $
  整理得
  $ y = x^2 + c. $
  我们来讨论几个 $c$ 的取值。
  - 举例来说，若 $c = 0$，那么等值线上的一些点是
    $(-3,9)$、$(-2,4)$、$(-1,1)$、$(0,0)$、$(1,1)$、$(2,4)$、$(3,9)$，
    甚至还有 $(sqrt(5), 5)$ 和 $(sqrt(11), 11)$。
    你大概已经看出发生了什么：$y = x^2$ 恰好是你以前在 18.01 中
    见过的方程形式，所以你得到一条抛物线。
    （更一般地，如果你得到的是 18.01 形式的方程，其中 $y$ 是 $x$ 的函数，
    你就可以像以前那样把它画出来）。
  - 如果我们把值改成 $c = 2$，这个方程表示的是一族抛物线。
    例如，值为 $2$ 的等值线将是经过
    $(-2,6)$、$(-1,3)$、$(0,2)$、$(1,3)$、$(2,6)$ 等点的抛物线。

  一般来说，当 $c$ 变化时，等值线是沿 $y$ 轴
  上下平移的抛物线。
  这些曲线的形状由二次项 $x^2$ 决定，
  这说明所有等值线都有相同的基本“U”形。

  #figure(
    image("figures/level-curve1.svg"),
    caption: [$f(x,y) = y-x^2$ 的等值线。],
  )
]

#example(title: [例：$f(x,y) = x - y^2$ 的等值线])[
  我们来画 $f(x,y) = x-y^2$ 的等值线。
  这个例子与上一个完全相同，只是 $x$ 和 $y$ 的角色对调了。

  #figure(
    image("figures/level-curve2.svg"),
    caption: [$f(x,y) = x-y^2$ 的等值线。],
  )
]

#example(title: [例：$f(x,y) = x^2+y^2$ 的等值线])[
  我们来画 $f(x,y) = x^2+y^2$ 的等值线。
  对每个 $c$，我们想画出曲线
  $ x^2 + y^2 = c. $
  当 $c < 0$ 时，这条曲线上一个点都不出现；而当 $c = 0$ 时
  唯一的点是原点 $(0,0)$。
  对 $c > 0$，这个方程表示一族以原点 $(0, 0)$ 为圆心、
  半径为 $sqrt(c)$ 的圆。
  例如：

  - 对 $c = 1$，等值线是半径为 $1$ 的圆。
  - 对 $c = 4$，等值线是半径为 $2$ 的圆。
  - 对 $c = 9$，等值线是半径为 $3$ 的圆。

  随着 $c$ 增大，这些圆从原点向外扩张。
  这些同心圆就是函数 $f(x, y) = x^2 + y^2$ 的等值线。

  #figure(
    image("figures/level-curve3.svg"),
    caption: [$f(x,y) = x^2+y^2$ 的四条等值线。],
  )
]

#example(title: [例：$f(x,y) = |x| + |y|$ 的等值线])[
  我们来画 $f(x,y) = |x| + |y|$ 的等值线。
  要画值为 $c$ 的等值线，我们考虑的是
  $ |x| + |y| = c. $

  和之前一样，若 $c < 0$ 则完全没有点对 $(x,y)$，而 $c = 0$ 时只有一个点。

  这个方程表示一族多边形。
  具体来说，对给定的 $c$，
  满足这个方程的点构成一个以原点为中心的菱形。
  事实上，在第一象限（在那里绝对值不起任何作用），
  它表示连接 $(0,c)$ 与 $(c,0)$ 的线段。

  所以举例来说，

  - 当 $c < 0$ 时，没有点。
  - 对 $c = 0$，等值线就是点 $(0,0)$。
  - 对 $c = 1$，等值线是顶点为 $(1, 0)$、$(-1, 0)$、$(0, 1)$、$(0, -1)$ 的菱形。
  - 对 $c = 2$，等值线是更大的菱形，顶点为 $(2, 0)$、$(-2, 0)$、$(0, 2)$、$(0, -2)$。
  - 对 $c = 3$，菱形进一步扩张，顶点为 $(3, 0)$、$(-3, 0)$、$(0, 3)$、$(0, -3)$。

  随着 $c$ 增大，这些菱形向外扩张，保持形状而尺寸增大。
  每条等值线都是一个旋转 45 度的正方形。

  #figure(
    image("figures/level-curve4.svg"),
    caption: [$f(x,y) = |x|+|y|$ 的四条等值线。],
  )
]

== [RECIPE] 画等值线

尽管这一节被标为“方法”，但并没有
对每个函数都适用的简单办法。
*你必须根据给定的具体函数，因题制宜地处理*。
对于考试中会遇到的许多函数，这会相当容易。

总结一下步骤：
给定像 $f(x,y)$ 这样的显式函数和 $c$ 的值，
我们要尝试画出空间中所有满足 $f(x,y) = c$ 的点 $(x,y)$。
我们在上面给了三个例子，其中：

- $f(x,y) = y-x^2$ 的等值线很容易画，因为
  对任意给定的 $c$，方程就直接变成了 18.01 那样的 $x y$ 图像。
- $f(x,y) = x-y^2$ 的等值线与上一个例子类似，
  只是 $x$ 和 $y$ 的角色对调了。
- 要画 $f(x,y) = x^2+y^2$ 的等值线，
  你需要知道 $x^2 + y^2 = r^2$ 表示以 $(0,0)$ 为圆心、
  半径为 $r$ 的圆。
- 要画 $f(x,y) = |x| + |y|$ 的等值线，
  我们不得不因题制宜地在每个象限中分别讨论；
  在第一象限我们分析出得到的是一条直线，
  然后把同样的图像应用到其他象限，
  得到菱形的形状。

所以你可以看到，这确实取决于给定的具体 $f$。
如果你写下一个非常讨厌的函数，比如 $f(x,y) = e^(sin x y) + cos(x + y)$，
那大概就没有什么手工画等值线的简单办法了。

== [TEXT] 等值面其实是一回事，只是把两个变量换成三个

上面的一切其实都不依赖于恰好有两个变量。
如果我们有一个三变量函数 $f(x,y,z)$，
我们可以对值 $c$ 画出_等值面_，
方法是画出 $RR^3$ 中所有满足 $f(x,y,z) = c$ 的点。

#example(title: [例：$f(x,y,z) = x^2+y^2+z^2$ 的等值面])[
  若 $f(x,y,z) = x^2 + y^2 + z^2$，
  那么当 $c >= 0$ 时，值 $c$ 的等值面是半径为 $sqrt(c)$ 的球面。
  （当 $c < 0$ 时，等值面是空集。）
]

#example(title: [例：$f(x,y,z) = x + 2 y + 3 z$ 的等值面])[
  若 $f(x,y,z) = x + 2 y + 3 z$，
  则 $f$ 的所有等值面都是 $RR^3$ 中的平面，
  它们彼此平行，法向量为 $vec(1,2,3)$。
]

== [EXER] 习题

#exer[
  对下列函数画出若干取值下的二维等值线：

  - $f(x,y) = 3/2 x + y$
  - $f(x,y) = x y$
  - $f(x,y) = sin(x^2 + y^2)$
  - $f(x,y) = e^(y - x^2)$
  - $f(x,y) = max(x,y)$（即 $f$ 输出两个输入中较大的那个，
    所以例如 $f(3,5)=5$ 而 $f(2,-9)=2$）。
] <exer-level-curves>

#exerstar[
  给出一个多项式函数 $f(x,y)$ 的例子，
  使得值为 $100$ 的等值线恰好由七个点组成。
] <exer-level-seven>
