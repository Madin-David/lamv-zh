#import "@local/evan-zh:1.0.0":*

= 线性变换与矩阵

本章的目标是告诉你如何取一个线性变换，
并把它编码成一个矩阵。
换言之，这里只涉及一个方法。
不过，这样讲述的一个好处是，我终于能解释
为什么矩阵乘法会按你学过的那种方式定义。

本章的讲法与你将在许多其他地方看到的略有不同；
我先讲线性变换，
然后才把矩阵作为线性变换的编码来讨论。
我相当坚定地认为这种讲法更好，但如果你正在上一门实际的课程，
他们的讲法很可能不同（而且更差）。

== [TEXT] 线性变换 <sec-lintransf-def>

我即将给出的定义是 18.700/18.701 中线性变换的定义，
但我坚决认为这个定义比 18.02 中的那个更好。

#definition(title: [线性变换的定义])[
  一个_线性变换_ $T : RR^n -> RR^m$ 是_任意_满足
  两条公理 $T(c bf(v)) = c T(bf(v))$ 与 $T(bf(v) + bf(w)) = T(bf(v)) + T(bf(w))$ 的映射。
]
所以它是个笨重的大家伙：对每个 $bf(v) in RR^n$，都有一个输出值 $T(bf(v)) in RR^m$。
公理的事先不用太担心；现在先读例子就好。

#example(title: [线性变换的例子])[
  以下都是 $RR^2$ 到 $RR^2$ 的线性变换：

  - 常值函数，即对每个向量 $bf(v)$ 都有 $T(bf(v)) = bf(0)$。
  - 到 $x$ 轴的投影：$T(vec(x,y)) = vec(x,0)$。
  - 旋转一个角度。
  - 关于一条直线的反射。
  - 到直线 $y = x$ 上的投影。
  - 乘以任意 $2 times 2$ 矩阵，例如公式
    $ T(vec(x,y)) = vec(x+2y,3x+4y) $
    也是一个线性变换。
]
#tip[
  注意在任何线性变换中都有 $T(bf(0)) = bf(0)$。
]

要理解的重要原则是：如果你知道一个变换 $T$
在足够多点的取值，你就能还原出其余的取值。

下面从一个简单的例子开始：

#sample[
  若 $T : RR^2 -> RR^2$ 是线性变换，且已知
  $ T( vec(3,4) ) = vec(pi, 9) " 与 " T( vec(100, 100) ) = vec(0, 12) $
  那么 $T( vec(103,104) )$ 和 $T( vec(203, 204) )$ 这两个向量是多少？
]
#soln[
  $
  T( vec(103,104) ) = vec(pi, 9) + vec(0, 12) = vec(pi, 21) \
  T( vec(203,204) ) = vec(pi, 9) + 2 vec(0, 12) = vec(pi, 33). #qedhere
  $
]

再来看另一个例子。
#sample[
  若 $T : RR^2 -> RR^2$ 是线性变换，且已知
  $ T( vec(1,0) ) = vec(1, 3) " 与 " T( vec(0,1) ) = vec(2, 4) $
  那么 $T( vec(50, 70) )$ 是多少？
]
#soln[
  $ T( vec(50, 70) ) = 50 vec(1,3) + 70 vec(2,4) = vec(190, 430). #qedhere $
]

更一般地，第二个问题表明：如果你知道
$T( vec(1,0) )$ 和 $T( vec(0,1) )$，
你就应该能够_计算_出 $T$ 在任意其他向量（例如 $vec(50,70)$）处的输出。
再展开说明一下：
#eqn[
  $ T( vec(a,b) ) = a T( vec(1,0) ) + b T( vec(0,1) ). $
  <t-apply>
]

更一般地，通过理解上面两个问题的解答，
你应该能理解下面这个我们将会反复用到的重要陈述。

#memo[
  设 $T : RR^n -> RR^m$ 是线性变换。
  如果你知道 $T$ 在一个基上的输出，
  那么你就能推出 $T$ 在任意其他输入处的取值。
]

就目前而言，“基”指的只是那 $n$ 个向量 $ee_1$, ..., $ee_n$。
但之后我们还会把这个概念推广到其他一些情形。

== [RECIPE] 矩阵编码

一个_矩阵_是用尽可能少的数字来_编码_ $T$ 的_输出_的方式。
也就是说：

#definition[
  一个矩阵通过把 $T(ee_1)$, ..., $T(ee_n)$ 的*输出*写成*列向量*的列表，
  来*编码一个线性变换 $T$ 的全部输出*。
]

例如，如果你有 $T : RR^2 -> RR^2$ 且
$ T( vec(1,0) ) = vec(1, 3) " 与 " T( vec(0,1) ) = vec(2, 4)
  <==> T " 编码为 " mat(1,2;3,4). $

把它写成方法的形式：
#recipe(title: [编码一个变换的方法])[
  给定变换 $T : RR^n -> RR^m$，要把它编码成矩阵：

  1. 计算 $T(ee_1)$ 直到 $T(ee_n)$，并把它们写成列向量。
  2. 把它们拼在一起，得到一个 $m times n$ 的数字阵列。
]

再来看更多例子。

#sample[
  设 $T : RR^2 -> RR^2$ 是到 $x$ 轴的投影。
  把 $T$ 写成一个 $2 times 2$ 矩阵。
]
#soln[
  注意
  $ T(vec(1,0)) = vec(1,0) #h(2em) T(vec(0,1)) = vec(0,0). $
  把它们拼在一起，就得到 $T$ 对应的矩阵 $ T = mat(1,0;0,0). #qedhere $
]
#remark[
  你也许会注意到，用编码出的矩阵去乘确实满足
  $ mat(1,0;0,0) vec(x,y) = vec(x,0). $
  这与你的预期相符：$vec(x,0)$ 的确就是 $vec(x,y)$ 到 $x$ 轴的投影！
  而且这对每个线性变换都成立。
  这一点太重要了，我下一节还会再说一遍，这里只是先提一下。
]

#sample[
  设 $T : RR^2 -> RR^2$ 是关于直线 $y = x$ 的反射。
  把 $T$ 写成一个 $2 times 2$ 矩阵。
]
#soln[
  注意
  $ T(vec(1,0)) = vec(0,1) #h(2em) T(vec(0,1)) = vec(1,0). $
  把它们拼在一起，就得到 $T$ 对应的矩阵 $ T = mat(0,1;1,0). #qedhere $
]

#sample[
  设 $T : RR^2 -> RR^2$ 是绕原点逆时针旋转 $30 degree$。
  把 $T$ 写成一个 $2 times 2$ 矩阵。
]
#figure(
  image("figures/matrix-rot.svg", width: auto),
  caption: [旋转 $30$ 度。],
) <fig-rotate-30>
#soln[
  见图 @fig-rotate-30。
  通过观察单位圆，我们看到
  $ T(bf(e_1)) = vec(cos 30 degree, sin 30 degree) = (sqrt(3)/2, 1/2). $
  向量 $ee_2$ 再往前 $90 degree$，
  $ T(bf(e_2)) = vec(cos 120 degree, sin 120 degree) = (-1/2, sqrt(3)/2). $
  把它们拼在一起，就得到 $T$ 对应的矩阵
  $ T = mat(sqrt(3)/2, -1/2; 1/2, sqrt(3)/2). #qedhere $
]

#remark(title: [注：旋转矩阵的来历])[
  如果你把这道题中的 $30 degree$ 换成任意角 $theta$，会得到答案
  $ T = mat(cos theta, cos(theta+90 degree); sin theta, sin(theta+90 degree)). $
  这就是对应于旋转的矩阵。
  然而，在文献中你常常会看到它被改写为
  $ T = mat(cos theta, -sin theta; sin theta, cos theta) $
  以消去 $+90 degree$ 的偏移。
  这没什么问题，但个人认为它在某种程度上掩盖了旋转矩阵公式的来历。
]

另一个例子是恒等函数：
#example(title: [例：单位矩阵对得起它的名字])[
  设 $I : RR^3 -> RR^3$ 表示三维恒等函数，即 $I(bf(v)) = bf(v)$。
  要编码它，我们看它在 $ee_1$, $ee_2$, $ee_3$ 处的取值：
  $
    I(ee_1) = ee_1 = vec(1,0,0),
    #h(1em)
    I(ee_2) = ee_2 = vec(0,1,0),
    #h(1em)
    I(ee_3) = ee_3 = vec(0,0,1).
  $
  我们通过把各列并排写出来把它编码成矩阵，得到你所预期的结果：
  $ I " 编码为 " mat(1,0,0;0,1,0;0,0,1). $
  这给出了一个更自然的理由，说明为什么单位矩阵
  就是对角线上为 $1$、其他地方为 $0$ 的那个矩阵
  （相比你高中学到的“嗯，拿它乘一下试试”）。
]

== [SIDENOTE] 矩阵乘法 <sec-matrix-mult>

在预备知识中，我说过你应该知道矩阵相乘的法则，
所以你应该已经知道例如
$ mat(5,6;7,8) mat(1,2;3,4) = mat(23, 34; 31, 46). $

这个旁注的目的就是解释为什么矩阵乘法
会以这种奇怪的方式定义。
我们会看到两个结论：

- 用列向量 $bf(v)$ 去乘 $T$ 的矩阵，
  对应于求值 $T(bf(v))$。
- 把 $S$ 和 $T$ 的矩阵相乘，
  得到的是复合函数 $S compose T$ 的矩阵。#footnote[
  $compose$ 符号表示先作用 $T$ 再作用 $S$ 的那个函数。
  例如，若 $f(x) = x^2$ 且 $g(x) = x+5$，那么
  $(f compose g)(x) = f(g(x)) = (x+5)^2$。
  我们大多是在想指代 $f compose g$ 本身而不带 $x$ 时使用这个圆圈符号，
  因为如果写成 "$f(g$" 之类的东西会非常难看。
]

=== 一个矩阵

回忆 @sec-lintransf-def 中的例子：若 $T$ 是满足
$ T(ee_1) = vec(1,3) " 与 " T(ee_2) = vec(2,4) $
的线性变换，
那么 $ T( vec(50,70) ) = vec(190, 430). $
我们刚刚还看到，要把 $T$ 编码成矩阵，我们有
$ T = mat(1,2;3,4). $

现在，你认为按照高中所学去计算
$ mat(1,2;3,4) vec(50,70) $
会发生什么？
惊喜：你会得到
$ mat(1,2;3,4) vec(50,70) = vec(1 dot 50 + 2 dot 70, 3 dot 50 + 4 dot 70) = vec(190, 430). $
这不仅答案相同，连中间的计算也相同。
换言之，
#idea[
  用一个矩阵 $M$ 去乘列向量 $bf(v)$，
  对应于把 $M$ 所编码的线性变换 $T$ 作用到 $bf(v)$ 上。
]

=== 两个矩阵

现在，只要数学里有函数，我们就能_复合_它们。
所以让我们用一对函数 $S$ 和 $T$ 来玩同样的游戏，
并思考它们的复合 $S compose T$。
设想我们被问到下面这个问题：

#sample[
  设 $T : RR^2 -> RR^2$ 是线性变换，满足
  $ T( vec(1,0) ) = vec(1, 3) " 与 " T( vec(0,1) ) = vec(2, 4). $
  再设 $S : RR^2 -> RR^2$ 是线性变换，满足
  $ S( vec(1,0) ) = vec(5, 7) " 与 " S( vec(0,1) ) = vec(6, 8). $
  求 $S(T(vec(1, 0)))$ 与 $S(T(vec(0, 1)))$。
]
#soln[
  $
  S(T(vec(1, 0))) = S(vec(1, 3)) = 1 vec(5,7) + 3 vec(6, 8) = vec(23, 31) \
  S(T(vec(0, 1))) = S(vec(2, 4)) = 2 vec(5,7) + 4 vec(6, 8) = vec(34, 46). #qedhere
  $
]

现在，$S compose T$ _本身_就是一个函数，所以用上一道例题的答案
把 $S compose T$ 也编码成矩阵是合理的：
$
  S(T(vec(1, 0))) = vec(23, 31) " 与 "
  S(T(vec(0, 1))) = vec(34, 46)
  <==>
  S compose T " 编码为 " mat(23, 34; 31, 46).
$

矩阵乘法的法则于是被设计成能再次通过同样的计算
给出同样的答案：
$
  mat(5,6;7,8) mat(1,2;3,4) = mat(23, 34; 31, 46).
$

换言之：
#idea[
  把两个矩阵 $M$ 和 $N$ 相乘，
  对应于复合 $M$ 和 $N$ 所编码的线性变换。
]

这说明了为什么 18.700/18.701 的定义比 18.02 的更好。
在 18.02 中，矩阵乘法的法则是_定义_：
“这就是关于列与行取乘积的这条人为规则，相信我就行”。
但在 18.700/18.701 中，矩阵乘法的法则是_定理_；
它是把例题推广到八个变量（对 $n times n$ 矩阵则是
$n^2+n^2 = 2n^2$ 个变量）时会发生的事。

#digression[
  顺带一提，这应该能解释为什么矩阵乘法满足结合律却不满足交换律：

  - 因为 #link("https://w.wiki/BAHh")[函数复合满足结合律]，
    所以矩阵乘法也满足。
  - 因为函数复合一般来说_不_满足交换律，
    所以矩阵乘法也不满足。
]

== [EXER] 习题

#exer[
  设 $T : RR^2 -> RR^2$ 是把 $RR^2$ 中每个向量先绕原点
  逆时针旋转 $30 degree$、
  再关于直线 $y = x$ 反射的线性映射。
  把 $T$ 写成一个 $2 times 2$ 矩阵。
] <exer-matrix-rotrefl>
