#import "@local/evan-zh:1.0.0":*

= 梯度 <ch-grad>

$f : RR^n -> RR$ 的梯度，记作 $nabla f$，
是整个“多元微分”部分中最重要的一个概念。
虽然它的定义其实很容易计算，
我还是想好好解释一下它是从哪里来的。

在本章中，请记住两个重要的观点：

- 导数的目标是用线性函数去近似一个函数。
- 以前凡是用到斜率的地方，现在都应该改用法向量。

如果你想提前知道后面要讲什么，看下面这张表。

#figure(
  table(
    columns: 3,
    align: left,
    table.header([对象], [18.01], [18.02]),
    [输入], [$f: RR -> RR$], [$f: RR^n -> RR$],
    [输出], [$f': RR -> RR$], [$nabla f: RR^n -> RR^n$],
    [看作], [斜率（上升/水平距离）], [度量 $n$ 个方向_各自_的变化],
    [近似], [乘以小的水平增量], [与小位移作点积],
    [图像], [$x y$ 图像中切线的斜率], [等值线切线的法向量]
  ),
  kind: table,
  caption: [对多元函数如何看待 $nabla f$，
    并与 18.01 中的导数作比较。]
)

== [TEXT] 梯度把线性近似改写成一个点积 <text-gradient>

在 18.01 中，当 $f : RR -> RR$ 是函数、$p in RR$ 是输入时，
我们把单独一个数 $f'(p)$ 看作斜率，以便对它作几何解释。
现在我们进入了 18.02，我们有 $n$ 个不同的变化率，
但我们还没有讨论过如何对它作几何解释。

事实证明，正确的定义是把这 $n$ 个数组成一个向量。
请耐心听我说一句：

#definition[
  如果 $f(x,y)$ 是二元函数（即 $f : RR^2 -> RR$），
  那么 $f$ 的*梯度*（gradient），记作 $nabla f$，是通过
  把两个偏导数取作坐标而得到的函数 $RR^2 -> RR^2$：

  $ nabla f(x,y) = vec(f_x (x,y), f_y (x,y)). $

  $n$ 个变量的情形是类似的；
  例如若 $f(x,y,z)$ 是三变量函数，那么
  $ nabla f(x,y,z) = vec(f_x (x,y,z), f_y (x,y,z), f_z (x,y,z)). $
]

#typesig[
  这里的类型容易让人困惑。为继续强调类型安全：

  - 设 $f : RR^2 -> RR$ 接受 $RR^2$ 中的*点*并输出 $RR$ 中的*标量*。
  - 那么 $nabla f : RR^2 -> RR^2$ 接受 $RR^2$ 中的*点*并输出 $RR^2$ 中的*向量*。

  画图时请牢记点与向量的区别。
  我们总是把点画成圆点，把向量画成箭头。
]

定义这个梯度的理由在于，它让我们能用*点积*来做
线性近似，
因此点积在本章中会非常重要。
让我展示一下怎么做。
让我们回到我们的主角
$ f(x,y) = x^2+y^2 $
在点 $P = (3,4)$ 处。
在很久之前的 @example-circle-3-4（位于 #pageref(<example-circle-3-4>)）中，
我们算出了 $f_x (P) = 2 dot 3 = 6$ 和 $f_y (P) = 2 dot 4 = 8$，
并用它得到了近似
$
  f(P + chevron.l epsilon_x, epsilon_y chevron.r)
  &= f(chevron.l 3,4 chevron.r + chevron.l epsilon_x, epsilon_y chevron.r) \
  &= (3 + epsilon_x)^2 + (4 + epsilon_y)^2 approx 25 + 6 epsilon_x + 8 epsilon_y. $

现在，让我们能做几何的想法是：把这对数
$epsilon_x$ 和 $epsilon_y$ 换成一个“小位移”向量
$bf(v) = vec(epsilon_x, epsilon_y)$，
并把这对数 $6$ 和 $8$
换成向量 $vec(6,8)$，
这样*近似的那部分就变成了一个点积*：
$ f(vec(3,4) + bf(v)) approx f(vec(3,4)) + vec(6,8) dot bf(v). $

#warning(title: [警告：方向导数很糟糕])[
  有些地方你会看到缩写
  $D_bf(v) f (P) := nabla f (P) dot bf(v)$
  以及“方向导数”这个名字。
  我讨厌这个术语，因为不同的人有不同的记号与定义
  （根据 Wikipedia，有些作者要求 $bf(v)$ 必须是单位向量，等等）。

  所以我总是直接写点积 $nabla f (P) dot bf(v)$，
  它没有歧义，而且你要记的符号少了一个。
  梯度能做到方向导数能做的一切，而且做得更好。
]

在完全抽象的形式下，我们可以把线性近似改写为：
#memo(title: [记住：线性近似])[
  设 $f$ 在点 $P$ 处可微。
  那么对小位移向量 $bf(v)$，
  *线性近似*保证
  $ f(P + bf(v)) approx f(P) + nabla f(P) dot bf(v). $
  换言之，从 $f(P)$ 到 $f(P + bf(v))$ 的净变化
  由点积 $nabla f(P) dot bf(v)$ 近似。
]
到目前为止，我们所做的只是用不同的记号重写了之前的方程；
还没有引入任何新东西。
我们为什么要费这么大力气用不同的符号说同一件事呢？

重要的想法就是我很久以前告诉过你的：
*以前凡是用斜率思考的东西，现在都应该改用法向量来重新思考*。
事实证明，要把类比补完整到微分的情形，
那个法向量正是梯度 $nabla f(P)$，
我们稍后就会看到为什么（剧透：是因为点积）。
目前你只需要知道，$nabla f(P)$ _将会是_同时画出
全部 $n$ 个变化率的正确方式，尽管我还没有解释为什么。

在继续之前，让我们写下方法与一些例子，
以确保梯度的_定义_说得通，
然后我会解释为什么梯度正是我们完成类比所需要的那个法向量。

== [RECIPE] 计算梯度

#recipe(title: [计算梯度的方法])[
  1. 计算给定函数的每一个偏导数。
  2. 输出以这些偏导数作为分量的向量。
]

#sample[
  考虑来自 @text-compute-partial 和 @recipe-compute-partial 的
  下面六个函数：
  $
  f_1(x,y) = x^3 y^2 + cos(y), &#h(2em) f_2(x,y,z) = e^(x y z)  \
  f_3(x,y) = x^2 + y^2, &#h(2em) f_4(x,y,z) = x+y+z \
  f_5(x,y,z) = x y + y z + z x  &#h(2em) f_6(x,y) = x^y
  $

  计算它们的梯度。
]

#soln[
  把我们已经算出的偏导数取作分量：
  $
  nabla f_1 (x,y) = vec(3x^2 y^2, 2 x^3 y - sin(y)), &#h(2em)
  nabla f_2 (x,y) = vec( y z e^(x y z), x z e^(x y z), x y e^(x y z) ), \
  nabla f_3 (x,y) = vec(2 x, 2 y), &#h(2em)
  nabla f_4 (x,y,z) = vec(1,1,1), \
  nabla f_5 (x,y,z) = vec(y+z, x+z, x+y), &#h(2em)
  nabla f_6 (x,y) = vec(y x^(y-1), log(x) dot x^y). #qedhere
  $
  （记住 $log$ 是自然对数，不是以 $10$ 为底的对数。）
]

== [RECIPE] 线性近似

我们其实可以在定义偏导数之后
就立刻给出等价的方法，
但从概念上讲，我认为一切都用梯度来思考更好，
所以我等到定义完梯度之后才写这个方法。

#recipe(title: [线性近似的方法])[
  要对小位移向量 $bf(v)$ 做 $f(P + bf(v))$ 的线性近似：

  1. 计算 $nabla f (P)$，即 $f$ 在点 $P$ 处的梯度。
  2. 取点积 $nabla f(P) dot bf(v)$，得到一个数，即近似的变化量。
  3. 输出 $f(P)$ 加上上一步得到的变化量。
]

#sample[
  设 $f(x, y) = x^2 + y^2$。
  利用从 $(3,4)$ 出发的线性近似，近似 $f(3.01, 4.01)$ 的值。
]
#soln[
  通过取两个偏导数来计算梯度：
  $ nabla f(x, y) = vec(2x, 2y). $
  于是起点处的梯度向量为
  $ nabla f(3, 4) = vec(2 dot 3, 2 dot 4) = vec(6, 8). $
  目标点 $(3.01, 4.01)$ 与起点 $(3,4)$ 相差
  位移 $bf(v) = (0.01, 0.01)$。
  所以 $f$ 的近似变化量为
  $ underbrace(vec(6,8), = nabla f(3,4)) dot underbrace(vec(0.01, 0.01), =bf(v))
    = (6 dot 0.01 + 8 dot 0.01) = 0.14. $
  因此，
  $ f(3.01, 4.01) approx underbrace(f(3,4), = 25) + 0.14 = 25.14. #qedhere $
]


#sample[
  设 $f(x, y) = x^3 - y^3$。
  利用从 $(2,-1)$ 出发的线性近似，近似 $f(2.01, -1.01)$ 的值。
]
#soln[
  通过取两个偏导数来计算梯度：
  $ nabla f(x, y) = vec(3x^2, -3y^2). $
  于是起点 $(2,-1)$ 处的梯度向量为
  $ nabla f(2, -1) = vec(3(2)^2, -3(-1)^2) = vec(12, -3). $
  目标点 $(2.01, -1.01)$ 与起点 $(2,-1)$ 相差
  位移 $bf(v) = (0.01, -0.01)$。
  所以 $f$ 的近似变化量为
  $ underbrace(vec(12,-3), = nabla f(2,-1)) dot underbrace(vec(0.01, -0.01), =bf(v))
    = (12 dot 0.01 + (-3) dot (-0.01)) = 0.15. $
  因此，
  $ f(2.01, -1.01) approx underbrace(f(2,-1), = 9) + 0.15 = #boxed[$ 9.15 $]. #qedhere $
]

#sample[
  设 $f(x, y) = e^x sin(y) + 777$。
  利用从点 $(0,0)$ 出发的线性近似，近似 $f(0.04, 0.03)$ 的值。
]
#soln[
  通过取两个偏导数来计算梯度：
  $ nabla f(x,y) = vec(e^x sin y, e^x cos y). $
  于是起点 $(0,0)$ 处的梯度向量为
  $ nabla f(0,0) = vec(e^0 sin 0, e^0 cos 0) = vec(0,1). $
  目标点 $(0.04, 0.03)$ 与起点 $(0,0)$ 相差 $(0.04, 0.03)$。
  所以 $f$ 的近似变化量为
  $ underbrace(vec(0,1), = nabla f(0,0)) dot underbrace(vec(0.04, 0.03), =bf(v))
    = 0 dot 0.04 + 1 dot 0.03 = 0.03. $
  因此，
  $ f(0.04, 0.03) approx underbrace(f(0,0), =777) + 0.03 = #boxed[$ 777.03 $]. #qedhere $
]

== [TEXT] 梯度下降

在 @text-gradient 的结尾，我们承诺点积的几何定义
会带来回报。现在我们兑现这个承诺。

这里的问题是：
#question[
  设 $f(x,y) = x^2 + y^2$。
  想象我们站在点 $P = (3,4)$。
  我们想朝自己选定的某个方向走一步，步长为 $0.01$。
  例如，我们可以走到 $(2.99, 4)$、$(3, 4.01)$ 或 $(2.992, 4.006)$，
  或者下图中所标出的圆上的任何其他点。
  （对第三个点，注意 $sqrt((3-2.992)^2+(4-4.006)^2) = 0.01$，
  所以那个点确实距离为 $0.01$。）

  - 如果我们想让新点处的 $f$ 值最大，应该朝哪个方向走？
  - 如果我们想让 $f$ 的值大致保持不变，应该朝哪个方向走？
  - 如果我们想让新点处的 $f$ 值最小，应该朝哪个方向走？
]

你可以在 @fig-grad-descent 中看到这个情景的示意图。
注意此图不是按比例画的，因为 $0.01$ 太小，无法清楚地画出，
所以黑色圆画得比它实际大得多。

#figure(
  image("figures/grad-gdescent.svg", width: auto),
  caption: [从 $P = (3,4)$ 出发，我们走一步 $bf(v)$，其中 $|bf(v)|=0.01$。
    未按比例绘制。],
) <fig-grad-descent>

为了回答这个问题，我们现在使用点积的几何解释。
记住 $f$ 的变化量近似为
$ f(P + bf(v)) - f(P) approx nabla f (P) dot bf(v). $
点积的几何定义是它等于
$ nabla f (P) dot bf(v) = |nabla f(P)| |bf(v)| cos theta $
其中 $theta$ 是夹角。
但 $|nabla f(P)|$ 是固定的（在本例中它是 $sqrt(6^2+8^2)=10$），
而 $|bf(v)|$ 也是固定的（在本例中我们取它为很小的数 $0.01$）。

所以实际上我们关心的只有夹角 $theta$！稍微想一想这件事。
然后回忆余弦函数是怎么运作的：

- $cos(0 degree) = 1$ 是余弦的最大值，
  它出现在 $bf(v)$ 与 $nabla f(P)$ 指向同一方向时。
- $cos(180 degree) = -1$ 是余弦的最小值，
  它出现在 $bf(v)$ 与 $nabla f(P)$ 指向相反方向时。
- 如果 $nabla f (P)$ 与 $bf(v)$ 垂直
  （即 $theta = 90 degree$ 或 $theta = 270 degree$），那么点积为零。

翻译成：

#memo[
  - *沿着*梯度方向移动，可以最快地增大 $f$。
  - *逆着*梯度方向移动，可以最快地减小 $f$。
  - *垂直于*梯度方向移动，可以避免 $f$ 朝任何一个方向变化太多。
]

== [TEXT] 切线与切平面的法向量

我们只需要再补一个想法：
_让 $f$ 大致保持不变应当对应于沿切线或切平面移动_。

确实，在二维情形中，切线是与等值线最“贴合”的直线，
所以我们把它看作使 $f$ 尽量不发生变化的方向。
在三维情形中，等值面的切平面也是如此；
该平面在点 $P$ 附近紧贴曲面。
所以这意味着最后一条可以改写为

#memo[
  梯度 $nabla f (P)$ 垂直于 $P$ 处的切线/切平面。
  它指向使 $f$ 增大的方向。
]
#example[
  在前面关于等值线的例子中，梯度指向背离内部的方向。
  这在一般情况下并不成立。
  例如，想象换成函数
  $ f(x,y) = 1 / (x^2 + y^2). $
  点 $(3,4)$ 位于 $f(3,4) = 1/25$ 的等值线上。
  $f(x,y)$ 取值为 $1/25$ 的等值线_也_是半径为 $5$ 的圆，
  因为它对应方程 $1/(x^2+y^2) = 1/25$。

  然而，梯度看起来相当不同：经过足够的计算可得
  $ nabla f(x,y) = vec((-2x) / ((x^2+y^2)^2), (-2y) / ((x^2+y^2)^2)). $
  在 $(3,4)$ 处求值，我们得到
  $ nabla f(3,4) = vec(-6/625, -8/625). $
  因此，对于函数 $f(x,y) = 1/(x^2+y^2)$，
  画出与 @fig-grad-descent 类似的图形，得到的图看起来非常相似，
  只不过绿色箭头指向_相反_的方向，而且小得多。
  这是合理的：当你_朝_原点移动时，你会预期 $1/(x^2+y^2)$ 变大。
  见 @fig-grad-descent-2。
]

#figure(
  image("figures/grad-gdescent2.svg", width: auto),
  caption: [类似的一张图，但针对 $f(x,y) = 1/(x^2+y^2)$。
    它看起来与 @fig-grad-descent 非常相似，但现在梯度指向相反方向，
    而且绝对值小得多，这表明
    当我们_朝_中心移动时 $f$ 的值增大（但只是略微增大）。
    未按比例绘制。]
) <fig-grad-descent-2>

#remark[
  回到课程线性代数部分的三维几何中，
  我们通常既不知道也不关心法向量的符号和大小。
  也就是说，当被问到“平面 $x-y+2z=8$ 的一个法向量是什么？”时，
  你可以回答 $vec(1,-1,2)$ 或 $vec(-1,1,-2)$，甚至 $vec(-100, 100, -200)$。
  但这不再适用于梯度：
  虽然它是切线/切平面的法向量，
  它的模长却携带了额外的信息，我们不应该随手丢掉。
]

== [RECIPE] 计算等值线/等值面的切线/切平面

到这里，我们就能轻松计算切线和切平面了。
我们把 @recipe-plane-known-dir 中的老方法
（已知法向量和一个点求平面）
应用到以 $nabla f (P)$ 为法向量的情形。
具体写出就是：

#recipe(title: [方法：等值线/等值面的切线/切平面])[
  要求函数 $f$ 的等值线/等值面在点 $P$ 处的切线/切平面：

  1. 计算梯度 $nabla f$。它是一个法向量，
    所以它告诉你直线/平面方程的左端。
  2. 调整右端使它经过 $P$，就像 @recipe-plane-known-dir 中那样。
]

#sample[
  计算 $x^2 + y^2 = 25$ 在点 $(3, 4)$ 处的切线。
]
#soln[
  令 $f(x,y) = x^2 + y^2$，于是我们在看 $f$ 取值为 $25$ 的等值线。
  我们已经看到
  $ nabla f = vec(2x, 2y) ==> nabla f(3,4) = vec(6, 8). $
  因此，切线应当具有形式
  $ 6x + 8y = d $
  其中 $d$ 是某个数。
  要经过 $P = (3,4)$，我们需要 $d = 6 dot 3 + 8 dot 4$，所以答案是
  $ #boxed[$ 6 x + 8 y = 50 $]. #qedhere $
]

#sample[
  计算 $y = x^2 + 5$ 在点 $(3,14)$ 处的切线。
]
#soln[
  这不是一道 18.01 的题吗？
  是的，但用等值线在这里也同样好用。
  我们把这抛物线看作 $f(x,y) = y - x^2$ 取值为 $5$ 的等值线。
  于是梯度为
  $ nabla f = vec(-2x, 1) ==> nabla f(3,14) = (-6, 1). $
  因此切线应当具有形式 $-6x+y=d$，其中 $d$ 是某个数。
  我们需要它经过 $(3,14)$，所以取 $d = (-6) dot 3 + 14 = -4$，得到答案
  $ #boxed[$ -6 x + y = -4 $]. $
  （写成 18.01 的形式就是 $y = 6 x - 4$，这不应让人惊讶，
  因为我们知道 $x^2+5$ 在 $x=3$ 处的导数是 $6$。）
]

== [RECAP] Echo 部“多元微分”回顾

让我们总结最近几节。

- 我们把 18.01 中使用的旧图像换成了等值线和等值面图像，
  见 @ch-level。
  这些新图像与 18.01 的图像不同，因为坐标轴上的所有变量
  现在都是输入，我们对它们一视同仁。
- 我们在 @ch-partial 中解释了如何求 $f(x,y)$ 或 $f(x,y,z)$
  的偏导数，它度量的是仅其中一个变量的变化。
- 我们用这些偏导数在 @ch-grad 中定义了梯度 $nabla f$。
  这使线性近似变成了点积，
  其中对小位移 $bf(v)$ 有 $f(P + bf(v)) approx f(P) + nabla f(P) dot bf(v)$。
- 利用点积的几何解释，$nabla f (P)$ 是 $f$ 过 $P$ 的等值线的
  一个法向量，并且：
  - 沿梯度方向走，$f$ 增大得最快
  - 逆梯度方向走，$f$ 减小得最快
  - 垂直于梯度方向走，你就会沿 $P$ 处的切线或切平面移动。

== [EXER] 习题

#exer[
  计算球面 $x^2 + y^2 + z^2 = 14$
  在点 $(1,2,3)$ 处的切平面方程。
] <exer-sphere-123>

#exer[
  某个可微函数 $f(x,y)$ 取值为 $-7$ 的等值线
  是一条以 $(0,0)$ 为圆心、半径为 $2$ 的圆。

  - 给出一个这样的函数 $f$ 的例子。
  - $nabla f(1.2, -1.6)$ 可能是哪些向量？
  - 从点 $(1.2, -1.6)$ 出发，用线性近似估计 $f(1.208, -1.594)$。
] <exer-level-grad-poss>

#exer[
  对每一小题，要么给出 $f : RR^2 -> RR$ 的例子，要么说明它不存在。

  - 你能找到一个函数 $f : RR^2 -> RR$ 使得 $nabla f (x,y) = chevron.l x, y chevron.r$ 吗？
  - 你能找到一个函数 $f : RR^2 -> RR$ 使得 $nabla f (x,y) = chevron.l 100 x, y chevron.r$ 吗？
  - 你能找到一个函数 $f : RR^2 -> RR$ 使得 $nabla f (x,y) = chevron.l y, x chevron.r$ 吗？
  - 你能找到一个函数 $f : RR^2 -> RR$ 使得 $nabla f (x,y) = chevron.l 100 y, x chevron.r$ 吗？
] <exer-antigrad-preview>

#exerstar[
  设 $a$、$b$、$c$、$d$ 是非零实数，并设 $ f(x,y) = a e^(x+y) + b e^(x-y). $
  设 $f$ 取值为 $c$ 的等值线在原点处与直线 $y = 5x$ 相切，
  并且它还经过 $(0,d)$。计算 $d$。
] <exer-grad-abc>
