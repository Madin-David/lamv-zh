#import "@local/evan-zh:1.0.0":*

= 俯瞰 Golf 部的打气

本章通篇都是一次打气。方法（recipe）和细节我们会留到后面的小节再讲。

== [TEXT] 积分的大表格

18.02 剩下的内容将涉及一大堆不同的积分。
如果你到这里为止一直听从我的建议、注意类型安全，那它会在这里帮到你。
我（Evan）坦然承认，在 18.02 的这一部分里我自己也经常犯类型错误，
所以不要放松警惕。

#figure(
  image("figures/integrals-triangle.svg", width: 100%),
  caption: [
    对每个 $0 <= d <= n <= 3$，我们画出相应类型的积分并给它起个名字。
    可在 #url("https://web.evanchen.cc/textbooks/poster-ints.pdf") 下载。
  ]
) <poster-triangle>

记住：
#idea[
  设给定 $f : RR^n -> RR$，且 $0 <= d <= n$。
  $f$ 的 $d$ 维积分的目的是
  把 $f$ 在 $RR^n$ 中某个 $d$ 维对象上的所有取值加起来。
]

例如，这个想法甚至在 $d = 0$ 时也说得通！
在 18.02 中，$0$ 维对象就是一个点（或一堆点），
而要求 $f$ 在某个点处的值，直接代入即可。
所以从哲学上说，$0$ 维积分就只是 $f$ 在某些点处取值的有限和。
我提起这个退化情形也许显得很蠢，
但后面我们讲散度/梯度/旋度时会发现，$0$ 维情形是有关的。

我们在 @poster-triangle 中给出了一张巨大的图表。
（这张图表太大，页面有点放不下，
但你可以下载
#link("https://web.evanchen.cc/textbooks/poster-ints.pdf")[大尺寸 PDF 版本]）。
这张图表列出了十种不同的积分，
对应每个满足 $0 <= d <= n <= 3$ 的 $(d,n)$。
下面简要介绍一下图表中的内容。

- $d = 0$ 的情形很蠢，正如我刚才所说，
  它出现在这里只是因为我后面会引用它。
- $d = 1$ 且 $n = 1$ 的情形在 18.01 中已经讲过。
  就是那个熟悉的老朋友：利用原函数、通过微积分基本定理计算的单变量积分。

- 在那之后，概念上最简单的情形其实是 $d = n = 2$ 和 $d = n = 3$
  ---也就是对角线上的那些。
  一般来说，当 $n=2$ 时它们可以称为*二重/面积积分*，
  当 $n = 3$ 时称为*三重/体积积分*。
  稍后我们会谈一点在实践中如何计算它们，
  但好消息是，很多时候你只要把 18.01 的老积分串联起来即可；
  有时候你甚至不需要参数化。

- 当 $d = 1$ 且 $2 <= n <= 3$ 时，得到的是*线积分*。
  其想法是：你在 $RR^n$ 中有一条轨迹，
  它由某个参数方程 $bf(r) : RR -> RR^n$ 定义。
  你还有一个函数 $f : RR^n -> RR$。
  线积分让你把 $f$ 沿这条轨迹的取值加起来。

  结果它其实只是一个_单个_的 18.01 积分。
  通常你的路径由单个变量 $t$ 参数化。
  所以，尽管积分号内的表达式
  $ integral_(t_0)^(t_1) f(bf(r(t))) |bf(r)'(t)| dif t $
  看起来可能很吓人，
  如果你真的拿到了具体的 $f$ 和 $bf(r)(t)$，那么你_真正_拥有的只是
  $ integral_(t_0)^(t_1) ["expression involving only " t] dif t $
  它是一个 18.01 积分！
  所以这是你已经会做的事情。

  换言之，如果 $d=1$ 且 $n > 1$，
  你基本上可以立刻把它替换成沿参数化线段的一个单积分。
  换言之，*线积分可以直接转化为 18.01 的单积分*。

- 当 $d = 2$ 且 $n = 3$ 时，我们得到的是*曲面积分*。
  要计算它们，你通常得参数化一个_曲面_；
  但由于曲面是二维的，
  你不再用带时间参数 $t$ 的 $bf(r)(t)$，
  而是用带两个参数 $u$ 和 $v$ 的 $bf(r)(u,v)$ 来描述曲面。
  这让它们稍微麻烦一点。

  但和线积分一样，在把参数化那一摊做完之后，
  曲面积分会转化为一个 $2$ 变量的面积积分。
  换言之，*曲面积分可以直接转化为面积积分*。

所以底部的那三个 ---二维/三维的线积分与曲面积分---
最终只是单积分和二重积分的特例。
后面我们会看到一些例子；
但实际上它们会是 Golf 部里_最后_才讲的内容。
Golf 部的大部分篇幅反而留给了二重积分和三重积分。

== [TEXT] 关于底部那三个的警告 <sec-warning-trio>

@poster-triangle 中的这些积分更恰当的称法是
*标量场线积分*和*标量场曲面积分*，以强调
这是对_标量_函数 $f : RR^2 -> RR$ 或 $f : RR^3 -> RR$ 的积分。
你不太会见到这个术语，原因在于下面这个重要的提醒：
*@poster-triangle 中的线积分和曲面积分很少被用到*。

在 18.02 中，我们用到它们的场合只有：
- 弧长（$d=1$）和表面积（$d=2$）
- 与物体（例如 $d=1$ 的金属丝，或 $d=2$ 的金属板）的
  质量或质心有关的问题。

在本书中，这会出现在 @ch-scalint 里，而在那之后，
线积分和曲面积分将总是
*向量场*的版本（在 India 部和 Juliett 部中）。
如果你想提前翻看，
可以瞄一眼 @poster-stokes 和 @table-purples。

== [TEXT] 当 $d = n$ 且 $n >= 2$ 时这些积分如何计算的想法

所以正如我刚才所说，现在先把注意力放在 $d = n = 2$ 或 $d = n = 3$
（也就是图表 @poster-triangle 中的二重积分格和三重积分格）。

最简单的情形是你所积分的区域是矩形或棱柱。
尽管由于积分号的个数它们看起来很吓人，
但在实际计算中，它们其实被认为是思考起来最“简单的情形”：

- 矩形上的二重积分就是两个 18.01 积分依次相接。
- 长方体上的三重积分
  确实就是三个 18.01 积分依次相接。

接着还有一些情形：$d = n = 2$ 或 $d = n = 3$，
但区域不是矩形的。
例如，也许在 $RR^2$ 中你想在圆盘 $x^2+y^2 <= 1$ 上做*面积积分*，
或者你想在球 $x^2+y^2+z^2 <= 1$ 上做*体积积分*。

- 即使在这种情形下，有时你仍可以不必换元
  就写出二重积分或三重积分。
  例如，圆盘上的积分
  $ integral.double_(x^2+y^2<=1) f(x,y) dif x dif y $
  其实可以改写为二重积分
  $ integral_(-1)^1 integral_(-sqrt(1-y^2))^(sqrt(1-y^2)) f(x,y) dif x dif y. $
  尽管它因为积分限是表达式而不是数而看起来更吓人，
  它并不需要任何新技术。
  它真的就只是两个 18.01 积分，一个接一个。

- 如果改写成二重或三重积分行不通，
  那么策略就改为*换元*。
  这个方法将在 @ch-chvar 中详细展开。

总结一下
#idea[
  每当你试图计算 @poster-triangle 中的某个多元积分时，
  你的目标都是把它转化为一个看起来是矩形的单/二重/三重积分，
  然后反复使用你 18.01 的老方法来求值。
]

这其实是极其、极其好的消息！
你也许还记得，在 18.01 里计算像 $integral e^x sin(x)$ 这样单变量函数的积分，
嗯，很难！#footnote[顺便说一句，它是 $1/2 e^x (sin(x) - cos(x)) + C$。]
计算原函数一点也不容易；事实上，它如此不平凡，以至于 MIT 的学生
举办了一个 #link("https://en.wikipedia.org/wiki/Integration_Bee")[叫做 integration bee 的活动]，
它就像拼字比赛（spelling bee）的积分版（我没开玩笑）。
你也许曾担心，在 18.02 里你可能需要学一些更加可怕的东西。

但不必，你不需要！
这很像你起初可能会被多元微分吓到，
看到 $nabla f$ 或偏导数这些符号，
直到你意识到计算偏导数其实是
一件你在 18.01 里_早就已经会做_的事。

多元积分也会是这样。
真正的挑战不会是原函数——它们和 18.01 里没有区别。
真正困难的部分其实是弄清楚积分的_积分限_！
